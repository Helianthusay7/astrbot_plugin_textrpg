# 更新日志

> 修仙文字 RPG · AstrBot 插件
> 版本号遵循 `metadata.yaml` 的 `version` 字段。

---

## 调整 — 属性面板排版（2026-09-21）

> **起因（老板原话）**：「这个属性面板能不能连在一起，不然分开最后发个图片
> 好奇怪，我希望把图片放在开头，后面的战绩可以分出去，而且修为下面的乘区栏
> 不要写上去，看着好奇怪」
>
> **补充澄清（第二轮）**：「我指的是图片和文字放在一起，一个消息框包括图片和
> 文字，图片在文字上面。而且我指的战绩就是完整的这个
> `秘境最深 / 秘典 / 战绩` 三行 —— 你怎么把前两个融合进去了，
> 我说了原本就是分离的是对的啊」

⚠️ 第一轮我把两点都理解错了，记录下来免得再犯：

1. 以为「图片放在开头」= 先单独发一条图片，**再**发面板 → 实际要的是
   **同一条消息里图片在上、文字在下**；
2. 以为「战绩分出去」= 只把 `战绩：` 那一行拆出来 → 实际那三行
   （秘境最深 / 秘典 / 战绩）是**一组**，要一起拆，前两行不能并回主面板。

### 改动

| 项 | 改前 | 改后 |
|---|---|---|
| 形象 | 面板之后单独一条图片 | **与面板同一条消息**（图文同框，图片在上） |
| 面板本体 | 被 `max_chunk_chars`（默认 180）切成 3~4 条 | **整块一条**（`_reply(single=True)`） |
| 进度区 | 秘境 / 秘典 / 战绩混在面板末尾 | **三行一组，单独一条** |
| 乘区栏 | `乘区：境界 ×1.00 × 转生 ×1.00 × 突破 ×1.00 = ×1.00` | **移除** |

### 最终形态（两条消息）

```
第 1 条   ┌──────────────────────┐
          │  [角色形象图]         │   ← 图片在上
          │  📋 七元. 的道途      │
          │  境界：炼气期 1 转     │   ← 文字在下
          │  修为：0/784          │      同一条消息
          │  ...                 │
          │  穿甲：0%　套装：（0/5）│
          └──────────────────────┘

第 2 条   秘境最深：第 5 层
          秘典：未装配　（装配位 1）
          战绩：历练胜 12　道消 3
```

没有形象的玩家就是两条纯文本，不会空发图片。

### 图文同框怎么做的

`_reply()` 新增 `image` 参数，**第一段**走 `chain_result` 拼成一条：

```python
if image and idx == 0:
    yield event.chain_result(
        [Comp.Image.fromFileSystem(image), Comp.Plain(chunk)]
    )
else:
    yield event.plain_result(chunk)
```

`Comp.Image.fromFileSystem()` 会把本地路径转成 `file://` URI
（`components.py:519`）。**必须用 chain_result** ——
`plain_result` 只能发文字，`image_result` 只能发图，两者都是单独一条消息。

`_reply()` 的 `single` 参数（不分段）同时保留，`/属性` 两个一起用。

### 顺带修掉一个旧 bug：气血行的双括号

```
改前：气血：120/120　（120　（120 × 1.00））    ← 括号套了两层
改后：气血：120/120　（120 × 1.00）
```

根因：旧写法把 `_stat_line()` 的**完整输出**（本身已经带括号）
再塞进一层括号里。现在把「拆解串」抽成 `_stat_parts()`，
由 `_stat_line()` 和气血行共用，加括号只有一处。

### 回归断言

| 断言 |
|---|
| 有形象时只发 2 条：图文面板 + 进度战绩 |
| 第 1 条图文同框（既有图片又有面板文字） |
| 图片排在文字前面（图片在上） |
| 面板没被分段切碎（整块一条） |
| 第 2 条含秘境行 / 秘典行 / 战绩行 |
| 主面板里不再含这三行 |
| 面板不再显示「乘区」栏 |
| 气血行括号只有一层（不再套两层） |

> 为此在 `selftest.py` 新增了 `run_cmd_parts()`（返回**每条 yield 的原始
> 对象**）和 `_ChainResult` 桩（把消息链拼成可读文本）。
> 「图文是一条还是两条」「图片在文字前还是后」这类问题，
> 用原来的 `run_cmd()` 拼成一个大字符串根本看不出来。

---

## 修复 — 热重载会清空整份存档（2026-09-21，P0 数据安全）

### 现象

线上 `data/plugin_data/astrbot_plugin_textrpg/`：

```
players.json      2 字节   {}     ← Sep 21 22:34
players.json.bak  2 字节   {}     ← Sep 21 22:34（备份也空了）
avatars/1095489827_3455845639.png  183030 字节  ← 22:37，玩家刚传的形象还在
```

玩家角色全部蒸发，而头像文件好端端躺在磁盘上 ——
这就是老板看到「角色形象不见了」的直接原因。

### 根因

`data.py` 的 `PlayerStore` 是**懒加载**的：

```python
self._cache: dict[str, Player] = {}
self._loaded = False          # 首次访问才 _load_sync()
```

而 `_write_sync()` **直接写盘，没有任何「已加载」检查**，
`main.py` 的 `terminate()` 又无条件 `await self._commit()`：

```python
async def terminate(self):
    """插件卸载/停用时保存存档。"""
    await self._commit()       # ← 热重载 / 卸载必触发
```

于是这条路径会毁档：

```
插件实例启动（_loaded=False，_cache={}）
   ↓ 还没处理任何一条消息
热重载 → terminate() → _commit() → save() → _write_sync()
   ↓
payload = {}                      ← 用空缓存序列化
os.replace(players.json, .bak)    ← 旧存档挪成备份（但备份紧接着也被覆盖）
os.replace(.tmp, players.json)    ← 写入 {}
```

**结果是主存档和备份同时变成 `{}`，玩家数据不可恢复。**
本地反复改代码 + 热重载，正好反复命中这条路。

### 修复

`_write_sync()` 开头加守卫 —— **没加载过就不许写盘**：

```python
if not self._loaded:
    logger.warning(
        "[textrpg] 存档尚未加载，跳过本次写入以免用空缓存覆盖玩家数据"
    )
    return
```

代价是这一次写入被跳过（无害，因为没有任何内存改动需要落盘），
收益是玩家数据不可能因为一次热重载而蒸发。

### 回归断言（`selftest.py [52]`）

| 断言 | 类型 |
|---|---|
| `_write_sync` 源码里出现 `self._loaded` | 源码级 |
| 磁盘有存档 → 新建 store 但不访问 → `save()` → 内容仍在 | **行为级** |
| 正常 `ensure_loaded()` 后 `save()` → 内容保留（保护不误伤正常写入） | 行为级 |

---

## 新增 — 属性面板显示角色形象（2026-09-21）

> **起因（老板原话）**：「为什么属性面板看不到我的角色形象，
> 我设计这个形象不就是看的吗」

### 根因

不是「没显示」，是**这个功能从来没做**：

- `profile()` 里拼 `lines` 时**完全没碰过 `player.avatar`**；
- `_avatar_abs_path(rel)`（`main.py:479`）**定义了，但全项目零调用**
  （`grep -rn "_avatar_abs_path"` 只命中定义处那一行）。

玩家捏脸时上传的图被 `_save_avatar()` 老老实实写进了
`data/plugin_data/<插件>/avatars/<群>_<QQ>.png`，存档里也记了相对路径，
但**没有任何一处把它发出来**。

### 修复

`profile()` 在文本面板 yield 完之后，追加一条图片消息：

```python
async for _chunk in self._reply(event, "\n".join(lines)):
    yield _chunk

_avatar_path = self._avatar_abs_path(player.avatar)
if _avatar_path is not None and _avatar_path.exists():
    try:
        yield event.image_result(str(_avatar_path))
    except Exception as exc:
        logger.error(f"[textrpg] 发送角色形象失败: {exc}")
```

### 为什么单独 yield，不塞进 `_reply`

`_reply()` 的唯一出口是 `event.plain_result()`，**只能发纯文本**。
图片必须走 `event.image_result()`
（`astr_message_event.py:408` → `MessageEventResult.file_image()`
→ `Image.fromFileSystem()`）。

所以：**文本照旧走 `_reply`**（保住分段 / 随机延迟 / 频率熔断三层保护），
**图片追加一条**。发图失败只记日志，不让整个面板失败。

### 回归断言（`selftest.py [52]`）

| 断言 |
|---|
| 新号 `avatar` 默认为空 |
| 无形象时 `/属性` 不发图片 |
| 有形象时 `/属性` 追加一条图片，路径 = 存档相对路径绝对化 |
| 有形象时文字面板照旧（图是追加不是替换） |
| `avatar` 指向的文件不存在时不崩、不发图 |
| `avatar` 为空串时不发图 |

> 测试桩 `AstrMessageEvent` 同步补了 `image_result()`
> —— 缺了它，插件里那句 `yield event.image_result(...)`
> 会被 `try/except` 吞掉，断言就打在兜底分支上，变成假绿。

---

## 修复 — 历练修为只给 1 点（2026-09-21 线上实测）

> **起因（老板原话）**：「怎么初始打怪怎么就加一点修为，
> 突破炼气二转（lv2）都需要 784 修为」

### 根因

`battle.py` 的 `EXP_SCALE_FIELD = 0.15` 是 **`monster.exp` 的倍数**，
不是「修炼收益的百分比」。而 `monster.exp` 本身很小 —— 1 转怪 `exp = 7`：

```
int(7 × 1.0(win) × 0.15) = 1 点
```

一次 `/修炼` 是 50 点。**打怪只有修炼的 0.16%**，升级要打 **784 场**。
旧注释写的「15% 的量级」是**比例**，实际绝对值差了 7.5 倍 ——
新人第一条线（打怪）完全没有正反馈。

### 改成什么

`0.15` → **`1.5`**。打怪收益的中位数变成修炼的 **21%**：

| 转数 | 修炼一次 | 打怪中位 | 比值 | 升级需要 | 打怪场次 |
|---|---|---|---|---|---|
| 1 | 50 | 10 | 20.0% | 784 | 78 |
| 10 | 100 | 21 | 21.0% | 2229 | 106 |
| 20 | 200 | 42 | 21.0% | 6688 | 159 |
| 40 | 800 | 171 | 21.4% | 59736 | 349 |
| 64 | 6400 | 1365 | 21.3% | 1179524 | 864 |

**单场实际在 10%~43% 之间浮动** —— `spawn_monster` 在「本境 ±1 境」的
池子里随机抽，怪物 `exp` 是离散档位（7 / 14 / 28 / 57 / 114 / 228 / 455 / 910）。
抽到越境强敌收益更高，这个方差是**刻意的**（有惊喜感）。

打怪依旧不是升级主路：修炼约 600 点/小时，打怪约 126 点/小时。
定位仍是「灵石与物品来源」，只是零头终于看得见了。

### 回归断言

`selftest.py` 改 4 项、新增 3 项（927 → **931**）：

- `EXP_SCALE_FIELD == 1.5`，且确实是**乘在修为上的倍数**
- **1 转打怪所得 ≥ 5 点**（防退回「1 点」）
- **1 转打怪 ≈ 修炼的 15%~35%**
- **打怪/修炼 比值的中位数在各转数稳定在 20%~25%**
- 打怪收益有档位方差（抽到越境强敌更高）

⚠️ 写第一版断言时踩了个坑：**只抽一只怪就断言比值**。
`spawn_monster` 是随机的，单次结果在 10%~43% 浮动，稳定的是**中位数** ——
第一版是个概率性假失败。现在改成采样 300 次取中位数 + 固定随机种子。

连跑 5 次全绿；`verify_v35_token` 57 / `verify_v34_special` 16 /
`verify_real_engine` 52 全绿。

---

## 改造 — 闭关 / 探索改成互斥的「挂机状态」（2026-09-21）

> **起因（老板线上实测）**：「我一开始只是说在闭关期间不能修炼/探索等操作，
> 但你怎么给我修炼之后闭关也进入冷却了」

### 根因：一个字段扛了三件事

`闭关` 压根没有「进行中」这个概念 —— 它只有一个 `last_train_ts`，
同时兼任 **① 开始计时 ② 结算的时间基线 ③ 冷却起点**。

对照 `探索`：它一开始就是 `explore_start_ts`（状态）+ `last_explore_ts`（冷却）
两个字段，所以从来没出过这个问题。

由此产生两个必现的 bug：

| # | 症状 | 代码位置（改前） |
|---|---|---|
| 1 | 进入闭关后 **1 小时内再发 `/闭关` 结算不了**，被冷却当成「刚出关不久，需调息」 | `main.py` 的 `seclusion_remain()` 拦在前面 |
| 2 | 结算过一次后 `last_train_ts` 恒为正 → `if last_train_ts <= 0` 的「进入闭关」分支**永远进不去**，闭关退化成「每小时领一次收益」的按钮 | `main.py` `if player.last_train_ts <= 0` |

另外，「闭关期间禁止修炼/探索/历练」这条**根本没实现** ——
`修炼` / `探索` / `历练` / `寻敌` / `秘境` 里都没有任何检查；
`/属性` 状态行也只显示「游历中」，看不到「闭关中」。

### 改成了什么（老板拍板）

| 项 | 决定 |
|---|---|
| 形态 | 闭关、游历**都是状态型挂机**（不再是冷却技能） |
| 进入 | `/闭关`、`/探索` |
| 结束 | **`/结束闭关`（别名 `/出关`）**、**`/结束探索`（别名 `/收工`、`/结束游历`）** |
| 兼容 | 再发一次同名指令也能结束（老玩家的操作习惯） |
| 互斥 | 同一时间只能挂一个；在 A 状态发 B 指令 → 提示先结束 A |
| 挂机期间 | 禁止 `修炼` / `历练` / `寻敌` / `秘境` |
| 结算后 | **无冷却**，可以立刻再挂 |
| 防刷 | 靠时长下限：闭关不足 6 分钟、游历不足 30 分钟无收益；上限 8 小时 |
| 文案 | `/属性` 状态行新增「闭关中，已过 …」；死亡休整的「闭关疗伤中」改名「疗伤中」（避免和真正的闭关撞词） |

### 代码改动

| 文件 | 改动 |
|---|---|
| `player.py` | 新增字段 `train_start_ts`（闭关开始时间，0 = 不在闭关）；新增 `is_in_seclusion()` / `seclusion_elapsed()` / `is_idle()` / `idle_name()`；`SECLUSION_COOLDOWN` 3600 → **0**、`EXPLORE_COOLDOWN` 1800 → **0**（配置项保留，想恢复改配置即可） |
| `battle.py` | `settle_training()` 改用 `train_start_ts` 算时长，**结算即出关**（`train_start_ts` 归零）；`last_train_ts` 降级为「上次结算时间」的纯记录字段 |
| `main.py` | `/闭关`、`/探索` 改为状态机（互斥 + 进出）；新增 `/结束闭关`、`/结束探索`；结算逻辑收口到 `_settle_seclusion()` / `_settle_explore()` 两个方法，两条入口共用一份文案；新增 `_idle_hint()` 统一挂机拦截文案；`修炼`/`历练`/`寻敌`/`秘境` 各加一道 `is_idle()` 拦截；`/属性` 状态行加「闭关中」 |
| `menu.py` | 菜单加 `/结束闭关` `/结束探索`，新增「游历」分组；`/简介` 说明挂机互斥与限制 |

### 老存档兼容

`train_start_ts` 默认 0，`from_dict()` 走白名单自动补齐 →
老存档读进来就是「不在闭关」，不会崩、也不会白送收益。
代价是**旧存档里那次没结算的闭关时长会丢**（一次性）。

### 回归断言

`selftest.py` 新增 `[51]` 段 **39 项**，重点钉住两个历史 bug：

- 「刚进入闭关就能出关（不再被冷却拦住）」
- 「出关后可立刻再闭关（无冷却）」
- 闭关/游历互斥双向、挂机期间四条玩法全被拦、状态不串
- `/属性` 显示「闭关中」
- 源码级：`settle_training` 必须读 `train_start_ts`，且**不再**出现 `(now - player.last_train_ts)`

自测 **888 → 927 通过，0 失败**（连跑 4 次稳定）；`verify_v35_token` 57、
`verify_v34_special` 16 全绿。

`verify_real_engine.py` **37 → 52 通过**，新增三类**真实注册表**层面的检查
（stub 自测够不到的那一层）：

- 四条新指令 + 全部别名在 `star_handlers_registry` 里确实在册
- 没有 handler 挂多个 `CommandFilter`（AND 语义会让指令全死）
- 指令**路由实测**：`/结束闭关`、`/出关` 只命中 `end_seclusion`，
  `/闭关` 只命中 `seclusion`，互不吞并

实测结果（`main` 模块 39 个 handler）：

```
seclusion        闭关
end_seclusion    结束闭关/出关
explore          探索
end_explore      结束探索/收工/结束游历
```

---

## 新增 — AI 陪聊（LLM 只读快照）（2026-09-21）

> 玩家 @ 机器人闲聊时，AI 能准确说出他的角色状态，
> 但**只能读，改不了任何游戏数据**。

### 做了什么

`main.py` 新增 `@filter.on_llm_request()` + `_llm_snapshot()`：
在 LLM 请求前把角色状态注入系统提示词。

注入内容：角色名 / 境界（如「金丹期 3 转」）/ 气血 / 修为 / 灵石 /
天道令 / 装备加成（攻击·防御·气血·特殊·穿透）。

### 「不影响游戏数据」是怎么保证的（三层）

| # | 保障 | 怎么证明 |
|---|---|---|
| 1 | 只调 `store.get()`（纯读），不写存档 | 测试里做**存档字节级比对** |
| 2 | 插件没注册任何 `@filter.llm_tool` | AST 断言查装饰器 |
| 3 | `computer_use_runtime = "none"` | 框架只在 `sandbox`/`local` 才注入文件与 shell 工具 |

第 3 条是外部配置，**不是插件能控制的**，所以写在这里备查：
`astr_main_agent.py:1776` 里只有 `computer_use_runtime` 为
`sandbox` 或 `local` 时才调 `_apply_*_tools()`。改成 `local` 会给 LLM
`LocalExecuteShellTool` + `FileWriteTool`，**能直接改存档**。

### 注意

`on_llm_request` **必须返回 `None`** —— 返回 `True` 会让框架
（`agent_sub_stages/internal.py:261` 的 `if await call_event_hook(...): return`）
直接中断这次 LLM 请求。

### 回归断言

`selftest.py` 新增 `[1d]` 段 **15 项**：注入内容齐全、未报名不注入、
按群隔离、原有提示词不被覆盖、**存档字节级不变**、AST 确认无 `llm_tool`。

自测 **873 → 888 通过，0 失败**。

### 配套的部署配置（不在插件代码里）

- LLM 提供商：硅基流动，对话 `deepseek-ai/DeepSeek-V3`
- RAG 知识库：「修仙世界设定」（33 chunks），向量模型 `BAAI/bge-m3`
- 触发方式：群里 @机器人 或 `/` 前缀；私聊直接
  （不带 `/` 也不 @ 的群消息会被框架丢弃，**不走 LLM**）

---

## 修复 — 捏脸第 2 步「图片读取失败」（2026-09-21 线上实测事故）

> **症状**：捏脸走到「发一张图片作为头像」，玩家每次发图都回
> 「图片读取失败了，麻烦重新发一次。」—— **每次必失败**。

### 根因（两条独立缺陷叠加）

**① 把本地路径当 URL 去下载**

日志实锤：

```
[textrpg] 下载头像失败: d:%5Cai%5CAstrBot%5Cdata%5Ctemp%5Cmedia_image_20260921204738887_009b.jpg
```

`%5C` 就是 `\` —— 这是 **URL 编码过的 Windows 本地路径**，
却被 `_download_image()` 交给 aiohttp 当 `http` 地址请求，必然抛异常。

**② 读了一个根本不存在的字段**

旧代码读 `getattr(image_comp, "base64", None)`。但 AstrBot 的
`Image` 组件（`core/message/components.py:501`）**没有 `base64` 字段**：

```python
class Image(BaseMessageComponent):
    type: ComponentType = ComponentType.Image
    file: str | None = ""     # base64 数据是以 "base64://" 前缀塞在这里的
    _type: str | None = ""
    url: str | None = ""
    path: str | None = ""

    @staticmethod
    def fromBase64(base64: str, **_):
        return Image(f"base64://{base64}", **_)   # ← 注意塞在 file 里
```

所以这条路永远是 `None`，直接掉到兜底分支。

### 修复

| # | 做法 |
|---|---|
| 1 | 新增 `_read_image_source()`，一次覆盖四种真实形态：`base64://` 内联 / `file://` URI / URL 编码的本地路径（`unquote` 后按盘符或 UNC 判断）/ `http(s)://` |
| 2 | `_save_avatar()` 改成按 `path -> file -> url` **依次尝试**，任一成功即停 |
| 3 | 新增 `character.sniff_image_ext()`：`file` 是 `base64://` 时 `basename` 是一长串 base64、没有扩展名，会误报「图片格式不支持」；改为**用魔术字节兜底**认 PNG/JPG/GIF/WEBP |

### 回归断言（`selftest.py` 新增 `[1c]` 段，21 项）

覆盖：四种地址形态各自能读、路径不存在返回 `None` 不抛异常、
`_save_avatar` 端到端吃下 URL 编码路径 / `file=base64://`、
非白名单格式（BMP）被拒、`sniff_image_ext` 四种格式 + 乱码、
以及**源码级断言「不再 getattr 读 Image.base64」**。

自测：**852 → 873 通过，0 失败**。

---

## 修复 — `/报名` 被自己吞掉当名字（2026-09-21 线上实测事故）

> **症状**：发 `/报名`，机器人先问「请发送你的角色名」，紧接着又回
> 「✅ 角色名「报名」」—— **同一条消息被处理了两次**。

### 根因

`register`（`@filter.command("报名")`）和 `on_creation_message`
（`@filter.event_message_type(ALL)`，收捏脸后续消息的全局拦截器）
**filter 同时通过**，两个 handler 都进了 `activated_handlers`，
而 `process_stage/method/star_request.py:36` 是**顺序执行**的：

1. `register` 先跑 → 建捏脸会话 → 回第 1 步
2. `on_creation_message` 接着跑 → 会话已存在 → 把**同一条消息**当名字提交

而 `/` 早被 `wake_prefix` 剥掉（`waking_check/stage.py:130`），
`message_str` 正好剩「报名」两个字 —— 一个合法的 2 字名字，校验直接通过。

### 修复（双保险）

| # | 做法 | 依据 |
|---|---|---|
| 1 | `register` 末尾 `event.stop_event()` | `star_request.py:37` 有 `if event.is_stopped(): break` |
| 2 | 会话记 `origin_msg_id`，拦截器跳过那条消息本身 | 插件自己的逻辑，不依赖框架行为 |

顺带修了同一处的另一个坑：**捏脸途中发 `/菜单` 会被当成角色名**。
拦截器现在会放行「以 `/` 开头的指令」—— 判断时读消息段的**原始**文本
（`event.get_message_str()` 里的 `/` 已被框架剥掉，用它分不出
`/菜单` 和 `菜单`），`/取消` 例外（它是流程自己的退出词）。

### 新增 helper

- `_msg_id(event)` —— 取本条消息 id
- `_raw_text(event)` —— 取消息段里的原始纯文本（未经 wake_prefix 剥离）

### 防回归

`selftest.py` 新增 `[1b]` 段 12 项断言，**复刻 `process_stage` 的 handler
循环**（先看 `is_stopped` 再执行）—— 不这么做就测不出 `stop_event()` 的效果。
同时增强了事件 stub：`stop_event()` 真的记状态，并补上
`is_stopped()` / `message_obj` / `get_message_str()`。

自测规模 **840 → 852 项**。

---

## 修复 — 所有多别名指令全部失效（2026-09-20 线上实测事故）

> **症状**：群里发 `/菜单` 毫无反应，机器人回一句
> 「LLM 请求失败：未找到任何可用的对话模型（提供商）」。
> 极易误判成「模型没配」，实际是指令**压根没匹配上**，消息掉进了 LLM 流程。
>
> **一句话根因**：多个 `@filter.command` 叠着写，在 AstrBot 里是 **AND** 语义。

### 出问题的写法

插件里 12 个方法都这样写别名：

```python
@filter.command("菜单")
@filter.command("帮助")
@filter.command("指令")
@filter.command("help")
async def cmd_menu(self, event): ...
```

### 为什么是 AND（源码依据）

1. `core/star/register/star_handler.py:49-52` 的 `get_handler_or_create()`
   用 `f"{handler.__module__}_{handler.__name__}"` 当 key，
   **同一函数返回同一个 `StarHandlerMetadata`**；
2. 同文件 `:121-123`，每次装饰都执行
   `handler_md.event_filters.append(new_command)` ——
   于是同一个 handler 身上挂了 **4 个独立 `CommandFilter`**；
3. `core/pipeline/waking_check/stage.py:192-200` 遍历 `event_filters` 是
   **AND 语义**：任一 `filter.filter()` 返回 `False` 就 `passed = False; break`。

→ 要求一条消息**同时等于**「菜单」「帮助」「指令」「help」，永远不成立。

**所以这 12 个方法、24 个指令名，一个都触发不了。**

### 受影响指令

| 方法 | 主名 | 别名 |
|---|---|---|
| `cmd_menu` | 菜单 | 帮助 / 指令 / help |
| `cmd_intro` | 简介 | 玩法 / 介绍 |
| `profile` | 属性 | 面板 / 属性面板 |
| `inventory` | 背包 | 储物袋 |
| `hunt` | 历练 | 打怪 |
| `dungeon` | 秘境 | 闯关 |
| `challenge` | 寻敌 | 挑战 |
| `revive` | 恢复 | 复活 |
| `equip` | 装备 | 祭炼 |
| `cmd_sign_in` | 签到 | 打卡 |
| `cmd_vip` | vip | 赞助 |
| `shop` | 坊市 | 商店 |

单名指令（`/报名`、`/改名` 等）不受影响 —— 所以自测全绿，问题只在线下才暴露。

### 修复

改用 `register_command` 原生支持的 `alias` 参数，一个装饰器搞定：

```python
@filter.command("菜单", alias={"帮助", "指令", "help"})
```

`get_complete_command_names()` 会返回全部四个名字，匹配是 OR 语义。

### 防回归

`selftest.py` 新增 `[50c]` 段，13 项断言：

- AST 静态扫描 `main.py`，**任何方法叠了 >1 个 `@filter.command` 立刻红**；
- 逐个核对 12 个方法的主名与别名集合，漏一个就报错。

自测规模 **827 → 840 项**。

---

## v3.5.0 — 天道令兑换帝兵 + VIP 赞助者系统

> 老板拍板三件事：**废弃天道抽奖**改「攒 100 枚天道令换帝兵」；
> **帝兵去唯一化**（全民可得）；**套系名**不叫「剑修/法修/体修」
> （那是职业名，留给未来的角色创建）—— 另起一组修仙味名字。
>
> 本版还修掉一个**静默 bug**：坊市退化成「135 件全都能买」，
> 九品装备可以直接花钱买到，把「七品以上靠打」的设计整个架空。

### 新增：天道令（凭证道具）

- **道具定义**：`items.CONSUMABLES` 加一条
  `{"name": "天道令", "effect": "token", "value": 1, "price": 0}`。
- **`effect: "token"` 是刻意的**，带来两条硬约束（各 2 行代码）：

  | 约束 | 拦在哪 | 为什么 |
  |---|---|---|
  | 不能被 `/使用` 服用 | `main.use_item()` | 不拦的话玩家会以为它是丹药 |
  | 不能被 `/出售` 换灵石 | `main.sell()` | 100 枚 = 一件帝兵，误卖一枚就是白攒一天 |

  `price: 0` 是双保险 —— 万一拦截漏了，卖掉也是 0 灵石，玩家会立刻发现不对。

- **两个产出口**（不新增第三个）：

  | 来源 | 数量 | 判定 |
  |---|---|---|
  | 每日 `/签到` | +1 枚 | 存档字段 `sign_in_date` 日期字符串，**跨零点重置** |
  | 秘境**首通** | +1 枚 | 复用现成的 `first_clears > 0` 判定 |

- **首通按「整趟」发，不是按「每层」发。** 这是硬口径：
  每首通一层给 1 枚 → 玩家从第 5 层开打、连破 20 层，一次拿 20 枚，
  5 天凑齐帝兵，节奏全崩。按趟发则一天最多 1 枚。
  这个判定**天然防刷**：打不过去的人拿不到，反复重刷老层也拿不到。

- **背包满也要入包**（`special.grant_token`）：天道令是进度型道具，
  因「背包满」吞掉一次产出，玩家会白等一天而且**根本不知道漏了**。
  所以入包失败时强制写入（最多突破 1 格上限，卖掉点东西就恢复）。

### 新增：`/签到`（别名 `/打卡`）与 `/兑换 <id>`

- **`/签到`**：每天 1 次，得天道令 ×1。用**日期字符串**而非时间戳 ——
  玩家要的是「每天一次」，不是「距上次满 24 小时」；23:59 签完，
  00:01 就该能再签一次，用时间戳玩家会觉得被坑。
- **`/兑换 <帝兵id>`**：以 100 枚天道令兑换。
  **扣款顺序：先校验 → 再扣令 → 最后写归属**，
  保证任何一步失败都不会出现「令扣了但装备没到手」。
- **`/特殊 列表`** 从「天道兵库」改成「天道兑换」进度面板：

  ```
  ⚜️ 天道兑换
  天道令：73 / 100
  　[██████████████······]

  　帝兵（dibing_000）　全属性 +20.0%
  　　弑杀成长：每击杀 +0.1%，封顶 +30.0%
  　　→ 还差 27 枚天道令
  ```

### 变更：废弃天道抽奖，改为确定性兑换

- **删掉** `special.lottery_pool()` / `roll_special()`（连 `import random`）。
- **新增** 7 个函数：`exchange_pool()` / `token_count()` / `grant_token()` /
  `owns_special()` / `can_redeem()` / `redeem()` / `redeem_preview()`。
- **为什么废弃抽奖**（三条，写给以后的我）：

  | # | 理由 |
  |---|---|
  | 1 | **唯一物 + 概率制 = 结构死局**。概率低了几个月没人抽到，活动形同虚设；概率高了第一个人抽走后池子空掉，**后面所有人抽了个寂寞** |
  | 2 | **确定性比概率更有粘性**。「今天 73/100」能看着涨；抽奖只能靠运气安慰自己 |
  | 3 | **全民化之后抽奖失去意义**。帝兵不再唯一，「抽到」不再稀缺 |

- **`/特殊 认主` 加了一道校验**：必须**已兑换过**才能认主。
  判据用 `player.special_owned`（永久流水）而不是 `player.special` ——
  后者会被 `/特殊 卸下` 清空，用它会导致「卸下后就再也装不回去」，
  或者更糟：**卸下后再兑换被白扣 100 枚**。
  原来的 `unique + owner` 校验已删除（去唯一化后是死代码）。

### 变更：帝兵去唯一化 + **数值连带下调**（重要）

老板要求「帝兵不搞唯一了，全民可获取」。这句话本身很清楚，
但它**必然要求改数值** —— 因为原数值是**为「唯一」设计的**：

| 项 | 原值 | 现值 | 理由 |
|---|---|---|---|
| `base_pct` | 62.0 | **20.0** | 独立乘区 +20%，量级合理 |
| 成长 `pct` | 1.0 | **0.1** | 每击杀 +0.1%，约 300 只怪满 |
| 成长 `cap_pct` | 150.0 | **30.0** | 封顶 +30% |
| **满成长合计** | **+212%（×3.12）** | **+50%（×1.50）** | — |
| `unique` | `True` | **`False`** | 不再全服唯一 |
| `source` | `"lottery"` | **`"token_exchange"`** | 来源改为兑换 |
| `owner` | `""` | `""`（**停用**） | 字段保留，展示层不再读 |

**冲突在哪**：帝兵是**独立乘区**，叠乘在 5 件普通装备之上。

```
最终面板 = 裸装基础 × (1 + 装备%) × (1 + 帝兵%)
```

「全服只有 1 件」时 +212% 没问题 —— 一个人独享，那正是唯一物的价值来源。
**一旦人人可得，+212% 就意味着所有玩家面板 ×3.12**，
135 件装备、三套流派、九个品阶的梯度**全部失去意义**。

> 这是设计上的硬冲突，不是「调不调」的选择题。
> **去唯一化和原数值，二者只能留一个。**

**新值换算成直觉**：`帝兵满成长 ×1.50 ÷ 一整套九品 ×1.22 ≈ 1.23`
→ 一件帝兵 ≈ **1.2 套九品装备**的量级。
作为「100 天签到」的长期目标，明显强于任何单件/单套，
但不至于让装备体系失去意义。

### 修复：坊市 `shop_stock()` 静默 bug（九品可直接买）

原实现：

```python
cap = t * 2 + 6
if int(eq.get("atk", 0)) <= cap:      # ← 恒为真
```

v3.4 把装备的 `atk`（绝对值）改成 `atk_pct`（百分比）之后，
**`eq.get("atk", 0)` 恒为 0**，`0 <= cap` 恒真 ——
坊市退化成「**135 件全都能买**」，连九品也不例外。

**所以「九品也能直接买」不是设计，是个没人发现的 bug。**
（老板直觉觉得不对，直觉是对的。）

修法：`SHOP_MAX_TIER_RANK = 6` + 两条过滤（品阶 + `req_turns`）。
条件二用 `req_turns` 而不是攻击值 —— 它直接对齐「玩家能穿得上」这个语义，
不会出现「买回来发现装不了」。

### 新增：秘境里程碑（七/八/九品的主出口）

七品以上下架坊市之后，必须有个「打得出来」的出口，否则顶级装备会断供。

| 首通层 | 给 |
|---|---|
| 第 30 层 | 七品装备 ×1 |
| 第 35 层 | 八品装备 ×1 |
| 第 40 层 | 九品装备 ×1 |

- **按品阶指定**，不是全池随机 —— 里程碑的意义就是
  「爬到这一层，确定拿到一件顶级装备」。走全池随机的话，
  玩家爬到 40 层拿到一品剑，会骂人的。
- **部位随机、套系随机** —— 让玩家有「再来一件凑套」的动力，
  而不是一次毕业。
- **只在首通给**，重刷拿不到，避免「反复刷同一层刷满套装」。

### 变更：套系展示名（破锋 / 通玄 / 镇岳）

**老板否掉了设计师的「剑修/法修/体修」方案**，理由很准：

> 「套系我觉得不应该用这个名字，这个名字应该是职业名字，
> 让玩家创建角色时选择职业。」

所以那三个名字**留给未来的职业系统**，套系另起一组：

```
内部 id（存档用，永不改）        展示名（玩家看到）
   "物穿"          ──────►        破锋   （破敌之锋）
   "法穿"          ──────►        通玄   （通达玄妙）
   "肉盾"          ──────►        镇岳   （镇压山岳）
```

**⚠️ `set_id` 一个字都不能改。** 存档里装备是按 `set_id` 匹配套系的
（`Player.active_set()`），改了 = 线上所有玩家的套装当场失效。

落地方式：`items.SET_DISPLAY_NAMES` + `set_display_name()`，
两个展示出口（`battle.BattleResult.mech_summary()`、
`main._equip_bonus_text()`）改走映射。
`battle.py` 里用**延迟导入** —— `items` 反过来要读 `battle` 的常量，
顶层 import 会成环。

### 新增：VIP（赞助者）系统（`vip.py`）

**两条硬约束（老板定的）**：

1. **VIP 只能由主人（天道）发放。** 没有自助购买、没有卡密、
   没有消费自动升级。发放权只在老板手上。
2. **VIP 不碰战斗数值。** 只加速「攒天道令 → 换帝兵」：
   **给时间，不给战力。**

| 等级 | 定价 | 权益 | 凑齐 100 枚 |
|---|---|---|---|
| 普通 VIP | 10 元/月 | 每日签到额外 **+1** 枚 | 33 ~ 50 天 |
| 至尊 VIP | 30 元/月 | 每日签到额外 **+3** 枚 | 20 ~ 25 天 |
| （免费玩家） | — | — | 50 ~ 100 天 |

- **为什么 VIP 数据必须单独存 `vip.json`**：游戏存档是**按群隔离**的
  （键是 `群号:QQ号`）。如果 VIP 写进群存档，会出现荒谬结果 ——
  **同一个玩家在 A 群充了 VIP，去 B 群玩就没权益了。**
  所以按 **QQ 号**索引，与群无关，**跨群有效**。
- **加成跟着签到发，不是每天自动发**：签到本来就有「一天一次」的
  保护（`sign_in_date`），VIP 加成挂进来就**自动继承**这层防刷 ——
  一行防刷代码都不用新写。而「每天首次任意指令时发」要在 30+ 个
  指令入口都判断一次，改动面大，还容易被玩家用 `/属性` 刷。
  代价是「当天不签到，当天加成拿不到」—— 可接受，因为天道令
  本来就只有「签到 + 秘境首通」两个来源，VIP 的定位是加速签到。
- **续费不能简单用 `now + months`**：
  `base_ts = max(now, expire_ts)` —— 否则提前续费会**吃掉剩余天数**，
  这是最容易引起投诉的低级 bug。
- **过期不删记录**：续费时能延续 `since_ts` / `total_months`，
  老板也能查历史收入。
- **`/vip` 指令**（4 个子命令）：

  | 指令 | 权限 | 作用 |
  |---|---|---|
  | `/vip` `/vip 查询 <QQ>` | 所有人 | 查状态（含已过期的历史，提示续费） |
  | `/vip 开通 <QQ> <月数> [normal\|supreme] [备注]` | 仅天道 | 发放 / 续费 |
  | `/vip 撤销 <QQ>` | 仅天道 | 立即失效 |
  | `/vip 列表` | 仅天道 | 列有效 VIP + 剩余天数 |

- **发放主入口改成两条「精确短指令」**（老板要求「一步到位」）：

  | 指令 | 权限 | 作用 |
  |---|---|---|
  | `/激活vip <QQ>` | 仅天道 | 发普通 VIP（10 元/月，默认 1 个月） |
  | `/至尊vip <QQ>` | 仅天道 | 发至尊 VIP（30 元/月，默认 1 个月） |

  不用敲一长串参数，一条指令自带全部语义。
  `/vip 开通 <QQ> <月数> ...` 降级为「多月份 / 要写备注」时才用。

- **`/vip` 无参数改为「价目 + 是否激活」**（老板要求一眼看到）：

  ```
  ⚜️ VIP（赞助者）· 价目

  　赞助者　10 元/月　每日签到额外 +1 枚天道令
  　至尊赞助者　30 元/月　每日签到额外 +3 枚天道令

  你的状态：✅ 已激活 · 赞助者　剩余 30 天（至 2026-10-19）

  VIP 只能由主人（天道）发放，无自助购买。
  （VIP 只加速攒天道令，不影响战斗数值）
  ```

  状态三态：`✅ 已激活` / `⌛ 已到期`（提示续费，附累计月数）/ `❌ 未激活`。
  对非 VIP 也打价目，但**不回显他人 QQ**。

- **三条发放入口共用一条流程 `_vip_grant_flow()`**：
  `/激活vip`、`/至尊vip`、`/vip 开通` 做的是同一件事
  （校验权限 → 发放 → 公告），抽成一条流程后**权限判断只有一处** ——
  不会出现「新开了个入口却忘了校验权限」这种白送 VIP 的漏洞。
  单测里有结构性断言钉住这条：三条入口都必须委托给它。

- **`/激活vip` 不会被已有的 `/激活` 吞掉 —— 查过源码**：
  `astrbot/core/star/filter/command.py:202` 的匹配条件是
  `message_str.startswith(f"{full_cmd} ") or message_str == full_cmd`，
  **命令名后必须跟空格，或整串完全相等**。
  `/激活vip 123` 既不等于 `激活`，也不以 `激活 ` 开头，不会误触。
  这条必须查源码确认：若框架用裸 `startswith(cmd)`，
  QQ 号会被当成 `/激活` 的参数，**静默发不出去**。
  selftest 里复刻了这条规则做回归保护。

- **斜杠可以省，但那是 AstrBot 的全局机制**：
  插件侧注册的名字**本来就不含斜杠** —— `wake_prefix`（默认 `["/"]`）
  在匹配前就被剥掉了（`core/pipeline/waking_check/stage.py:130`）。
  所以群里发 `/激活vip 123`，或者 `@机器人 激活vip 123`；
  私聊 / 被 @ 时 `激活vip 123` 也可以。
  但**群里不 @ 直接发「激活vip 123」不会触发** ——
  AstrBot 要先确认这条消息是在跟机器人说话，这是平台层规则，插件改不了。

- **权限用方法内显式判断，不是 `filter.permission_type(ADMIN)`**：
  「查询」要对所有人开放（玩家要能看自己还剩几天），
  而「开通/撤销/列表」只给天道 —— 一个方法只能挂一种框架权限。
  副作用是好的：显式判断**可测** —— 框架层拦截在单测里是桩，
  拦不住任何东西，而「非管理员不能发放 VIP」是**安全属性**，
  必须能被断言钉住。
- **发放包装成世界观动作「天道赐福」**，而不是「已开通 VIP」：

  ```
  ⚜️ 天道垂青
  　道友「xxx」以诚心供奉，自今日起受天道眷顾。
  　尊号：至尊赞助者（30 元/月）
  　每日签到额外得天道令 3 枚。
  　有效期至 2026-10-19。
  ```

  付费玩家买到的社交价值翻倍（全群都看见），免费玩家看到的是
  「有人受天道眷顾」而不是「有人充钱了」——
  前者是世界观，后者是商业化，观感差别很大。
- **`/属性` 面板挂称号**（「赞助者」/「至尊赞助者」）：
  付钱买的不只是 1 枚天道令，还有「我在支持这个群」的身份感。
- **到期前 3 天在签到回复里提醒续费**。

### 新增：`/属性` 面板挂 VIP 称号、`/菜单` 加 `/签到` `/兑换` `/vip`

### 测试

- **`selftest.py` 827 项断言全绿**（v3.4 是 697 项，新增 130 项）。
- **新增 `verify_v35_token.py`（57 项）**：走**真实指令入口**的端到端链路 ——
  签到 → 攒令 → 兑换 → 认主 → 拦截 → 里程碑 → 重刷不发令。
  与 selftest 的分工：selftest 钉**不变式**（跑得快、粒度细），
  verify 脚本钉**链路**（慢、但覆盖跨模块协作）。
- **连跑 3 次 selftest + 2 次 verify 全绿**（VIP 名单是全局文件，
  测试段开头会 `clear_all()`，避免上一轮数据污染）。
- **线上目录同样 793 + 57 全绿**，31 个文件逐字节一致。

### 老存档兼容

- 新增字段全部有默认值 + `from_dict` 兜底，老玩家不会崩。
- `special_owned` 的兜底**不能只做「无则置空」** —— 要兼容三种脏数据：
  缺失 / 存成了字符串 / 列表里混进非字符串。用 `dict.fromkeys` 去重保序。
- `vip.json` 不存在或损坏时**降级为空名单**，不影响插件启动。
- **帝兵数值变更对已有玩家生效**：按当前状态线上应该没人拿到帝兵
  （抽奖从未开放），所以无实际影响。

---

## v3.4.0 — 十品装备体系 + 指令菜单 + 数值实验室

> 老板定的方向：**装备独立分十级**（一品~九品 + 极品帝兵），
> 不走「按境界分层」；装备加成改**百分比**；中上品引入
> **无视防御 / 反伤 / 免伤** 等高级乘区。
>
> 本版同时修掉三个会**静默吃掉玩家收益**的真 bug，
> 并把数值方法论固化成可复跑的实验室。

### 新增：十品装备体系

- **135 件装备，9 个品阶 × 3 套系 × 5 部位**。品阶倍率骨架：
  下品（一~三品）每级 ×2、中品（四~六品）每级 ×2.5、上品（七~九品）每级 ×3。
- **品阶门槛**（`req_turns`）：一品 1 转起，逐品递增，九品 68 转。
- **三套系分工**（不再「一味乘算」）：

  | 套系 | 定位 | 机制 |
  |---|---|---|
  | 物穿 | 破防输出 | 穿甲 + 概率无视防御（追加伤害）+ 暴击 |
  | 法穿 | 术法输出 | 穿甲 ×1.15 + 术法增伤 + 冷却缩减 |
  | 肉盾 | 生存 | 减伤 + 反伤 + 低血线额外减免（上品） |

- **帝兵（特殊装备）**：不占 5 个普通部位，独立【特殊】栏；
  **无阶位限制**；带成长被动（每击杀 +1% 全属性，可累积）；
  数据表用 `SPECIAL_EQUIPMENTS` 列表 + `passives` 字段，
  **不写死数量**（老板明确要求后续会出更多）。

### 新增：帝兵系统层（`special.py`）+ `/特殊` 指令

- **`/特殊`**（无参数）：查看帝兵与成长进度（基础 % + 成长 %）。
- **`/特殊 认主 <id>`**：让帝兵认主（不叫「装备」—— 语义上和穿装备是两回事：
  无阶位限制、可成长、全服限定）。
- **`/特殊 卸下`**：卸下但**成长进度保留**（清零会让玩家不敢换装）。
- **`/特殊 列表`**：看当前可获取的帝兵。
- **`/属性` 面板**：有帝兵时追加【特殊】栏，没有时不显示（老板要求）。
- **战斗结算接入成长**：`_apply_death` 统一收口，历练/寻敌都走它。
- **成长被动走注册表** `PASSIVE_HANDLERS` + `@register_passive`：
  加新被动（比如「每天首次登录 +2%」）只加函数，不碰战斗代码。
  「怎么涨、涨到哪、死不死」全由数据表 `passives` 决定，
  累加值存玩家存档 —— 对应老板的「具体问题具体分析」。
- **天道抽奖框架**：`lottery_pool()` / `roll_special()`。
  只搭**结构**（池子语义、`unique` 已有主则出池），不填概率 ——
  概率是运营参数，等老板定活动规则再配。

### 修复：碾压击杀不算「弑杀」（真 bug，端到端才抓得到）

战斗结果有**五档，其中三档都算赢**：

```
crush（碾压） / win（胜利） / narrow（惨胜）  ->  都算赢
draw（平局）  / lose（败北）                  ->  不算
```

成长代码第一版写的是 `outcome == OUTCOME_WIN`，于是**碾压和惨胜全被漏掉** ——
而碾压恰恰是最常见的结果（数值体检显示全境界安全边际 4~9.6，几乎场场碾压），
实际表现是「打了一晚上，帝兵一次没涨」。

修法：改用 `result.won`。

> **为什么单元测试没抓到**：直接调 `advance_passives(kills=1)` 永远是对的，
> bug 在「什么算击杀」这一层。为此专门写了 `verify_v34_special.py`
> 走**真实指令流程**（真插件对象 + 真 `/历练`），它一次就把问题顶出来了。
> 这也印证了「夹具与真实路径不一致，实验室会替线上漂白」。

### 修复：装备门槛校验重复实现（`main.py`）

`/装备` 第一版手写 `turns >= req_turns` 做门槛校验，但**帝兵是无阶位限制的例外**
（它根本没有 `req_turns` 字段），手写比较会把帝兵一起拦掉。
改用 `items.can_equip`（内部已处理例外），避免两处各写一遍迟早分叉。

### 数值实验室补强（`_balance/lab/`）

- **★ 夹具改用真实口径**：`Slot.quality` 默认 `"common"`，造玩家时给装备名加
  品质后缀（`青锋剑[普]`）—— **与游戏内真实状态一致**。
  之前用裸名造玩家，导致「品质后缀让加成归零」这个 bug 在实验室里**完全不可见**
  （报告还一路绿灯，与线上相反）。这是本次最值得记的教训：
  **实验室与被测系统的夹具口径必须一致，否则实验室会替线上漂白。**
- **`env_guard()` 新增第 7 项**：钉住「裸名 vs 带后缀名必须算出同一份加成」。
  将来谁把 `equip_pct` 改回裸查，跑数值之前就会炸出来。
- **`analyze.quality_consistency()`**：品质口径一致性 + 品质倍率单调性检查，
  已接进 `report.py` 成为常驻第 2 节。

### 自测

- 断言数 **605 → 697**，连跑 8 次全绿。
- 新增覆盖：菜单/简介指令、十品结构约束、穿甲/减伤表单调性、
  三套系机制、品阶门槛正反用例、帝兵无门槛、机制注册表非空、
  **名字解析（基础名命中带品质后缀 / 多品质优先序 / 精确匹配）**、
  **`equip_pct` 品质倍率口径**、**帝兵系统层（成长累积/封顶/不掉/无帝兵 no-op）**、
  **`/特殊` 指令端到端**、**三档赢法都算击杀**。
- 重写了过时的旧装备测试段（原测「每件穿甲 1%」「穿甲上限 5%」
  基于已删除的 42 件旧装备）。
- 新增独立端到端脚本 `verify_v34_special.py`（15 项断言，走真实指令流程）。

### 已知未完成

- 天道抽奖的**概率与消耗**（框架已就位，等运营规则）。
- 待老板定：① 套系命名 ② 十品装备获得方式 ③ 帝兵唯一性细节。

### 新增：装备机制注册表

`battle.py` 引入 `ATTACK_MODIFIERS` / `DEFENSE_MODIFIERS` +
`@register_attack_modifier` / `@register_defense_modifier` 装饰器。

- 主循环**只查表遍历，不写 if-else 链** —— 将来加机制只加一个函数。
- 修正器返回协议：`dmg_mult`（乘区）/ `dmg_flat`（追加）/ `tag`（日志）/ `count`（计数）。

### 新增：指令菜单与玩法简介

- `/菜单`（别名 `/帮助` `/指令` `/help`）：6 组分类罗列全部指令，
  顶部按玩家当前状态给**唯一一条下一步建议**（修为圆满→`/突破`、
  血少→`/恢复`、装备没穿满→`/历练`……）。
- `/简介`（别名 `/玩法` `/介绍`）：7 个板块讲清「怎么变强 / 修为从哪来 /
  装备十品三套 / 五行秘典 / 秘境 / 战斗结果怎么读」。

### 修复（三个真 bug，都会静默吃掉玩家收益）

- **★ 带品质后缀的装备，属性加成全部归零**（`player.py`）——
  **本版最严重的一个**。装备名有两套写法：掉落/坊市产出的是
  `青锋剑[稀]`，而 `item_db` 的 key 是基础名 `青锋剑`。
  `equip_pct` / `equip_bonus` 直接 `item_db.get(名字)`，于是：

  ```
  item_db.get("青锋剑[稀]")  ->  None  ->  加成静默归零
  ```

  更隐蔽的是**数值口径分裂**：即便查到基础名，拿到的也是未乘品质倍率的
  裸值，而展示/掉落走 `items.get_item`（会乘倍率）。结果是
  **同一件装备，面板显示一个数、实际生效另一个数**。

  修法：新增 `Player._lookup_equip()`，把「解析名字 + 乘品质倍率」
  统一交给 `items.get_item` 单一出口，`equip_pct` / `equip_bonus` 都走它。
  实测修复后：`青锋剑[普/精/稀/史/传]` → 加成依次 ×1.0/1.3/1.7/2.2/3.0。

- **★ 玩家输入基础名时「明明有却被告知没有」**（`player.py` + `main.py`）——
  储物袋里存的是 `小回气丹[普]`，玩家输入的是 `小回气丹`，
  指令里直接 `inventory.get(item_name)` 查不到。
  `/使用` `/出售` `/装备` 三个指令全中招，不会崩，只会让玩家觉得游戏坏了。

  修法：新增 `Player.find_inventory_name(name, *, prefer_quality=True)` 统一解析。

  | 指令 | 品质倾向 | 理由 |
  |---|---|---|
  | `/装备` `/使用` | 品质**高**者优先 | 玩家说「装上剑」显然想用好的那件 |
  | `/出售` | 品质**低**者优先 | 卖东西时意图相反：先出手不值钱的 |

  实现上两个分支都是**全量扫描取极值**，不是「遇到第一个就返回」——
  后者等于依赖 dict 迭代顺序，行为不可复现（调试时真踩到过）。

- **★ 品阶门槛定义了但没有任何地方校验**（`main.py`）——
  `req_turns` 数据一直在，却没有闸门，**1 转新手可以直接穿九品毕业装**，
  十品体系辛苦搭的成长曲线被一次性跳过。已在 `/装备` 补上校验，
  并在被拦时给出「还需几转」的具体数字。

- **存档迁移的品质后缀格式**（`player.py`）——
  `split_quality` 返回的是内部 key（`common`）而非中文标记（`普`），
  迁移代码直接拼 `f"[{qual}]"` 导致老存档变成 `小回气丹[common]`
  （**比不迁移更糟**：这种名字连 `split_quality` 都认不出，物品立即失效）。
  且不幂等：二次迁移变 `[common][common]`。
  修法：新增 `Player._rebuild_item_name()` 统一走 `items.decorate()`。

- **怪物掉落表残留旧装备名**（`items.py`，9 处）——
  `生命晶石` / `暗影匕首` 在 v3.4 已重做，掉落表还写着旧名。
  `get_item()` 靠别名映射能查到，但 `all_item_names()` 不能 →
  两个口径不一致。选择**改数据本身**（治本），而不是让查询接口兼容旧名。

### 新增：数值实验室（`_balance/lab/`）

老板的要求：**「数值计算你需要做好点，不然以后可能会崩，你可以参考别的成熟游戏的数值计算」**。
本版把这套方法论固化成可复跑的实验室，而不是「跑一遍看看」。

| 模块 | 作用 |
|---|---|
| `lab/__init__.py` | 环境自检 `env_guard()` + 统一夹具 `make_player()` |
| `lab/analyze.py` | 收益归因（面板/机制/总）+ 强度对齐 + TTK/TTLK 战斗模型 |
| `lab/report.py` | 一条命令出完整体检报告 |
| `lab/baseline.py` | 黄金基线 `save` / `check`，改动超阈值即告警 |

**收益归因三层拆解**（相对同转数裸装的百分点）：

```
面板增益 = 纯面板 / 裸装 - 1        （只看装备给的属性）
机制增益 = 完整   / 纯面板 - 1      （只看破防/暴击/反伤等机制）
总增益   = 完整   / 裸装   - 1      （玩家实际拿到的）
```

**强度对齐口径**：只看「输出」会误判肉盾套（它本来就不靠输出），
所以用**合成增益 = 输出 + 生存**做对齐判定，容差 4pp。

**安全边际**：`可扛回合 / 杀敌回合`，≥2.0 碾压 / ≥1.4 稳 / ≥1.1 险 / <1.1 必败。

### 实验室立刻量化出的真问题（待设计师定夺，未修）

归因跑出来后，之前靠手感察觉的「三套不均衡」被**精确定量**：

| 品阶 | 物穿 | 法穿 | 肉盾 | 合成极差 |
|---|---|---|---|---|
| 四品 | +13.5% | +21.6% | +17.9% | 8.1pp ✓ |
| 七品 | +38.0% | +54.9% | +38.1% | **16.8pp ✗** |
| 九品 | +49.8% | +68.0% | +53.5% | **18.2pp ✗** |

- **法穿套超模**：术法增伤只作用于普攻的折扣系数（`MAGIC_AMP_TO_ATTACK`）
  当初为「让法穿不只是秘典专属」而加，实际让它变成了**主输出通道**。
  这个修复方向本身是错的（加了之后法穿更超模），需要重新设计。
- **肉盾套机制贡献仅 +2.2%**：反伤对高血怪几乎无意义
  （400 场触发 1216 次，对 11205 血怪贡献 0.035%）。它有 +33% 生存增益，
  所以合成口径下不算崩，但**输出通道确实缺失**。
- **战斗难度整体偏松**：全部境界安全边际 4~9.6（碾压级），玩家几乎不可能输。

> 这三项已上报老板，老板回复「我去问问设计师」——
> **本版不动**，等设计师意见再改，避免朝令夕改。

### 自测

- 断言数 **605 → 663**，连跑 8 次全绿。
- 新增覆盖：菜单/简介指令、十品结构约束、穿甲/减伤表单调性、
  三套系机制、品阶门槛正反用例、帝兵无门槛、机制注册表非空、
  **名字解析（基础名命中带品质后缀 / 多品质优先序 / 精确匹配）**、
  **`equip_pct` 品质倍率口径**。
- 重写了过时的旧装备测试段（原测「每件穿甲 1%」「穿甲上限 5%」
  基于已删除的 42 件旧装备）。

### 部署优化：小内存服务器适配

> 老板要挂 **1核1G** 云服务器（AstrBot + NapCat 同机）。
> 实测下来瓶颈是**内存**而非玩家数，据此做了优化。

- **存档改紧凑序列化**（`data.py`）：`indent=2` → `separators=(",", ":")`。
  实测 1000 人存档三种写法对比：

  | 写法 | 体积 | 序列化耗时 |
  |---|---|---|
  | `indent=2`（原） | 1377 KB | 14.5 ms |
  | `indent=None`（默认分隔符） | 1109 KB | 14.5 ms |
  | `separators=(",", ":")` | **1012 KB** | **12.6 ms** |

  体积 **-26%**、耗时 **-13%**，且临时字符串的**内存峰值同步下降**
  —— 小内存机器上这一步能直接减轻 GC 压力。

- **容量基线**（实测，脚本 `bench_capacity.py`）：

  | 玩家数 | 存档 | 完整保存一次 | 高活跃 CPU\* |
  |---|---|---|---|
  | 100 | 138 KB | 9.3 ms | 3% |
  | 500 | 689 KB | 19.6 ms | 33% |
  | 1000 | 1378 KB | 41.6 ms | 139% |

  \* 每人每分钟 2 条指令。低活跃（0.5 条/分）下 1000 人仅 35%。

- **内存基线**（实测，`bench_memory.py`）：`import astrbot.api` 一步
  吃掉 **178 MB**，而**插件本体只加 1.6 MB**。
  → 1核1G 的瓶颈是框架 + NapCat 的基础占用（约 730 MB），
  **不是玩家数**；存档常驻内存 1000 人也只有 4.7 MB。

- **已知待优化**（未做，需老板拍板）：存档目前是「单一 JSON 全量写」，
  任一玩家操作都要全量序列化 + 写盘，成本 O(玩家数)，
  且写锁串行会让突发操作排队（1000 人规模突发 100 条 → 最后一人等 4.2 秒）。
  可选方案：写盘合并（debounce，CPU 可降约 16 倍）、按群分片存档。

---

## v3.3.0 — 解开回合上限枷锁：妖王成为「高消耗战」

> 由老板的 `端到端验收报告.md` 驱动。上一版把妖兽属性改成
> 「按玩家面板反推」之后，留下了一个**设计目标与实现直接冲突**的隐患，
> 这一版把它拔掉。

### 修复

- **`MAX_BATTLE_TURNS` 10 → 30**（`player.py`）—— **这是本轮最重要的改动**。
  它其实不是「平衡调整」，是**修一个逻辑冲突**：

  | | 设计目标 | 上限 10 时的实际 |
  |---|---|---|
  | 普通怪 | 5 回合击杀 | 5~6 回合 ✓ |
  | 玩家可扛 | **19 回合** | ❌ 物理上不可能（上限只有 10） |
  | 妖王（血厚） | 磨一场硬仗 | ❌ 第 10 回合判 `draw`，玩家还剩 47~74% 血 |

  妖王被打成 `draw` 时，看起来像「打不过」，实际是**打不完** ——
  两个完全不同的体验，却给出同一种结算。提到 30 之后妖王变成
  一场干净的**高消耗战**（磨 7~9 回合），「可扛 19 回合」的目标也终于可达。

- **`DUNGEON_HP_CAP_RATIO` 3.0 → 5.0**（`dungeon.py`）——
  跟随回合上限等比抬高秘境兜底血线（30 回合输出约为 10 回合的 3 倍）。
  实测当前曲线下这条线**从未被触发**（cap 取 3.0/5.0/99.0 三档，各转数
  最高层完全一致），属「把安全垫抬到与新上限匹配」，防的是将来加内容后撞线。

### 调整

- **`DUNGEON_WARMUP_BACKOFF` 2 → 4**（`dungeon.py`）——
  **仅调整节奏感，不影响推进速度。**
  退 4 层让玩家进秘境后有「热身 → 逐层爬」的起承转合，
  而不是开门就在天花板上挨打。

  ⚠️ **实测澄清**：曾以为退更多层能提高净推进速度，**实测否定了这个猜测**。
  12 天连续闯关模拟（真实 `run_dungeon`）：

  | backoff | 10 转 | 30 转 | 46 转 | 72 转 |
  |---|---|---|---|---|
  | 2 | 18 → 18 (+0) | 30 → 30 (+0) | 37 → 37 (+0) | 43 → 44 (+1) |
  | 4 | 18 → 19 (+1) | 30 → 30 (+0) | 37 → 37 (+0) | 43 → 44 (+1) |

  中期无论取多少都是 0 —— 卡点是**曲线天花板**，不是起始层。
  真正的修法（重刷锚定产出 / 首通里程碑）请见 `秘境副本数值设计.md`。

### 新增（回归防线）

- `selftest.py` 新增 2 条断言（603 → **605** 项）：
  - 「回合上限 ≥ 20」—— 将来若有人把上限压回 10，这条会立刻拦下；
  - 「秘境 HP_CAP ≥ 5.0」—— 保证兜底线与回合上限匹配。

### 手感实测（真实引擎，300 场/采样点，跨 1~72 转 13 个采样点）

| 档位 | 平均回合 | 最高回合 | 胜率 |
|---|---|---|---|
| 普通 | 5.0 ~ 6.8 | 8 | 100% |
| 妖王 | 7.2 ~ 9.1 | 11 | 100% |

**零平局、零败北**，且回合数远离 30 上限 —— 上限是安全垫，不是瓶颈。

### 已知未做

- **装备绝对加法导致中后期失效**（大乘期满装仅 +5% 面板）。
  修法见 `装备分层方案_A2.md`，但它会与 v3.2 的妖兽缩放器
  （`MONSTER_POWER_RATIO`）互相影响，需要重标定，**属独立一轮改动**。
- **秘境深度后期停滞 / 43 层后奖励难度比跌破 1.0**。
  修法见 `秘境副本数值设计.md`，**独立一轮**。

---

## v3.2.0 — 数值平衡重做：全境界手感统一

> 本轮由老板在另一个窗口设计的数值方案驱动，实装前用**真实战斗引擎**
> 逐条验证（脚本见 `_balance/`，可复跑）。核心思路：
> **妖兽属性必须由玩家面板反推，不能独立设计。**

### 新增

- **妖兽 19 → 72 只** —— 覆盖 Lv1~72 全部转数（每境 9 只）。
  新怪按境界调性命名（元婴期「九幽鬼将」、化神期「太虚剑灵」、
  大乘期「天劫魔尊」等），掉落表按档位分 10 段。
- **境界分池抽怪** —— `spawn_monster` 从「Lv ≤ 转数+2」改为
  「本境 70% / 低一境 20% / 高一境 10%」。池子恒为 27 只左右、跨 3 境，
  **不再出现「72 转抽到 Lv1 史莱姆」**。
- **新手保护** —— 9 转前怪 Lv 不超过转数+3，避免开局抽到越境怪。

### 调整

- **妖兽属性全部重填** —— 由 `玩家面板 × 公式` 反推：
  `妖def = 面板def × 0.45`、`妖hp = (面板atk - 妖def×0.5) × 4.5`、
  `妖atk = (面板hp/19 + 面板def×0.5) ÷ 平均伤害系数`。
  效果：**8 个境界都是「5 回合击杀 / 19 回合可扛」**，手感完全一致。
- **`INIT_ATK` 10 → 18** —— 旧值下 1 转打史莱姆要 9 回合，新手第一战就是拖沓的。
- **`INIT_HP` 100 → 120** —— 给新手的容错余量。
- **`MONSTER_LEVEL_ADJUST` 0.045 → 0** —— 这个等级偏差修正是 v3.1 为
  「数据只到 Lv28」打的补丁。妖兽全 Lv 覆盖 + 分池后，它变成**双重惩罚**
  （怪本身已按同档位反推，再按等级差削一次）。
  实测：0.045 → 0 让平均回合 4.00 → 4.61、碾压率 80% → 10%。
  机制保留，将来数据覆盖不足时可再调回小值。
- **`MONSTER_LEVEL_GAP_CAP` 4 → 9** —— 分池后最大等级差就是 9（跨 3 境），
  旧封顶 4 把邻境怪全压成了「弱 18%」。
- **妖兽 exp 全境界锁定 15% 占比** —— 基准 exp 4~910（按档位加权后
  期望 = 基准 × 1.172），保证「打怪 = 0.20 次修炼」在全境界一致。
- **秘典残页产出三处放宽** —— 探索残页权重 8 → 14、
  拾取次数 1.5 → 2.0 次/小时、三阶残页成本 6 → 5 张。
  满阶耗时 **417h → 161h**。

### 修复

- **`木之残页` 不存在** —— 五行秘典是「火/水/金/土/风」，没有木。
  生成掉落表时的笔误，已全部改为 `风之残页`。

### 已知影响

- **秘境难度整体上移约 2~3 层** —— `dungeon.anchor_stats()` 锚定在
  `base_atk(1)` 上，改 `INIT_ATK` 会连带拉高秘境怪的三围。
  实测 72 转极限层 46 → 44。属可接受范围（秘境本来就有爬塔空间）。

---

## v3.1.0 — 秘境、战力与装备扩展

### 新增

- **`/秘境`（别名 `/闯关`）** —— 每日一次的爬塔副本。
  从历史最高层的倒数第 3 层开打，自动连闯直到失败，**每层满血开打**。
  失败**不计死亡、不掉转、不进休整** —— 试炼不该有惩罚。
- **战力系统** —— `战力 = 攻击×2.0 + 防御×1.5 + 气血×0.5`，
  在 `/属性` 面板展示。**只用于展示，不参与战斗计算**，
  避免变成第二个难度旋钮。裸装战力 1 转 77 → 72 转 16356（212 倍）。
- **装备部位 3 → 5** —— 新增「灵靴」（偏攻/机动）与「法印」（偏防/厚血），
  各 6 件共 12 件新装备，装备总数 30 → **42 件**。
- **穿甲** —— 每件已披挂法宝 +1% 无视目标防御，封顶 5%。
- **套装** —— 穿满 5 个部位触发 **+5% 增伤**。
  同部位塞两件骗不过判定（每个部位最多计 1 件）。

### 调整

- **妖兽数值改为按玩家转数动态缩放**（`monster_stats`）。
  修掉了「妖兽数据只到 lv28，导致 5 转后每战必胜」的问题 ——
  现在怪物的整体强度会跟着玩家档位走，低级的田鼠缩放后仍比史莱姆弱。
  刻意**不含装备加成**：否则换装就毫无意义了。
- **历练降级为「物品与灵石来源」** —— 打怪修为压到 **15%**
  （`EXP_SCALE_FIELD`）。修为主来源是 `/修炼`、`/闭关`、`/探索` 三条挂机线。
  保留 15% 而不是清零，是为了让新人开局的第一次战斗有正反馈。
- **秘境难度锚点定为 1 转裸装** —— 曲线起点落在最弱玩家脚下。
  实测可达层数：1 转 → L2，10 转 → L20，72 转 → L46。

### 修复

- **高层秘境怪血量兜底完全失效** —— `build_monster` 的兜底调用
  `player.max_hp()` 漏传 `item_db`，抛出的 `TypeError` 被
  `except Exception: pass` 静默吞掉。结果是第 999 层算出了
  6.5×10⁵⁴ 的怪血，玩家必死。
  → 改为止 `max_hp(item_db)`，并把 `item_db` 从 `run_dungeon` 一路透传下来。
- **`prescaled` 标记的怪仍吃到 `level × 15` 等级加成** ——
  副本守卫声明「完全原样返回」，但等级加成在标记检查**之前**执行。
  当前副本怪恰好 `level = 0` 而躲过，属巧合而非设计。
  → 把 `prescaled` 判断提前到 `base` 计算之前。
- **旧物品名 / 旧部位名从未被真正迁移** —— `resolve_alias` 只在
  `get_item` / `buy_price` 里被调用，存档里的名字一直没改。
  结果是 `/使用 小型生命药水` 能查到定义（靠 `get_item` 兜底），
  但 `/背包` 显示的仍是 v2 旧名，与 `/坊市` 里的新名对不上。
  → 迁移改到 `Player.from_dict` 里做，读档即改写。
  新旧名同时存在时数量**相加**而非覆盖，迁移**幂等**。

### 测试

`selftest.py` 513 → **602 项断言**，连续跑 8 次全绿无抖动。
新增 `[47]` 分节：战力公式 / 妖兽缩放三态（`None` / 正数 / `≤0`）/
`prescaled` 语义 / 秘境曲线与可达层数 / 每日限制 / 失败不计死亡 /
5 件套与穿甲 / 历练修为折扣。
`[42]` 段补 7 项存档层迁移断言（含合并与幂等）。

---

## v3.0.0 — 修仙化重做

### 新增

- **八境九转境界体系**（72 转）：炼气 → 筑基 → 金丹 → 元婴 →
  化神 → 炼虚 → 合体 → 大乘，每境 9 转，境界倍率 ×1.00 ~ ×4.00。
- **渡天劫飞升** —— 大乘九转圆满 + 累计修为 250 万 → 渡劫 → 转生。
  天劫是**抗住不是打赢**：9 道雷首道为最大气血 8%，每道递增 8%，
  累计约 100% 气血。**不满血应劫几乎必失败**，渡劫丹每颗可多扛一道。
  飞升保留灵石/储物袋/法宝/秘典/突破次数，转数归 1、修为清零，
  永久获得 ×1.05 基础属性（可叠乘）。
- **大道秘典**（首期 5 条：火/水/金/土/风，每条 3 阶）——
  从 `/探索` 掉落残页，用 `/修行` 参悟。装配位随大境界提升，
  炼气 1 个 → 大乘 8 个。**未装配的秘典被动不生效**。
- **挂机三线** —— `/闭关`（最长 8 小时，有走火入魔风险）、
  `/探索`（30 分钟~8 小时，产出物品与秘典残页）。
- **`/突破`** 指令 —— 大境界突破。

### 调整

- **属性数据流重构**：`面板值 = 基础属性 × 总乘区 + 装备加成`，
  乘区 = 境界倍率 × 1.05^转生 × 1.20^突破。
  **加任何新系统都是「插一个新乘区」，不改战斗代码。**
- **效果注册表** —— 秘典在 `scriptures.py` 只做声明，
  战斗引擎靠 `EFFECT_HANDLERS` 查表分派，不写 if-else 链。
  现有 8 种 kind：`magic_damage` / `heal` / `buff` / `resist` /
  `add_damage` / `counter` / `dodge` / `dot`。
- **题材全量置换** —— 等级 → 转数、经验 → 修为、武器 → 法宝、
  金币 → 灵石、生命 → 气血、怪物 → 妖兽。
- **`/修炼` 语义修正** —— 从挂机改为主动一次性加修为，冷却 5 分钟；
  原挂机语义由 `/闭关` 承接。

### 修复

- **飞升链路完全不通** —— `/突破` 在渡劫场景两次调用走完全相同的分支，
  永远卡在「再发一次应劫」的提示，`tribulate` / `ascend` 从未被执行。
- **天劫数值必然失败** —— 每道递增 12% 使累计伤害达 118% 最大气血，
  满血也扛不住。递增率改 8%，累计 99.9%，满血刚好飞升。
- **`from_dict` 缺 `return`** —— 导致加载存档拿到 `None`，整个存档读不出来。
- **6 只妖兽缺 `element` 字段** —— 使五行抗性类被动对它们失效。
- **`/探索` 把事件文案塞进 `items`** —— 面板会显示成「所得：参悟天象…」。

### 兼容性承诺

v2 老玩家**不丢档**：旧指令全部保留为别名
（`/打怪` `/挑战` `/复活` `/商店` `/背包` `/装备` `/卸下`），
旧物品名走 `ITEM_ALIASES` 映射，旧部位名走 `SLOT_ALIASES`，
v2 的 `level`（上限 200）自动钳到 72 转。

---

## v2.0.0 — 捏脸与面板

### 新增

- **三步捏脸报名** —— 起名 → 选形象 → 确认，任一步可取消。
- **形象头像** —— 支持自定义头像链接，落盘到插件数据目录。
- **完整属性面板** —— `/属性` 展示三围、装备、战绩。
- **背包分类** —— `/背包` 按类别分组，避免长列表刷屏。
- **群内排行榜** —— `/排行榜` 只显示本群。

### 调整

- 战斗结果细分为五种文案：碾压 / 胜利 / 险胜 / 势均力敌 / 道消。

---

## v1.3.0 — 访问控制

### 新增

- **入群邀请守卫** —— 只有主人的邀请才自动同意，陌生人拉群自动拒绝。
- **群内激活机制** —— 群默认未激活，必须由主人发送 `/激活` 才响应。
  未激活的群，插件**完全不理**（指令方法根本不执行，不会留下垃圾存档）。
- **`/取消激活`、`/激活列表`、`/重置`** —— 仅主人可用。
  `/重置` 在私聊里只清自己的私聊存档，不会波及群数据。
- **`acl.py` / `activation.py` / `gate.py`** —— 访问控制、激活持久化、
  两个闸门装饰器（`@gate_session` / `@gate_sensitive`）。

### 关键设计

- **拦截发生在指令执行前**，不在回复出口。
  早先在 `_reply()` 里拦，导致未激活群照样创建了存档 ——
  用户看不到回复，后台却留下垃圾数据。
- **激活指令不能加 `gate_session`**，否则未激活的群永远激活不了。

---

## v1.2.0 — 防封节流

### 新增

- **`guard.py`** —— 三项降低行为特征值的防护：
  回复前随机延迟 0.4~1.6 秒；超 180 字自动分段（段间再等 0.6~1.3 秒）；
  单群 60 秒内超 20 条则静默 20 秒。

### 重要说明

**插件层防不了封号。** 风险主要在协议端（NapCat 是逆向协议）和账号本身，
所谓「防封 SDK」基本是智商税。唯一靠谱的防线是**用小号 + 养号 3~7 天**。
节流只是降低机器人行为特征值，不要把它宣传成「防封」。

---

## v1.1.0 — 群隔离与商店

### 新增

- **按群隔离存档** —— 存档键从纯 QQ 号改为 `群号:QQ号`，
  同人在不同群各自独立进度。排行榜也随之按群隔离。
- **旧存档自动迁移** —— v1.0.0 的纯 QQ 号键自动识别，
  `group_id` 置空归入私聊池，不丢数据。
- **商店系统** —— `/坊市` `/购买`。买入价 = 售价 × 3，
  保证「买了再卖」净亏 2/3，防刷灵石套利。
- **一键安装脚本** —— `bash install.sh [AstrBot路径]`。

---

## v1.0.0 — 首个版本

- 指令式文字 RPG 雏形：打怪、升级、背包、装备、挂机修炼。
- 按 QQ 号独立存档。
- 离线自测 88 项断言。
