# 部署指南 · 1核1G 小内存服务器

> 目标场景：**AstrBot + NapCat 同机**，1 核 1G 云服务器。
> 本文所有数字都来自实测（脚本：`bench_capacity.py` / `bench_memory.py`），
> 不是经验估计。

---

## 一、先说结论

| 问题 | 答案 |
|---|---|
| 1核1G 能带多少玩家？ | **建议 200 人以内**，最多不超过 300 |
| 瓶颈是 CPU 吗？ | **不是**。500 人时 CPU 只占单核 33% |
| 瓶颈是什么？ | **内存**。框架 + NapCat 基础占用约 730 MB / 1024 MB |
| 能救吗？ | 能。**开 swap** 是必须的，其余靠控制规模 |

### 实测容量表

| 玩家数 | 存档大小 | 单次完整保存 | 高活跃 CPU\* | 低活跃 CPU\*\* |
|---|---|---|---|---|
| 100 | 138 KB | 9.3 ms | 3% | 1% |
| 200 | 276 KB | 12.0 ms | 8% | 2% |
| **500** | 689 KB | 19.6 ms | **33%** | 8% |
| 1000 | 1378 KB | 41.6 ms | **139%** | 35% |

\* 每人每分钟 2 条指令（很活跃的群）　\*\* 每人每分钟 0.5 条（更接近真实群）

**关键：决定压力的是「活跃玩家数 × 操作频率」，不是群总人数。**
一个 500 人的群如果只有 50 个活跃玩家，压力等同于 50 人群。

---

## 二、内存账本（1核1G）

| 组成 | 占用 | 来源 |
|---|---|---|
| 系统（Debian/Ubuntu 最小化） | ~100 MB | 估算 |
| AstrBot 运行时 | ~330 MB | 实测 import 基线 178 MB + 运行时开销 |
| NapCat（少量群） | ~300 MB | 估算，需实测确认 |
| **合计** | **~730 MB** | 余量仅 ~300 MB |

> **插件本体只占 1.6 MB**，存档常驻内存 1000 人也只有 4.7 MB。
> 内存几乎全被框架和 NapCat 吃掉，**跟你写了多少游戏逻辑无关**。

⚠️ **风险**：NapCat 社区有内存涨到 800 MB 的 issue（GitHub #556，
极端场景：400 个群 + 应用端异常调用）。你的规模下大概率不会，
但**必须有兜底**，否则 OOM 杀进程 → 反复掉线 → 可能触发腾讯风控。

---

## 三、必做：开 2G swap（零成本，最重要）

1G 物理内存下，swap 是唯一的 OOM 保险。内存一旦超了只是变慢，不会杀进程。

```bash
# 创建 2G swap 文件
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# 开机自动挂载
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

# 有 swap 但别太激进（10 = 只在真缺内存时用）
sudo sysctl vm.swappiness=10
echo 'vm.swappiness=10' | sudo tee -a /etc/sysctl.conf

# 验证
free -m
```

**如果磁盘紧张**：用 1G swap 也比没有强。最小化系统盘一般 20G+，2G 文件无压力。

---

## 四、建议：限制 NapCat 的内存增长

在 NapCat 的 `docker-compose.yml` 里给容器加环境变量：

```yaml
services:
  napcat:
    environment:
      - NODE_OPTIONS=--max-old-space-size=256
```

作用：让 Node 的 V8 堆在 256 MB 时更积极 GC，而不是一路涨上去。

**可选**：再加硬限制兜底（超了容器会被杀，但保护宿主机）：

```yaml
    mem_limit: 400m
```

> 两者取舍：`NODE_OPTIONS` 是"提前回收"，`mem_limit` 是"超了就杀"。
> 建议**先只加 NODE_OPTIONS**，观察几天；确实失控再加 mem_limit。

---

## 五、监控：上线后盯这两条

```bash
# 容器内存（每 2 秒刷新）
docker stats

# 整体内存 + swap 使用
free -m
```

**判断标准**：

| 观察值 | 含义 | 动作 |
|---|---|---|
| `free -m` 的 available > 250 MB | 健康 | 可以加玩家 |
| available 100~250 MB | 偏紧 | 停止加人，继续观察 |
| available < 100 MB 或 swap 持续在涨 | 危险 | 减玩家 / 升配置 |

**建议**：先按 **100 人**规模跑 2~3 天，记录 `docker stats` 的峰值，
再决定能不能往上加。不要一次拉满。

---

## 六、已做的代码优化

- **存档紧凑序列化**：`indent=2` → `separators=(",", ":")`
  - 体积 **-26%**（1000 人：1377 KB → 1012 KB）
  - 序列化耗时 **-13%**
  - 临时字符串内存峰值同步下降

---

## 七、还没做、可选的两项优化

当前存档是**单一 JSON 全量写**：任一玩家操作都要序列化 + 写盘整个存档，
成本 O(玩家数)；且写锁串行会让突发操作排队。

| 方案 | 收益 | 代价 |
|---|---|---|
| **写盘合并（debounce）** | 500 人 CPU 33% → ~2%（16 倍）；消除突发排队 | 进程被强杀最多丢 1 秒改动 |
| **按群分片存档** | 成本 O(总人数) → O(单群人数) | 改动中等，需迁移 |

**建议先不做**。你的瓶颈是内存不是 CPU，这两项主要优化 CPU 和响应延迟。
**先上线观察实际内存占用**，如果 NapCat 稳定在 300 MB 以内，就没必要动。

---

## 八、v3.5 上线注意（VIP / 天道令）

### 8.1 必须重启才加载新代码

新增了 `vip.py`、新指令 `/签到` `/兑换` `/vip` `/激活vip` `/至尊vip`。
**热重载不会加载新模块** —— 必须重启 AstrBot 进程。

### 8.2 必须配 `admins_id`，否则 VIP 发放不了

VIP 发放挂的是「天道」权限判定，来源是 AstrBot 的 `admins_id`。
**只填老板自己的 QQ** —— 因为设计上「VIP 只能由主人发放」，
多填一个人就等于把发放权交出去了一半。

发放主入口是两条精确短指令（群里发，要带 `/`）：

```
/激活vip 3455845639      → 普通 VIP（10 元/月，默认 1 个月）
/至尊vip 3455845639      → 至尊 VIP（30 元/月，默认 1 个月）
```

私聊机器人（或在群里 @机器人）时可以省掉 `/`。
**群里不 @机器人、直接发「激活vip 3455845639」不会触发** ——
AstrBot 要先确认这条消息是在跟机器人说话，这是平台层规则。

发完可以让对方发 `/vip` 自查（会显示 ✅ 已激活 + 剩余天数）。

### 8.3 新增的存档文件

```
data/plugin_data/astrbot_plugin_textrpg/
├── players.json      ← 老文件，格式未变（新增字段有兜底，老档不崩）
└── vip.json          ← 新增：全局 VIP 名单（按 QQ 号，跨群有效）
```

- `vip.json` **不存在也能正常启动**（降级为空名单），不用手动建。
- 玩家存档新增两个字段（`sign_in_date` / `special_owned`），
  `from_dict` 有兜底，**老玩家不会崩**，不需要迁移脚本。
- 建议把 `vip.json` 一起纳入备份（它记录的是**付过钱的名单**，
  丢了会很难对账）。

### 8.4 内存影响

VIP 名单在内存里是**全量常驻**（几百人也就几十 KB），
读盘只在启动时一次 —— 对 1核1G 的机器**无实质影响**。

---

## 九、什么时候该升配置

出现以下任一情况，建议升到 **2核2G**：

- `free -m` 的 available 长期低于 100 MB
- swap 使用量持续增长（说明在频繁换页，性能已经很差）
- 玩家超过 300 人且活跃度高
- 响应明显变慢（1 核跑 Python + Node 两个运行时，本来就紧张）

2G 相比 1G 通常每年只贵几十块，但余量翻倍、OOM 风险基本消除。
