# 文字 RPG 插件 · 设计文档

> 目标平台：AstrBot 4.x　｜　协议端：NapCat（OneBot v11）
> 版本：v1.1.0　｜　文件总数：12　｜　自测断言：88 项

---

## 一、整体架构

```
QQ 群消息
   ↓
NapCat（登录 QQ，把消息转成 OneBot 事件）
   ↓
AstrBot 消息平台适配器（aiocqhttp）
   ↓
AstrBot 事件流水线（过滤器匹配 → 指令路由 → 权限校验）
   ↓
┌─────────────────────────────────────┐
│  astrbot_plugin_textrpg             │
│  main.py   指令注册 + 消息组装       │
│  battle.py 战斗引擎                  │
│  player.py 玩家模型 + 数值           │
│  data.py   存档读写                  │
│  items.py  怪物/装备/商店数据         │
└─────────────────────────────────────┘
   ↓
data/plugin_data/astrbot_plugin_textrpg/players.json
```

**职责边界**：`main.py` 只做三件事 —— 注册指令、校验输入、拼消息。
所有数值计算在 `battle.py`/`player.py`，存档 IO 在 `data.py`。
这么切的好处：改伤害公式只动 `battle.py`，加新系统只需新增文件 + 几行注册。

---

## 二、文件清单

| 文件 | 行数 | 职责 |
|---|---|---|
| `main.py` | ~760 | 插件入口，17 条指令，配置读取，统一回复出口 |
| `selftest.py` | ~720 | 121 项断言，无需 AstrBot 环境 |
| `guard.py` | ~260 | 防封节流：延迟、分段、频率熔断 |
| `battle.py` | ~250 | 战斗引擎、经验结算、离线修炼 |
| `player.py` | ~215 | 玩家模型 + **全部可调数值** |
| `data.py` | ~250 | 存档读写、群隔离、格式迁移 |
| `items.py` | ~215 | 6 怪 / 11 装备 / 4 消耗品 / 商店 |
| `install.sh` | ~170 | 一键安装脚本 |
| `DESIGN.md` | 本文 | 设计文档 |
| `README.md` | ~190 | 使用说明 |
| `metadata.yaml` | 10 | 插件元数据 |
| `_conf_schema.json` | 60 | WebUI 配置面板（11 项） |

---

## 三、数据模型

### 存档键：`群号:QQ号`

**为什么按群隔离**：同一个人可能同时在多个群玩。在 A 群是大佬，
到 B 群不该直接满级带人。所以每个群各有一份独立进度。

```json
{
  "999:10001": {
    "qq": "10001",
    "group_id": "999",
    "name": "老板",
    "level": 5,
    "exp": 120,
    "hp": 180,
    "gold": 340,
    "inventory": { "小型生命药水": 3, "铁剑": 1 },
    "equipment": { "weapon": "铁剑" },
    "total_kills": 12,
    "total_deaths": 1,
    "last_train_ts": 1758212345.6
  }
}
```

私聊场景 `group_id` 为空字符串，键形如 `":10001"`，归入独立存档池。

**旧格式兼容**：v1.0.0 的键是纯 QQ 号（不含冒号）。
加载时检测到没有冒号就自动迁移，`group_id` 置空，不丢数据。

---

## 四、数值系统

所有参数集中在 `player.py` 顶部「可调数值区」，改平衡只动那一处。

### 玩家属性

```
攻击力   = 10 + (等级-1) × 3 + 装备加成
防御力   = 5  + (等级-1) × 2 + 装备加成
生命上限 = 100 + (等级-1) × 20 + 装备加成
升级经验 = 100 × 等级^1.5
```

升级是**逐级扣减**的：升到 2 级扣 `exp_needed(1)=100`，
升到 3 级再扣 `exp_needed(2)≈282`，不是线性累加。

### 战斗

```
单次伤害 = 攻击力 × rand(0.9, 1.1) - 目标防御 × 0.5   （下限 1）
回合上限 = 10 回合（超出判平局）
```

两条保护缺一不可：
- **伤害下限 1** —— 否则高防怪会让双方永远打不动
- **回合上限 10** —— 即使伤害正常，也可能出现双方血量都打不完的情况

### 商店

```
买入价 = 出售价 × 3
```

定 3 倍是为了让「买了再卖」必定亏本（净亏 2/3），防止刷金币套利。
装备有等级门槛：只能买攻击加成 ≤ `等级×2+6` 的装备。

### 离线修炼

```
每小时经验 = 30
时长上限   = 8 小时
最小结算单位 = 0.1 小时（不足不给收益，防刷指令）
```

---

## 五、指令表（17 条）

| 分组 | 指令 | 说明 |
|---|---|---|
| **基础** | `/报名` | 创建角色（幂等）|
| | `/属性` | 查看面板 |
| | `/背包` | 查看背包与已装备 |
| **战斗** | `/打怪` | 随机遭遇（默认 3 秒冷却）|
| | `/挑战 <怪物>` | 指定怪物，最多越 5 级 |
| | `/逃跑` | 60% 成功率，失败掉血 |
| **装备** | `/装备 <物品>` | 同部位自动换下 |
| | `/卸下 <部位>` | 武器/护甲/饰品 |
| | `/使用 <物品>` | 消耗品 |
| | `/出售 <物品>` | 已装备的禁止出售 |
| **商店** | `/商店` | 查看货架 |
| | `/购买 <物品>` | 下单 |
| **成长** | `/修炼` | 离线收益，上限 8 小时 |
| | `/排行榜` | **本群**排行 |
| **管理** | `/重置` | 管理员，清空本群存档 |

---

## 六、关键工程决策

### 1. 存档用 JSON 而非 SQLite

单群量级几十到几百人，JSON 完全够用，且调试时能直接打开看。
只有要做**跨服统计**或**百万级数据**时才需要换 SQLite。

### 2. 原子写入 + 自动备份

写盘用「临时文件 → `os.replace`」两步。`os.replace` 在主流文件系统
上是原子操作，因此不存在「写到一半进程被杀，存档变半截 JSON」的窗口。
覆盖前先把旧存档备份到 `players.json.bak`。

### 3. 异步锁串行化写入

群里两个人同一秒发 `/打怪`，两个协程会各自读同一份存档、改完再写回，
**后写的覆盖先写的，玩家的掉落凭空消失**。

解决：模块级 `asyncio.Lock` 把所有写操作串起来，
再用 `asyncio.to_thread` 把阻塞的文件 IO 挪出事件循环。

### 4. 存档损坏优雅降级

三层防护：
1. 主存档 JSON 解析失败 → 自动尝试读 `.bak` 备份
2. 备份也坏了 → 降级为空存档启动（**不崩插件**）
3. 单条记录字段异常 → 跳过那一条，不影响其他人

### 5. 配置必须真的被读取

**这是本项目踩过的坑。** 一开始声明了 `_conf_schema.json`
但代码里一个都没读 —— 用户在 WebUI 改了不生效，比不做配置还糟。

现在所有配置项都通过 `_conf()` 统一读取，并处理 5 种异常：
无配置、`config=None`、`config` 非 dict、值类型不对、值为 `None`。

---

## 七、测试策略

`selftest.py` 注入假的 `astrbot` 模块，让插件能在**没有 AstrBot 环境**
的情况下导入并跑完整流程。

覆盖 88 项断言，重点在**容易被忽略的边界**：

| 检查项 | 为什么重要 |
|---|---|
| 500 次抽怪伤害均 ≥1 | 防止高防怪导致战斗死循环 |
| 高防对局 10 回合内结束 | 回合上限真的生效 |
| 经验差 1 点不升级 | 升级边界 |
| 重启后读回存档 | 持久化真的落盘 |
| 塞入非法 JSON 不崩溃 | 存档损坏的兜底 |
| 同人多群进度互不影响 | 群隔离正确性 |
| 旧格式存档自动迁移 | 升级不丢数据 |
| 卖回亏本 | 防刷金币套利 |
| 已装备物品禁止出售 | 防误操作 |
| 非法配置值退回默认 | 配置健壮性 |
| 长文本分段内容无丢失 | 分段不能吞字 |
| 无标点文本硬切 | 极端输入 |
| 熔断只在触发会话生效 | 不能误伤其他群 |
| 沉默期过后自动解除 | 不能永久卡死 |
| 关闭防封后完全放行 | 开关真的生效 |
| 分段上限过小被钳到 50 | 防止消息被切碎 |

运行：`python selftest.py`

---

## 八、防封节流设计

### 为什么需要，以及它做不到什么

QQ 官方没有开放个人机器人的群聊接口，所以 NapCat 这类协议端都是
**逆向协议**——模拟客户端行为。腾讯随时能升级检测，这是猫鼠游戏。

**插件层防不了封号。** 风险主要来自协议端和账号本身。
插件能做的只有一件事：让机器人的行为特征别那么像机器。

### 三个可观测的机器人特征

| 特征 | 人类行为 | 机器人默认行为 | 对策 |
|---|---|---|---|
| **响应时间** | 有思考+打字时间 | 0ms 秒回 | 随机延迟 |
| **消息长度** | 零散、长短不一 | 整齐的长文本 | 超长自动分段 |
| **发送频率** | 有自然间隔 | 密集连续 | 频率熔断 |

### 实现：`guard.py`

```
ReplyGuard
├── wait_before(session)   随机延迟（首条 0.4~1.6s，分段间 0.6~1.3s）
├── split(text)            按 换行 > 句末标点 > 逗号 > 硬切 分段
├── is_muted(session)      是否在熔断沉默期
└── record(session)        记录发送，超阈值触发熔断
```

接入方式：`main.py` 里所有回复都走统一的 `_reply()` 出口，
而不是散落的 `yield event.plain_result()`。
好处是节流参数改配置就生效，不用翻几十处调用点。

### 阈值不能定太低（踩过的坑）

一开始设 `8 条/60 秒`，结果一个群里几个人各发几条指令
就累计触发了，**正常玩家的回复被静默**，看起来像机器人坏了。

改成 `20 条/60 秒 + 静默 20 秒`：
真人正常玩到不了这个量，脚本刷屏或滥用会立刻被拦住。

**教训**：凡是限流阈值，都要按「正常用户峰值」而不是「平均值」来定。

### 熔断语义：丢弃而非排队

触发熔断后本次回复直接丢弃，不入队。因为排队会导致
积压的消息在沉默期结束的瞬间集中发出——**那一刻的爆发反而更像机器人**。

---

---

## 九、已知限制

1. **没有 PVP** —— 玩家之间不能对战。
2. **没有转生/满级后的长期追求** —— 满级后只能刷装备。
3. **怪物 AI 是纯数值对撞** —— 没有技能、buff、元素克制。
4. **排行榜只显示等级** —— 没有金币榜、击杀榜。
5. **`metadata.yaml` 的 `repo` 是占位符** —— 本地可用，
   发布到插件市场需填真实 GitHub 仓库。

---

## 十、扩展方向

| 想加什么 | 改哪里 |
|---|---|
| 新怪物 | `items.py` 的 `MONSTERS` 加一条字典 |
| 新装备 | `items.py` 的 `EQUIPMENTS` 加一条 |
| 调数值平衡 | `player.py` 顶部「可调数值区」|
| 新指令 | `main.py` 加一个 `@filter.command` 方法 |
| 技能系统 | 新建 `skills.py`，在 `battle.fight` 里插入技能判定 |
| AI 生成剧情 | 用 `self.context.llm_generate()` 替换固定文案 |
| 转生系统 | `player.py` 加 `rebirth_count` 字段 + 属性乘数 |

---

## 十一、部署流程

```bash
# 1. 装 AstrBot（桌面版或命令行）
# 2. 跑 NapCat 连 QQ（建议小号）
docker run -d --name napcat -p 6099:6099 \
  -v ./napcat/config:/app/napcat/config \
  mlikiowa/napcat-docker:latest

# 3. 装插件（一键脚本）
bash install.sh /你的/AstrBot/路径

# 4. WebUI 启用插件，群里 /报名 开玩
```

`install.sh` 会自动定位 AstrBot、复制文件、跑自测、清理缓存。
