# 文字 RPG · v2.0 完整化设计稿

> 状态：**已定稿，开始实施**
> 基线：v1.4.0（18 条指令，207 项自测断言）
> 目标：v2.0.0
> 范围：**P0 + P1**（P2 技能/职业/任务/组队不做）

---

## 零、已定决策（老板拍板）

| # | 决策 | 说明 |
|---|---|---|
| D1 | **取消死亡惩罚** | 不掉金币、不掉经验。死亡只产生「休整时间」 |
| D2 | **死亡 → 10 分钟休整** | 或花费金币 / 药水立即复活 |
| D3 | **不做体力系统** | 节奏控制完全靠冷却 |
| D4 | **打怪冷却 5 分钟** | 300 秒，可配 |
| D5 | **修炼冷却 1 小时** | 3600 秒，可配 |
| D6 | **战斗全自动** | 不引入回合制交互，一次指令打完全程 |
| D7 | **取消 `/逃跑` 指令** | 战斗自动跑完，无需逃跑操作 |

**设计原则（修正后）**：
> 用**时间冷却**控制节奏，不用**资源惩罚**制造压力。
> 文字游戏的核心是「隔一段时间回来看看有什么变化」，不是「抠资源算账」。

---

## 一、P0 改动清单

### P0-1 · 捏脸系统

#### 流程

```
/报名
  ↓
[Step 1] 机器人：请发送你的角色名（2~12 字）
  ↓
玩家：独孤求败
  ↓
[Step 2] 机器人：请发一张图片作为角色形象
                 也可以发送 /跳过 使用默认形象
  ↓
玩家：[图片]
  ↓
[Step 3] 机器人：
  ┌────────────────────────┐
  │  确认创建角色           │
  │  名字：独孤求败          │
  │  形象：[图片]            │
  │  生命 100 ｜ 攻击 10     │
  │  防御 5 ｜ 金币 50       │
  │                        │
  │  回复「确认」创建        │
  │  回复「取消」放弃        │
  └────────────────────────┘
  ↓
玩家：确认
  ↓
机器人：🎉 角色创建成功，独孤求败！
```

#### 校验规则

| 项 | 规则 | 失败提示 |
|---|---|---|
| 名字长度 | 2~12 字 | 「名字长度需在 2~12 字之间」 |
| 名字内容 | 不能纯数字、不能纯符号、不含敏感词 | 「这个名字不太合适，换一个吧」 |
| 名字重复 | 同群内不能与他人重名 | 「本群已有玩家叫这个名字」 |
| 图片大小 | ≤ 5 MB | 「图片太大了，请发小于 5MB 的」 |
| 图片格式 | jpg / png / gif / webp | 「只支持 jpg/png/gif/webp」 |

#### 会话状态机

```python
# character.py
SESSION_TIMEOUT = 60.0   # 秒

class CreationSession:
    group_id: str
    qq: str
    step: str          # "name" / "avatar" / "confirm"
    name: str = ""
    avatar: str = ""
    created_at: float

class CreationFlow:
    """内存中的捏脸会话池，带超时清理。"""
    _sessions: dict[str, CreationSession]
    def start(self, group_id, qq) -> CreationSession
    def get(self, group_id, qq) -> CreationSession | None
    def advance(self, key, **kwargs) -> None
    def drop(self, key) -> None
    def sweep(self, now) -> int        # 清理超时会话，返回清理数
```

**超时**：60 秒未完成 → 自动放弃，提示「角色创建已超时，请重新发送 /报名」。

#### 图片存储

```
data/plugin_data/astrbot_plugin_textrpg/
  └── avatars/
      ├── 1095489827_12345678.png      # 群号_QQ号.png
      └── ...
```

存档只存相对路径 `avatars/1095489827_12345678.png`。

#### 数据模型变更

```python
@dataclass
class Player:
    name: str = ""              # 改为玩家自定义
    avatar: str = ""            # 新增：头像相对路径
    created_at: float = 0.0     # 新增：建号时间戳
    # 新增（P0-3 用）
    last_hunt_ts: float = 0.0   # 上次打怪时间
    last_train_ts: float = 0.0  # 已有
    rest_until: float = 0.0     # 新增：休整结束时间戳；0 = 不需要休整
```

---

### P0-2 · 冷却系统统一

**现状问题**：冷却存在 `main.py` 的内存字典里（`self._cooldowns`），**重启就丢**，而且打怪冷却只有 3 秒。

**新设计**：冷却时间戳**写进玩家存档**，持久化。

| 行为 | 冷却 | 配置项 |
|---|---|---|
| `/打怪` | **300 秒**（5 分钟） | `battle_cooldown` |
| `/挑战` | **300 秒**（5 分钟） | 复用 `battle_cooldown` |
| `/修炼` | **3600 秒**（1 小时） | `train_cooldown` |
| `/商店` `/背包` `/属性` 等查看类 | **无冷却** | — |

**冷却提示文案**：

```
⏳ 你刚打完一场，需要休息一下
剩余时间：4 分 12 秒
```

**为什么写进存档**：现在是内存字典，进程重启就清零 —— 玩家可以靠「重启机器人」刷新冷却，这是漏洞。

---

### P0-3 · 死亡机制重做

#### 新流程

```
（战斗中玩家 HP 归零）
  ↓
☠️ 你被 野狼 击败了

选择复活方式（60 秒内回复数字）：
┌─────────────────────────────────┐
│ 1. 原地休整                      │
│    等待 10 分钟，免费              │
│                                │
│ 2. 使用药水                      │
│    消耗 1 瓶生命药水，立刻复活     │
│                                │
│ 3. 花钱治疗                      │
│    花费 30 金币，立刻复活          │
└─────────────────────────────────┘
```

#### 数值设计

| 项 | 值 | 说明 |
|---|---|---|
| 休整时长 | **10 分钟** | 可配 `death_rest_seconds` |
| 药水复活 | 消耗 1 瓶**任意**生命药水 | 优先消耗最便宜的 |
| 金币治疗 | **30 金币** | 可配 `death_revive_gold` |
| 金币不足 | 选项灰掉，提示「金币不足」 | — |
| 药水不足 | 选项灰掉，提示「背包没有生命药水」 | — |

#### 休整状态的判定

```python
def is_resting(player, now) -> bool:
    return player.rest_until > now
```

**休整期间的限制**：
- 不能 `/打怪`、`/挑战` → 提示剩余时间
- 可以 `/属性`、`/背包`、`/商店`、`/购买`、`/出售`、`/装备`、`/使用`、`/排行榜`
- `/修炼` 允许（挂机嘛，躺着也能练）

**关键：不扣任何东西。** 死亡只是「暂停 10 分钟」。

---

## 二、P1 改动清单

### P1-1 · 怪物扩充（6 → 18 种）

| 档位 | 等级 | 数量 | 设计要点 |
|---|---|---|---|
| 新手区 | 1-5 | 6 种 | 皮薄，不会秒人 |
| 中级区 | 6-13 | 6 种 | 属性倾向明显（高攻/高防/高速） |
| 高级区 | 14-25 | 4 种 | 高数值，掉落好东西 |
| 精英 | — | 2 种 | 低概率出现，经验金币 ×2 |

**精英机制**：打怪时 8% 概率遇到精英版（名字加前缀「精英 ·」），属性 ×1.5，奖励 ×2。

### P1-2 · 装备扩充（11 → 30 件）+ 品质系统

#### 品质表

| 品质 | 标记 | 属性倍率 | 掉落权重 |
|---|---|---|---|
| 普通 | `[普]` | ×1.0 | 60 |
| 精良 | `[精]` | ×1.3 | 25 |
| 稀有 | `[稀]` | ×1.7 | 10 |
| 史诗 | `[史]` | ×2.2 | 4 |
| 传说 | `[传]` | ×3.0 | 1 |

**品质在掉落时决定**，同一件装备可以有不同品质：

```
铁剑[普]  攻击 +8
铁剑[稀]  攻击 +14
铁剑[传]  攻击 +24
```

#### 装备类型扩充

| 部位 | 现有 | 新增 | 总计 |
|---|---|---|---|
| 武器 | 3 | 7 | 10 |
| 护甲 | 5 | 7 | 12 |
| 饰品 | 3 | 5 | 8 |
| **合计** | **11** | **19** | **30** |

### P1-3 · 消耗品扩充（4 → 12 种）

| 类型 | 物品 | 效果 |
|---|---|---|
| 回血 | 小型/中型/大型生命药水、史莱姆凝胶 | 恢复 HP（已有） |
| 增益 | 力量药剂 | 下场战斗攻击 +20% |
| 增益 | 铁壁药剂 | 下场战斗防御 +30% |
| 增益 | 迅捷药剂 | 下场战斗必定先手 |
| 特殊 | 经验卷轴 | 立刻获得 100 经验 |
| 特殊 | 幸运符 | 下场战斗掉落率 ×2 |
| 特殊 | 复活符 | 死亡时自动免休整复活 |

### P1-4 · 战斗结果分级

| 结果 | 条件 | 奖励修正 |
|---|---|---|
| **碾压** | 获胜且 HP ≥ 90% | 经验 ×1.2 |
| **胜利** | 常规获胜 | 经验 ×1.0 |
| **惨胜** | 获胜且 HP < 30% | 经验 ×1.0，掉落率 +10% |
| **平局** | 打满回合上限 | 经验 ×0.3 |
| **败北** | HP 归零 | 无奖励，进入休整选择 |

---

## 三、指令变更汇总

| 指令 | 变更 |
|---|---|
| `/报名` | **改为三步交互**（名字 → 形象 → 确认） |
| `/打怪` | 冷却 3s → **300s**；自动战斗；结果分级 |
| `/挑战` | 冷却 300s；自动战斗 |
| `/修炼` | 新增 **3600s** 冷却 |
| `/逃跑` | **删除** |
| `/复活` | **新增**：死亡后选复活方式（或合并进死亡提示） |
| 其余 14 条 | 不变 |

**指令总数**：18 → 18（删 1 加 1）

---

## 四、配置文件变更

```json
{
  "battle_cooldown": 300,
  "train_cooldown": 3600,
  "death_rest_seconds": 600,
  "death_revive_gold": 30,
  "creation_timeout": 60,
  "name_min_length": 2,
  "name_max_length": 12,
  "avatar_max_mb": 5,
  "elite_chance": 0.08,
  ...
}
```

---

## 五、兼容性

| 项 | 保证 |
|---|---|
| 老存档 | ✅ 不丢。新字段全走默认值，`from_dict` 白名单已兜住 |
| 老指令 | ✅ 18 条全在（`/逃跑` 除外，明确删除） |
| 老配置 | ✅ 只增不改，旧配置自动补默认值 |
| 已激活群 | ✅ 不受影响 |

**针对 `/逃跑` 删除**：若有玩家用过，无影响（它不产生存档数据）。

---

## 六、实施顺序

| 步骤 | 内容 | 验证方式 |
|---|---|---|
| 1 | `character.py` 捏脸会话 | 自测断言 +25 |
| 2 | `player.py` 加字段 + 冷却持久化 | 自测断言 +15 |
| 3 | `battle.py` 死亡/结果分级 | 自测断言 +20 |
| 4 | `main.py` 改指令（报名/打怪/修炼/删逃跑） | 自测断言 +20 |
| 5 | `items.py` 怪物 18 + 装备 30 + 消耗品 12 + 品质 | 自测断言 +30 |
| 6 | 全量自测 + 真实环境验证 | **目标 317 项断言全绿** |

---

## 七、不做的事（明确排除）

| 项 | 原因 |
|---|---|
| 死亡惩罚（扣钱扣经验） | 老板决策：用冷却控制节奏，不用惩罚制造压力 |
| 体力系统 | 老板决策：不需要这层复杂度 |
| 回合制交互战斗 | 老板决策：战斗全自动 |
| 技能 / 职业 / 任务 / 组队（P2） | 本次范围外 |

---

*设计稿 v2 定稿 · 2026-09-19 · 小布*
