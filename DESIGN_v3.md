# 文字修仙 · v3.0 设计稿

> 状态：**已定稿，开始实施**
> 基线：v2.0（19 条指令，228 项自测断言全绿）
> 目标：v3.0.0
> 范围：**修仙化 + 修炼/闭关双系统 + 境界九转 + 探索 + 大道秘典框架**

---

## 零、老板拍板的决策（本轮全量）

| # | 决策 | 说明 |
|---|---|---|
| V1 | **题材改修仙** | 凡人体质起步，九转之后飞升成神 |
| V2 | **修炼 ≠ 闭关** | 修炼 = 主动一次性加修为；闭关 = 持续挂机到点结算 |
| V3 | 打怪冷却 5 分钟 | 沿用 v2 的 300s |
| V4 | **修炼冷却 5 分钟** | v2 的 `/修炼`（3600s 挂机）语义作废 |
| V5 | **闭关冷却 1 小时** | 3600s，闭关结束后起算 |
| V6 | 死亡 → 10 分钟休整 | 付费/丹药立即恢复 |
| V7 | 战斗全自动、无逃跑 | 沿用 v2 |
| V8 | **面板指令别名** | `/属性`、`/面板`、`/属性面板` 三条等价 |
| V9 | **属性计算唯一源头** | **面板值 = 基础属性 × 装备加成**。基础属性是唯一计算源，装备/秘典/转生只做乘区叠加 |
| V10 | **转生加成 +5%** | 每次转生 → 基础全属性 ×1.05 |
| V11 | **大境界突破 +20%** | 每次大境界突破 → 基础全属性 ×1.20 |
| V12 | **悟道 = 修行大道秘典** | 不是「花钱买修为」。改为：从大道中习得并提升道法 |
| V13 | **大道秘典从冒险掉落** | 探索 / 历练中随机获得 |
| V14 | **探索（新增挂机玩法）** | 挂机产出少量修为 + 各类道具（含秘典） |
| V15 | **天劫 = 抗住天劫** | 不是打赢，是**扛过 N 轮雷劫**。准备不足者扛不住 |
| V16 | **灵石后期有用** | 玩家交易、活动消耗。不做「灵石换修为」 |
| V17 | **可扩展性优先** | 数据结构要能加内容不改逻辑 |
| V18 | **大道数量务实** | 「三千大道」是世界观口径，不真做三千条。首期 5 条，加一条只是填表 |

**核心设计原则**：
> 一切玩法都收敛到**「基础属性 × 乘区」**这一条数据流上。
> 加新内容（新秘典、新装备、新活动）只填表，不改计算逻辑。

---

## 一、数据流：属性是怎么算出来的（V9）

这是整个系统的地基，所有后续设计都要遵守。

```
                    ┌─────────────────────────┐
                    │   基础属性（唯一源头）    │
                    │  atk / def / hp 基础值   │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │  乘区 A：境界倍率         │
                    │  炼气×1.0 … 大乘×4.0     │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │  乘区 B：转生加成         │
                    │  ×1.05 ^ 转生次数         │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │  乘区 C：大境界突破加成   │
                    │  ×1.20 ^ 突破次数         │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │  加区 D：装备加成         │
                    │  Σ 已装备法宝的 atk/def/hp│
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │     面板值（最终）        │
                    │  用于战斗计算与 /属性 展示│
                    └─────────────────────────┘
```

**实现约束（写代码时不许违反）**：

```python
# player.py 里只允许出现这样一组函数
def base_atk(turns: int) -> int:        # 基础攻击，只跟转数有关
def base_def(turns: int) -> int:
def base_hp(turns: int) -> int:

def realm_mult(ascensions: int, breakthroughs: int) -> float:
    # 只返回乘区总系数，不碰具体属性
    
# Player 上的最终取值
def atk(self, item_db) -> int:
    return int(base_atk(self.turns) * self.stat_mult()) + self.equip_bonus("atk", item_db)
```

**为什么这么定**：现在 `calc_atk(level)` 直接把等级代进公式算，以后要加「转生 +5%」
就得在所有计算点改一遍。改成「基础 × 乘区 + 装备」之后，加新乘区只需往
`stat_mult()` 里乘一项，战斗代码一行不动。

---

## 二、概念分层：修炼 vs 闭关（V2）

| | **修炼** | **闭关** |
|---|---|---|
| 动作性质 | 主动，立刻执行 | 承诺，进状态等结算 |
| 指令 | `/修炼` | `/闭关` |
| 收益 | 小、固定（按境界缩放） | 大、按时长（3× 修炼/小时） |
| 冷却 | **5 分钟** | **1 小时**（闭关结束后起算） |
| 中断 | 不中断 | 发指令可打断，收益打折 |
| 风险 | 无 | **走火入魔**，时长越长概率越高 |
| 心智模型 | 「我现在有空，练一手」 | 「我要睡了，挂一晚」 |

**走火入魔**（闭关专属）：

| 闭关时长 | 入魔概率 | 结果 |
|---|---|---|
| < 2 小时 | 0% | — |
| 2 ~ 4 小时 | 5% | 收益减半 |
| 4 ~ 8 小时 | 12% | 收益减半 |
| ≥ 8 小时 | 20% | 收益减半 |

入魔**不扣任何资源**，只是收益打折 —— 延续 v2「不用惩罚制造压力」的原则。

---

## 三、境界体系：八境九转，共 72 转

```
炼气期 → 筑基期 → 金丹期 → 元婴期 → 化神期 → 炼虚期 → 合体期 → 大乘期
 1~9     10~18    19~27    28~36    37~45    46~54    55~63    64~72
                                                                    ↓
                                                    抗住天劫 → 飞升 → 转生
```

| 境界 | 转数 | 突破门槛（累计修为） | 境界倍率 |
|---|---|---|---|
| 炼气期 | 1~9 | 0 | ×1.0 |
| 筑基期 | 10~18 | 2,000 | ×1.25 |
| 金丹期 | 19~27 | 8,000 | ×1.5 |
| 元婴期 | 28~36 | 25,000 | ×1.8 |
| 化神期 | 37~45 | 70,000 | ×2.2 |
| 炼虚期 | 46~54 | 180,000 | ×2.7 |
| 合体期 | 55~63 | 450,000 | ×3.3 |
| 大乘期 | 64~72 | 1,100,000 | ×4.0 |

- **境界内每转**：修为满足即自动精进（不打断，不弹提示也可以，静默 +1 转）
- **大境界突破**：需要手动 `/突破`，成功触发**乘区 C ×1.20**（V11）
- **突破有失败概率**，可用道具提高成功率（后续做）

### 天劫（V15：抗住，不是打赢）

大乘期九转圆满后 `/突破` → 第一次提示备劫，**再发一次 `/突破` 才真的应劫**
（防误触，用 `player._tribulation_ready` 标记区分两次调用）。

```
天劫共 9 道雷，逐道变强。
你的气血就是护盾 —— 抗得住几道，看你的准备。

第 1 道雷：基础伤害 = 你的最大气血 × 0.08
每道递增 8%（复利）
9 道累计伤害 = 最大气血 × 99.9%

抗住 9 道     → 飞升
抗住 5~8 道   → 渡劫失败，掉 2 转，10 分钟休整（可重来）
抗住 <5 道    → 渡劫失败，掉 3 转，30 分钟休整
```

**【2026-09-19 数值修正】** 初稿写的是每道递增 **12%**，实测：
9 道累计伤害达到 **118.2% 最大气血** —— 意味着**满血也必死**，
天劫成了永远过不去的死局，与「做好准备就能抗住」的设计意图相反。
改为 **8%** 后：

| 应劫时气血 | 能抗住几道 | 结果 |
|---|---|---|
| 100%（满血） | 9 道 | **飞升** |
| 85% | 8 道 | 失败，掉 2 转 |
| 70% | 7 道 | 失败，掉 2 转 |
| 50% | 5 道 | 失败，掉 2 转 |
| 30% | 3 道 | 失败，掉 3 转 |

**「准备不足者扛不住」的体现**：
- 气血上限不够 → 中途倒下
- 不满血应劫 → 大概率过不去（这是设计上的核心约束）
- 带够**渡劫丹**（每颗多扛一道）能补救
- 装备/境界倍率越高，气血越高，越容易满血过关

这是**装备与准备度的检验**，不是纯运气。

---

## 四、转生与加成（V10 / V11）

**转生（飞升后）**：

| 项 | 变化 |
|---|---|
| 转数 | 归 1（炼气期 1 转） |
| 修为 | 清零 |
| 境界倍率 | 重置为 ×1.0 |
| **转生加成** | **×1.05 **（可叠加）** |
| 保留 | 灵石、储物袋、法宝、秘典、图鉴 |
| 称号 | 「一转飞升 · XXX」、「二转飞升 · XXX」 |

**基础属性乘区**：

```python
stat_mult = realm_mult(当前境界) × (1.05 ** 转生次数) × (1.20 ** 大境界突破次数)
```

举例：三转飞升 + 当前金丹期（突破过 2 次大境界）
```
= 1.5 × 1.05³ × 1.20²
= 1.5 × 1.1576 × 1.44
= 2.50 倍
```

**注意**：突破次数在转生时**保留**（那是你修行的积累），只有境界倍率重置。
这样每一次转生的起点都明显比上一次高，而不是纯粹的重复劳动。

---

## 五、探索（V14，新增挂机玩法）

`/探索` —— 派遣自己外出游历，挂机产出。

| 项 | 设计 |
|---|---|
| 形态 | 进入探索状态，到点结算（同关闭关） |
| 最短时长 | 30 分钟 |
| 最长时长 | 8 小时 |
| 产出 | **少量修为** + 随机道具（含**大道秘典**） |
| 产出数量 | 每小时 1~2 次拾取判定 |
| 冷却 | 结算后 30 分钟 |
| 中断 | 与探索无关的指令不打断；再次 `/探索` 可提前结算（收益按比例） |

**产出表（可扩展）**：

| 类别 | 物品 | 权重 |
|---|---|---|
| 修为 | 少量修为 | 35 |
| 消耗品 | 回气丹 / 渡劫丹 / 悟道石 | 30 |
| 法宝 | 随机装备（按境界分层） | 17 |
| **秘典** | **随机一阶大道秘典** | 8 |
| 灵石 | 灵石袋 | 8 |
| 奇遇 | 特殊事件（待扩展） | 2 |

**探索 vs 闭关的分工**：
- 闭关 = 纯修为，收益高，风险是走火入魔
- 探索 = 修为少但**能出秘典**，是秘典的主要来源

两条挂机线让玩家选：要修为还是要资源。

---

## 六、大道秘典系统（V12 / V13 / V17）

> **口径说明**：「三千大道」是世界观说法，**不是要做三千条**。
> 目标是把**框架**搭好，先做能玩的量 —— v3.0 上 5 条大道，每条 3 阶。
> 以后想扩，往表里加一条就行。

### 6.0 首期内容：五条大道

覆盖**输出 / 续航 / 防御 / 控制 / 速度**五个方向，彼此有取舍，能玩出流派。

| 秘典 | 定位 | 一阶（主动） | 二阶（被动） | 三阶（被动） |
|---|---|---|---|---|
| **火之秘典** | 爆发 · 输出 | **火焰**：200% 攻击力火焰伤害，CD 1 回合 | **控火**：火伤减免 25% | **燎原**：全伤害附加 20% 攻击力火伤 |
| **水之秘典** | 续航 · 回复 | **回春**：回复 25% 气血，CD 2 回合 | **润泽**：每回合回 3% 气血 | **潮涌**：回复时额外净化 1 个负面 |
| **金之秘典** | 防御 · 反伤 | **铁壁**：本回合减伤 50%，CD 2 回合 | **锐金**：受击反伤 10% | **不坏**：气血 < 30% 时防御 +50% |
| **土之秘典** | 控制 · 削弱 | **落石**：150% 攻击力伤害 + 降敌攻 20%，CD 2 回合 | **厚重**：气血上限 +15% | **陷阵**：敌方每回合流失 5% 气血 |
| **风之秘典** | 速度 · 连击 | **疾风**：本回合额外攻击一次，CD 2 回合 | **轻身**：25% 概率闪避 | **追风**：每次命中叠 5% 攻速，上限 30% |

**为什么是这五条**：
- 每条都有「主动 + 被动」的层次，不是纯粹堆数值
- 五条之间有**取舍**：带火就少带金，堆输出就没防御
- 没有一条是废的：低级有火能打，高级带金能扛

### 6.1 数据结构（可扩展的核心）

**秘典定义表**（`scriptures.py`）：

```python
SCRIPTURES = {
    "火之秘典": {
        "dao": "火",                       # 所属大道
        "desc": "掌控世间之火",
        "tiers": {
            1: {
                "name": "火焰",
                "desc": "对敌方造成 200% 攻击力的火焰魔法伤害",
                "type": "active",          # 主动技
                "cooldown": 1,             # 冷却回合
                "effect": {"kind": "magic_damage", "mult": 2.0, "element": "fire"},
            },
            2: {
                "name": "控火",
                "desc": "你受到的火焰伤害 -25%",
                "type": "passive",         # 被动
                "effect": {"kind": "resist", "element": "fire", "value": 0.25},
            },
            3: {
                "name": "燎原",
                "desc": "你所造成的所有伤害附加 20% 攻击力的火焰魔法伤害",
                "type": "passive",
                "effect": {"kind": "add_damage", "mult": 0.2, "element": "fire"},
            },
        },
    },
    # ... 水 / 金 / 土 / 风，结构完全一致，只是效果字段不同
}
```

**设计要点**：
1. **秘典 = 一条大道 + 若干阶**。加新大道只需往 `SCRIPTURES` 加一项
2. **效果用 `kind` + 参数声明**，战斗引擎按 `kind` 分派处理，不写 if-else 链
3. **一阶是主动技，二阶三阶是被动**（你的设计），但数据结构允许任意组合
4. **阶数开放**：`tiers` 是字典，加四阶五阶不用改结构
5. **五条大道共用的效果类型只有 8 种**（`magic_damage` / `heal` / `buff` /
   `resist` / `add_damage` / `counter` / `dodge` / `dot`），
   加第六条大道八成能复用现成类型，不用碰引擎

### 6.2 效果类型注册表

```python
# battle.py 中，效果处理按 kind 注册，不用 if-else
EFFECT_HANDLERS: dict[str, Callable] = {}

def register_effect(kind: str):
    def deco(fn):
        EFFECT_HANDLERS[kind] = fn
        return fn
    return deco

@register_effect("magic_damage")
def _magic_damage(ctx, effect): ...

@register_effect("resist")
def _resist(ctx, effect): ...

@register_effect("add_damage")
def _add_damage(ctx, effect): ...
```

**这是 V17「可扩展性」的核心落地**：以后加「冰之秘典」「雷之秘典」，
只要新增一个 `kind` 处理函数 + 一条表格，不用碰主战斗循环。

### 6.3 玩家侧的秘典状态

```python
@dataclass
class Player:
    # 已习得的秘典：{"火之秘典": 2}  表示火之秘典已修到二阶
    scriptures: dict[str, int] = field(default_factory=dict)
    # 已装配的秘典（战斗中生效），数量上限跟境界走
    equipped_scriptures: list[str] = field(default_factory=list)
```

**装配位**：炼气 1 个 → 每突破一个大境界 +1 个（大乘期 8 个）。
后期能同时带几种，凑流派（比如「火 + 风」走高速爆发，「金 + 土」走铁桶消耗）。

### 6.4 指令

| 指令 | 作用 |
|---|---|
| `/大道` | 查看已习得的秘典与阶数 |
| `/修行 <秘典名>` | 消耗秘典残页 + 修为，提升一阶（即 V12 的「悟道」） |
| `/装配 <秘典名>` | 装上/卸下 |

**悟道（V12）的准确语义**：不是花灵石买修为，而是**修行大道秘典**。
- 每提升一阶，消耗「对应大道的秘典残页」+ 修为
- 秘典残页从探索 / 历练掉落
- 这样秘典既是掉落物，又是成长线

---

## 七、指令总表

| 指令 | 别名 | 类别 | 说明 |
|---|---|---|---|
| `/报名` | | 基础 | 三步捏脸（名字 + 形象图） |
| `/属性` | `/面板`、`/属性面板` | 基础 | 角色面板（V8） |
| `/背包` | | 基础 | 储物袋 |
| `/改名` | | 基础 | |
| `/修炼` | | 成长 | **立刻加修为，冷却 5 分钟** |
| `/闭关` | | 成长 | **挂机 8 小时，冷却 1 小时** |
| `/探索` | | 成长 | **挂机游历，出秘典** |
| `/突破` | | 成长 | 突破境界 / 渡天劫 |
| `/大道` | | 秘典 | 查看秘典 |
| `/修行` | | 秘典 | 提升秘典阶数（原「悟道」） |
| `/装配` | | 秘典 | 装配/卸下秘典 |
| `/历练` | `/打怪` | 战斗 | 随机遭遇 |
| `/寻敌` | `/挑战` | 战斗 | 指定对手 |
| `/恢复` | `/复活` | 战斗 | 死亡后恢复方式 |
| `/装备` | | 装备 | |
| `/卸下` | | 装备 | |
| `/使用` | | 装备 | |
| `/出售` | | 装备 | |
| `/坊市` | `/商店` | 经济 | |
| `/购买` | | 经济 | |
| `/排行榜` | | 社交 | |
| `/重置` | | 管理 | 仅主人 |
| `/激活` `/取消激活` `/激活列表` | | 管理 | 仅主人 |

**指令总数**：19 → 24

---

## 八、题材置换表

| v2 | v3 |
|---|---|
| 经验 exp | **修为 qi** |
| 等级 level | **转数 turns**（1~72，内部复用 level 字段） |
| 升级 | **精进** |
| 打怪 | **历练** |
| 怪物 | **妖兽 / 邪修** |
| 精英怪 | **妖王 ·** |
| 装备 | **法宝** |
| 武器/护甲/饰品 | **法宝 / 护身宝衣 / 灵饰** |
| 商店 | **坊市** |
| 背包 | **储物袋** |
| 金币 | **灵石** |
| HP | **气血** |
| 生命药水 | **回气丹** |
| 复活符 | **还魂丹** |
| 经验卷轴 | **悟道石** |
| 死亡 | **道消** |
| 休整 | **闭关疗伤** |
| 平局 | **势均力敌** |
| 碾压/惨胜 | **碾压 / 险胜** |
| 飞升 | **转生**（每次 +5% 基础属性） |

---

## 九、兼容性

| 项 | 保证 |
|---|---|
| 老存档 | ✅ 不丢。`exp` → `qi` 迁移，`level` 限制 1~72（超出的按 72 算） |
| 老指令 | ✅ `/打怪` `/挑战` `/复活` `/商店` 全保留为别名 |
| 老配置 | ✅ 只增不改 |
| 品质后缀 | ✅ `铁剑[稀]` 照常解析 |
| 已激活群 | ✅ 不受影响 |

---

## 十、实施顺序

| 步 | 文件 | 内容 |
|---|---|---|
| 1 | `realm.py` **新建** | 境界表、转数映射、修为曲线、转生/突破乘区 |
| 2 | `scriptures.py` **新建** | 五条大道秘典表 + 8 种效果声明 |
| 3 | `player.py` | `qi` / `scriptures` / `equipped_scriptures` / `ascensions` / `breakthroughs` 字段；**基础属性 × 乘区**重构 |
| 4 | `battle.py` | 修为结算、效果注册表（8 种 kind）、天劫、探索产出表 |
| 5 | `items.py` | 物品改名、怪物改妖兽、渡劫丹、秘典残页 |
| 6 | `main.py` | 24 条指令改造 + 面板三别名 + 文案置换 |
| 7 | `selftest.py` | 补断言，**目标 300+ 项全绿** |

**分两批交付**：

- **v3.0 第一批（能玩）**：修仙化改名 + 修炼/闭关 + 境界九转 + 转生突破乘区 +
  历练 + 探索 + **五条大道秘典跑通**
- **v3.1 第二批（扩内容）**：更多大道（第六条起只是填表）、装配位策略、
  渡劫丹体系、活动与交易

---

## 十一、不做的事（本次排除）

| 项 | 原因 |
|---|---|
| 玩家交易系统 | V16 提到了，但它是独立大模块，放 v3.1 |
| 宗门 / 师徒 / 活动 | 社交系统，v3.1+ |
| 渡劫丹的合成配方 | 先做「掉落即用」，合成后置 |
| 更多大道（第六条起） | 框架跑通五条，验证结构后再批量填 |

---

## 十二、修为曲线重做（V19 · 2026-09-19 定稿）

### 起因

老板要一张「修为数值表」做平衡依据。做表时发现旧曲线两个病：

| 病 | 表现 | 根因 |
|---|---|---|
| 缺口累积 | 越往后越吃力，大乘末期单转纯闭关 **126 小时** | 需求跨境 ×2.4~4，产出跨境恒定 ×2 |
| 两套数字打架 | `qi_needed(9)=1504` 但 `can_breakthrough` 要 2000 | 门槛表与曲线表分别手填 |

### 关键纠正（老板提出）

> 「你不能只考虑闭关啊，修炼也有修为啊」

第一版平衡我按**纯闭关单线**算，把实际产出砍掉了 5/16，于是把健康曲线误判成"有堵墙"。
实际三条线**冷却互相独立，可以并行**：

```
/修炼  5 分钟冷却 -> 每小时最多 12 次 × cultivate_gain   ← 占 75%，大头
/闭关  1 小时冷却 -> 每小时等效 × 3
/探索  30 分钟冷却 -> 每小时等效 × 1
                     合计 = cultivate_gain × 16
```

### 新模型

耗时公式：`境界耗时 = 该境 9 转总需求 ÷ 该境每小时产出`

要让耗时**温和递增**，必须让 `需求增速 r` 略大于 `产出增速 g`：

| 参数 | 值 | 说明 |
|---|---|---|
| `g`（产出跨境） | **×2.0** | 与旧版一致，好读 |
| `r`（需求跨境） | **×2.844** | `q = r/g = 1.422` |
| 境内步进 `TURN_QI_STEP` | **0.055** | 旧版 0.15 太陡 |
| 第 9 转系数 | **0.675 × 下一境基准** | 保留 1.4x 突破感 |

`QI_BASE = [784, 2229, 6340, 18030, 51276, 145830, 414741, 1179524]`

### 效果

| 境界 | 耗时 | 累计 |
|---|---:|---:|
| 炼气期 | 11.2h | 11h |
| 筑基期 | 16.0h | 27h |
| 金丹期 | 22.7h | 50h |
| 元婴期 | 32.3h | 82h |
| 化神期 | 45.9h | 128h |
| 炼虚期 | 65.3h | 193h |
| 合体期 | 92.8h | 286h |
| 大乘期 | 109.9h | **396h ≈ 16.5 天** |

- 首末比 **9.8×**：后期慢，但不是墙
- 曲线**严格单调**，无跳变失控
- 比旧版（纯闭关口径 3,286h）健康得多

### 架构改进

`REALMS` 第 3 字段（累计门槛）改为**由曲线自动派生**（`_sync_realm_thresholds()`）。
从此「唯一真相源」是 `QI_BASE` + `TURN_QI_STEP` + 第 9 转系数，
门槛表是算出来的，不可能再对不上。

`can_breakthrough` 也统一走 `qi_needed`，不再另查门槛表。

### 护栏

在 `selftest.py [43]` 新增 14 项**曲线不变量**断言：

- 每转需求全为正、全程单调不降
- 最大环比涨幅 < 1.6×（防曲线失控）
- 九转门槛与 `qi_needed` 完全自洽（need 够、need-1 不够）
- `REALMS` 门槛严格递增
- 三线倍率正确（闭关 ×3 / 探索 ×1）、跨境恰好 ×2
- **全程耗时落在 340~460h**、境界耗时单调递增、首末比 < 15×

曲线是"改坏了不报错、只是变难玩"的那类东西，必须用断言钉住。

---

*设计稿 v3 定稿 · 2026-09-19 · 小布*
*V19 修为曲线重做 · 2026-09-19 · 小布*

---

## 十三、v3.1：秘境副本 · 战力 · 装备扩展

### 起因

老板提了四件事：加副本机制、削普通打怪修为、增战力系统、妖兽动态缩放。
之后追加：装备扩到 5 件套（+5% 增伤），每件装备 +1% 穿甲。

### 【重要纠正】一个被写死的错误前提

我原来的设计里把这条当成"硬约束"：

> 怪血不能超过玩家 10 回合输出上限（约 0.89 × 玩家气血），否则必然平局。

**老板指出这个前提是错的**，原话：

> 「我前面设计的"怪血必须在玩家10回合输出上限以下"这个限制条件的前提是错的，
> 因为玩家可以靠装备属性来提高输出，比如穿甲和增伤。」

我实测验证了老板是对的：

| 配置 | 10 回合输出 / 自身气血 |
|---|---|
| 裸装纯普攻 | **≈ 1.8 ×**（我算的 0.89 是错误地减掉了怪防）|
| 加秘典（magic_damage 1.5~2.0×ATK、add_damage +0.2×ATK/回合）| **3 ~ 4 ×** |

所以怪血**完全可以**超过那个"上限"。这个错误前提把难度设计锁死了 ——
怪血恒定、难度只能靠攻击力堆，结果是"打得动就全赢、打不动就全输"的二元阶跃。

**教训**：写死"硬约束"之前，先确认它是在什么假设下成立的。
我那条约束只在"裸装 + 纯普攻"下成立，而游戏里有秘典和装备。

### 秘境难度曲线

老板原话：

> 「难度设计的不够合理，应该按照第一层最简单，然后逐层加难度，
> 妖兽属性走成长曲线，刚开始怪物属性尽量低，然后成长」

参数（`dungeon.py`）：

```
DUNGEON_ANCHOR_TURNS  = 1        # 锚点取最低档，曲线起点落在最弱玩家脚下
DUNGEON_HP_BASE       = 0.70     # L1 怪血 = 锚点血 × 0.70
DUNGEON_HP_STEP       = 1.13     # 每层 ×1.13（复利）
DUNGEON_ATK_BASE      = 1.20
DUNGEON_ATK_STEP      = 1.13
DUNGEON_DEF_BASE      = 0.40
DUNGEON_DEF_STEP      = 1.13
DUNGEON_HP_CAP_RATIO  = 5.0      # 兜底：怪血不超过玩家血 5 倍（v3.3 由 3.0 抬升）
DUNGEON_WARMUP_BACKOFF = 4       # 从最高层往回退 4 层开始（v3.3 由 2 抬升）
DUNGEON_RERUN_MULT    = 0.25     # 重刷层只给 25% 收益
```

**锚点三次迭代**（每次都用 400 场采样实测）：

| 锚点 | 结果 |
|---|---|
| 72 转 | 所有玩家 0 层 —— L1 怪血就超出所有人输出 |
| 3 转 | 1 转仍 0% —— 0.7 × 3转血 = 147 已超新人输出 |
| **1 转** | 1 转能过 L2，72 转能过 L46，曲线完全连续 ✓ |

### 战力系统

```
战力 = 攻击 × 2.0 + 防御 × 1.5 + 气血 × 0.5
```

`battle.player_power(player, item_db)`。裸装 1 转 77 → 72 转 16356（212 倍）。

**战力只用于面板展示，不参与战斗计算** —— 刻意如此，
避免它变成第二个难度旋钮，与妖兽缩放互相打架。

### 妖兽动态缩放

`monster_stats(monster, player_turns=None, ascensions=0, elite=False)`：

| `player_turns` | 语义 |
|---|---|
| `None` | 不缩放（图鉴展示、老测试）|
| 正整数 | 按转数缩放（历练 / 寻敌）|
| `≤ 0` 或 `prescaled=True` | **完全原样返回**（秘境自算难度后走这条）|

缩放方式：算出玩家裸装战力 → 按 `ratio`（普通 0.62 / 妖王 0.92）定目标战力
→ 按原始结构等比放大。等比放大保持 攻/防/血 的相对关系，
怪物的"手感"不变，只是整体挪档。

**刻意不含装备**：妖兽跟着装备一起涨的话，换装就毫无意义了。

#### v3.2 变更：等级偏差修正置零

`MONSTER_LEVEL_ADJUST` 从 `0.045` 改为 `0.0`（机制保留），
`MONSTER_LEVEL_GAP_CAP` 从 `4` 放宽到 `9`。

**为什么**：这个修正是 v3.1 为「妖兽数据只到 lv28」打的补丁 ——
当时玩家 46 转只能抽到 lv28 的怪，原始属性跟不上，所以要按等级差削弱它。

v3.2 做了两件事后，这个补丁变成了**双重惩罚**：

1. **妖兽补满 Lv1~72**，每只的属性都由「玩家在该 Lv 的面板」反推 ——
   本身就已经是同档位强度，不需要再按等级差削；
2. **抽怪改境界分池**，等级差最多 9 且 30% 概率抽邻境 ——
   等级差是**设计意图**（越境强敌），不该再惩罚一次。

实测（真实引擎，150 场/档）：

| 参数 | 平均回合 | 碾压率 |
|---|---|---|
| `adj=0.045`（旧） | 4.00 | 80% |
| `adj=0.0`（新） | 4.61 | 10% |

碾压率 80% 意味着「打得过但毫无波澜」，10% 才符合「5 回合击杀」的设计目标。
`GAP_CAP` 放宽到 9 是因为旧值 4 会把邻境怪（差 5~9 级）统一压成「弱 18%」，
让分池的 30% 交叉设计彻底失效。

### 装备扩展 3 → 5 部位

老板要「5 件套 +5% 增伤」，但原本只有 3 个部位，凑不出 5 件。
**老板拍板：扩到 5 个部位。**

新增（各 6 件，共 12 件新装备，装备总数 30 → 42）：

- **灵靴** `boots`：偏速度/机动，攻击 + 小量气血，防御薄
- **法印** `talisman`：偏防御/厚重，防御 + 厚血，攻击低

老存档兼容：`equipment` 是 dict，缺的部位自然为空，**无需迁移**。

### 穿甲与套装

```python
SET_BONUS_SLOTS      = 5      # 穿满 5 部位触发
SET_DAMAGE_BONUS     = 0.05   # +5% 增伤
PENETRATION_PER_ITEM = 0.01   # 每件 +1% 穿甲
PENETRATION_CAP      = 0.05   # 上限 5%
```

- `player.penetration()` → 每件 +1%，5 件封顶 5%
- `player.damage_bonus()` → 穿满 5 件给 0.05，否则 0
- `player.equipped_count()` → 只数 `SLOTS` 里认得的部位，
  **每部位最多算 1 件**（塞两个同部位骗不过套装判定）

**穿甲作用方式**（老板选的「按比例无视怪物防御」）：

```python
eff_def = target_def * (1 - penetration)
raw = max(1, int(atk * U(0.9, 1.1) - eff_def * 0.5))
raw = int(raw * (1 + damage_bonus))
```

**只在玩家打怪这一侧用**，怪反击玩家仍走原 `roll_damage` ——
保持两个入口，语义清晰。

实测：5% 穿甲 +2.1%，叠 5% 增伤共 **+7.3%**。
幅度克制，因为穿甲只减一个已经被减半的防御项。

### `/秘境` 指令

- 别名 `/闯关`，每日一次（`dungeon_date` 本地日期字符串）
- 从 `max(1, best-2)` 层开打，跳过已碾压的层
- 自动连闯，失败即止，**不计死亡、不掉转、不进休整**
- 每层满血开打（否则前层消耗会让玩家被"累积伤害"磨死，
  那是折磨不是难度）
- 收益：首通层全额，重刷层 25% 且不给物品。
  分界线是**本次开始前**的 `best_before`（必须在更新 `dungeon_best` 之前取，
  否则整趟都会被算成重刷）

### 历练定位调整

修为主来源是「修炼 / 闭关 / 探索」三条挂机线 + 每日秘境，
打怪降级为**物品与灵石来源**，修为压到 **15%**（`EXP_SCALE_FIELD`）。

保留 15% 而不是清零的理由：完全不给修为的话，玩家会觉得
「打赢了没反馈」，而且新人第一条线就是打怪，全砍掉会让开局失去正反馈。

实现方式：`fight()` 新增 `exp_scale` 参数（默认 1.0），
只由 `main.py` 的历练/寻敌路径显式传 0.15。
秘境奖励走 `dungeon.dungeon_qi_reward` 自己的结算，**不受影响**。

### 修掉的两个真 bug

| bug | 现象 | 修法 |
|---|---|---|
| **高层秘境怪血兜底完全失效** | `build_monster` 的兜底调用 `player.max_hp()` **漏传 `item_db`**，抛出的 `TypeError` 被 `except Exception: pass` 静默吞掉。结果第 999 层算出 **6.5×10⁵⁴** 的怪血，玩家必死 | 改为 `max_hp(item_db)`，并把 `item_db` 从 `run_dungeon` 一路透传到 `build_monster` |
| **`prescaled` 的怪仍吃到 `level × 15`** | 副本守卫声明"完全原样返回"，但等级加成（`bits.py` 的 `实际生命 = hp + level × 15`）在标记检查**之前**执行。当前副本怪恰好 `level = 0` 而躲过，**属巧合而非设计** | 把 `prescaled` 判断提前到 `base` 计算之前 |

**教训**：宽泛的 `except Exception: pass` 会把"参数传错"这种真 bug
伪装成"没事发生"。第一处 bug 潜伏了整轮开发，是自测的兜底断言
（期望 ≤ 3 倍玩家气血）把它逼出来的。

### 验证

- `selftest.py` 513 → **595 项断言**，连跑 **8 次**全绿无抖动
- 两种运行方式都通过：`python selftest.py` / `python -m astrbot_plugin_textrpg.selftest`
- 14 个 py 文件全部 `ast.parse` 通过
- 在真实 AstrBot 4.28.1 环境（`.venv`）里 `import` 成功，
  `TextRPGPlugin` 类正常注册
- 源目录与部署目录逐文件 `diff` 一致

### 新增分节 [47]

战力公式 / 妖兽缩放三态（`None` / 正数 / `≤0`）/ `prescaled` 语义
（含"不吃 level×15"的显式断言）/ 秘境曲线与可达层数 / 每日限制 /
失败不计死亡 / 5 件套与穿甲 / 历练修为折扣。

其中**兜底断言额外加了一条**「兜底确实生效（不是被异常静默吞掉）」，
直接断言血量等于上限值 —— 这是针对上面第一个 bug 的回归防护。

### 教训

- 老板说「为什么副本难度要和玩家战力绑定？就是固定难度啊哥」——
  我提问时把"固定难度曲线"和"玩家战力联动"混为一谈了。
  **副本难度是固定锚点曲线，玩家变强自然打得更远，这本来就是设计意图。**
  提问前先把概念理清，别造出不存在的问题。
- 自测的兜底断言必须是**等式**而不是**不等式**。
  原来写的是 `hp <= cap`，而 bug 状态下 `hp` 是个天文数字，
  不等式确实会失败 —— 但如果 bug 表现为"血量偏小"，
  不等式就抓不到了。改成 `hp == cap` 才能钉死。

*V3.1 秘境 · 战力 · 装备扩展 · 2026-09-19 · 小布*

---

## 十四、v3.2：数值平衡重做（2026-09-19）

> 老板在另一个窗口设计了方案（`修仙数值平衡方案_定稿.md`），
> 本轮负责**评估 + 实装 + 验证**。方案文档的推导过程不重复，
> 这里只记「实装时发现的问题」和「落地参数」。

### 方案的核心洞察

**妖兽的数值必须由玩家面板反推，不能独立设计。**

旧做法是「先编妖兽 exp，再算占比」，结果妖兽用 `2^k` 增长、
玩家用线性增长 —— 两条曲线必然跑飞。新做法反过来：

```
① 定玩法权重目标（打怪占 15%）
        ↓ 反推
② 妖兽 exp 应该多少
        ↓ 反推
③ 妖兽 hp/atk/def 应该多少（按玩家面板同量纲）
        ↓ 验证
④ 跑模拟战斗，确认每个境界、每种装备强度都能赢
```

落地公式（`items.py` 的 72 只怪全按这个算）：

```
妖防御 = 玩家面板防御 × 0.45
妖气血 = (玩家面板攻击 - 妖防御 × 0.5) × 4.5     ← 5 回合击杀
妖攻击 = (玩家面板气血/19 + 玩家面板防御 × 0.5) ÷ 平均伤害系数
                                                  ← 可扛 19 回合
```

### 实装时踩到的三个坑

**坑 1：方案数值是「绝对值」，但代码里还有一道缩放器。**

我按公式算出妖兽的绝对值，直接填进 `MONSTERS`，
结果真实战斗只有 3.8 回合 —— 因为 `monster_stats` 又把它缩到了
`玩家战力 × 0.62`。**两道工序重复施加。**

解法：`MONSTERS` 里存的属性只需保持**正确的攻:防:血结构比例**，
绝对值交给缩放器定。反推验证：方案目标的战力比是 0.6119，
而 `MONSTER_POWER_RATIO = 0.62` —— **两边天然自洽**，不用改常量。

**坑 2：`MONSTER_LEVEL_ADJUST` 从补丁变成了双重惩罚。**

见上文「v3.2 变更：等级偏差修正置零」。
这是本轮**影响最大的一处改动**（碾压率 80% → 10%）。

**坑 3：秘境锚点绑定在 `base_atk(1)` 上。**

方案没提到这个副作用：改 `INIT_ATK` 会连带拉高秘境怪的三围
（第 46 层怪攻 2,935 → 5,284，+80%）。
实测极限层整体下移 2~3 层（72 转 46 → 44），属可接受范围，
但**改初始属性时心里要有这个数**。

### 验证方法：不信任简化模型

方案自带的验算脚本用的是**期望值近似**（`pa*AVG - mdef*0.5`），
我另外写了一套跑**真实 `battle.fight()`** 的验证，两者结论一致但精度不同。

其中我自己也写错过一次：为了"复刻"战斗循环，漏了回合上限的 `else` 分支，
把「打满 10 回合的平局」误记成「败北」，一度得出"妖王档 0% 胜率"的错误结论。
**教训：能调真实引擎就别复刻逻辑。**

验证脚本（`_balance/` 下，可复跑）：

| 脚本 | 作用 |
|---|---|
| `verify_final.py` | 方案自带验算（期望值近似） |
| `verify_truth.py` | 真实引擎逐条验证（prescaled 注入） |
| `verify_e2e2.py` | 端到端：真实抽怪 + 真实引擎，全转数扫描 |
| `tune_gap.py` / `tune_gap2.py` | gap 参数扫描调优 |
| `verify_dungeon_impact.py` | 初始属性改动对秘境的连带影响 |

### 终态手感（真实引擎实测）

| 档位 | 回合数 | 碾压率 | 败北率 |
|---|---|---|---|
| 普通（裸装） | 5.0 ~ 5.7 | 0~2% | 0% |
| 妖王（裸装） | 7.3 ~ 9.1 | — | 0%（100% 胜） |

跨 1~69 转共 18 个采样点，**全部一致**。这就是「产出跟境界走」在战斗层的落地。

*V3.2 数值平衡重做 · 2026-09-19 · 小布*

---

## 十五、v3.3：解开回合上限枷锁（2026-09-19）

v3.2 把妖兽属性改成「按玩家面板反推」之后，留下一个**不自洽**：

> 反推公式要求「玩家可扛 19 回合」，
> 但 `MAX_BATTLE_TURNS = 10` —— **19 回合物理上做不到。**

这不是数值问题，是**设计目标与实现直接冲突**。后果在妖王身上最明显：

| | 期望体验 | 上限 10 时的实际 |
|---|---|---|
| 普通怪 | 5 回合击杀 | 5~6 回合 ✓ |
| 玩家可扛 | 19 回合 | ❌ 不可达 |
| 妖王（血厚） | 磨一场硬仗 | ❌ 第 10 回合判 `draw`，玩家还剩 47~74% 血 |

**`draw` 是最坏的结果类型**：它把「打不完」和「打不过」合并成同一种结算，
玩家看到的是「鏖战 10 回合未分胜负」，体感接近失败，但实际毫无损失 ——
既没有挫败的明确原因，也没有胜利的正反馈。两头不靠。

### 改动

| 参数 | 旧 | 新 | 性质 |
|---|---:|---:|---|
| `MAX_BATTLE_TURNS` | 10 | **30** | **修逻辑冲突**（非平衡调整） |
| `DUNGEON_HP_CAP_RATIO` | 3.0 | 5.0 | 跟随上限等比抬高兜底线 |
| `DUNGEON_WARMUP_BACKOFF` | 2 | 4 | 节奏感 |

**为什么是 30**：妖王实测 7~9 回合，30 ≈ 2 倍余量给秘典/装备成长留空间；
同时仍然兜住「双方高防互相打不动」的死循环。

### 实测（真实 `fight()`，300 场/采样点 × 13 个转数）

| 档位 | 平均回合 | 最高回合 | 胜率 |
|---|---|---|---|
| 普通 | 5.0 ~ 6.8 | 8 | 100% |
| 妖王 | 7.2 ~ 9.1 | 11 | 100% |

**零平局、零败北**，回合数远离 30 上限 —— 上限是**安全垫**，不是瓶颈。

### ⚠️ 两处「改了但没效果」的诚实记录

数值工作最容易自欺的地方：改了参数就假定有效果。这次有两项**实测无效果**，
都记在这里，避免后人以为它们有用：

1. **`DUNGEON_HP_CAP_RATIO` 从未被触发。**
   cap 取 3.0 / 5.0 / 99.0 三档跑首通，各转数最高层**完全一致**
   （1转 2.0／10转 18.3／21转 25.4／30转 29.9／46转 36.7／60转 40.2／72转 43.1）。
   此项属「把安全垫抬到与新上限匹配」，防将来撞线，**不修当前任何问题**。

2. **`DUNGEON_WARMUP_BACKOFF` 不提高推进速度。**
   曾以为退更多层能加快净推进，**实测否定**：

   | backoff | 10 转 | 30 转 | 46 转 | 72 转 |
   |---|---|---|---|---|
   | 2 | +0 | +0 | +0 | +1 |
   | 4 | +1 | +0 | +0 | +1 |

   中期无论取多少都是 0 —— 卡点是**曲线天花板**，不是起始层。
   它管的是「进门有热身」的节奏感，不是「打得动打不动」。
   真正的修法见 `秘境副本数值设计.md`。

### 仍未做（需独立一轮）

- **装备绝对加法中后期失效**（大乘期满装 +5%）。
  修法见 `装备分层方案_A2.md`。**注意**：它会与 `MONSTER_POWER_RATIO = 0.62`
  的妖兽缩放器互相抵消（玩家变强→怪跟着变强→装备收益被吃掉），
  上装备分层必须**同时重标定缩放器**，不能只加装备表。
- **秘境深度停滞 / 43 层后奖励难度比 < 1.0**。见 `秘境副本数值设计.md`。

### 回归防线（新增 2 条断言，共 605 项）

- 「回合上限 ≥ 20」—— 压回 10 会被立刻拦下；
- 「秘境 HP_CAP ≥ 5.0」—— 保证兜底线与回合上限匹配。

*V3.3 回合上限修复 · 2026-09-19 · 小布*

---

## 十六、v3.4：十品装备体系 · 帝兵 · 数值实验室（2026-09-19）

> 老板定的方向：装备**独立分十级**（一品~九品 + 极品帝兵），
> **不走**「按境界分层」；装备加成改**百分比**；
> 中上品引入「无视防御 / 反伤 / 免伤」等高级乘区。
>
> 同时修掉三个**静默吃掉玩家收益**的真 bug，并把数值方法论
> 固化成可复跑的实验室。

### 16.1 十品装备体系

**135 件 = 9 品阶 × 3 套系 × 5 部位**（+ 帝兵走独立表）。

| 品阶 | 档位 | 倍率骨架 | 转数门槛 |
|---|---|---|---|
| 一 / 二 / 三品 | 下品 | 每级 ×2 | 1 / 10 / 19 |
| 四 / 五 / 六品 | 中品 | 每级 ×2.5 | 28 / 37 / 46 |
| 七 / 八 / 九品 | 上品 | 每级 ×3 | 55 / 64 / 68 |
| 帝兵 | 特殊 | 全服定制 | **无门槛** |

三套系**机制分层**（老板要的「不要一味乘算」）：

| 套系 | 定位 | 机制 |
|---|---|---|
| 物穿 | 破防输出 | 穿甲 + 概率无视防御（追加伤害）+ 暴击 |
| 法穿 | 术法输出 | 穿甲 ×1.15 + 术法增伤 + 冷却缩减 |
| 肉盾 | 生存 | 减伤 + 反伤 + 低血线额外减免（上品）|

**机制实现走注册表**（`ATTACK_MODIFIERS` / `DEFENSE_MODIFIERS` +
`@register_attack_modifier` / `@register_defense_modifier`）：
主循环只查表遍历，不写 if-else 链。加新机制 = 加一个函数。

**套装判定**：必须**同一 `set_id` 穿满 5 件**。3+2 混搭不触发任何机制。

### 16.2 面板公式改百分比（老板要求）

```
旧：面板 = 基础 × 乘区 + 装备绝对值
新：面板 = 基础 × 乘区 × (1 + 装备%) × (1 + 特殊装备%)
```

**为什么必须改**（v3.3 留下的坑）：

| | 炼气期 | 大乘期 |
|---|---|---|
| 顶级 5 件装备相对增益 | **+722%** | **+5%** |

乘区跨境翻倍（×2），装备却是固定值 → 越后期越没用。
改百分比后装备跟着面板涨，任何境界保持同一份相对增益。

#### ⚠️ 与妖兽缩放器的关系（v3.3 遗留警告的复核）

v3.3 章节留了一句警告：

> 「上装备分层必须**同时重标定缩放器**，不能只加装备表 ——
> 它会与 `MONSTER_POWER_RATIO = 0.62` 的妖兽缩放器互相抵消。」

**实测结论：这条警告在当前实现下不成立，但原因是设计上的，不是巧合。**

```python
# battle.monster_stats()
p_atk = int(base_atk(turns) * sm)      # ← 只算「基础 × 乘区」
```

缩放器锚定的是**裸装战力**，**不含装备百分比**。所以：

- 装备收益**不会被缩放器吃掉** —— 它是玩家实打实的成长杠杆；
- 代价是装备越强，游戏越简单。实测（`lab.report` [5] 节）：

| 配置 | 杀敌回合 TTK | 可扛回合 | 安全边际 |
|---|---|---|---|
| 九品裸装 | 4.53 | 21.46 | 4.74 |
| 九品满装 | 3.01 | 28.85 | **9.57** |

**这是一个刻意的设计取舍**，不是 bug：如果让缩放器锚定「含装备战力」，
装备就完全白拿了（玩家变强→怪同步变强→净收益 0）。
现在的做法保证「练装备 = 变强」，但把难度问题暴露了出来
（见 16.6 待决项）。

### 16.3 帝兵（特殊装备）

| 特性 | 实现 |
|---|---|
| 栏位 | `Player.special`，**不占 5 个部位**；无帝兵时不渲染【特殊】栏 |
| 门槛 | 无阶位限制（`can_equip` 内部对特殊装备直接放行）|
| 加成 | `base_pct` 62%，**叠乘**在普通装备之上（额外一层乘区）|
| 成长 | `passives` 列表驱动，`PASSIVE_HANDLERS` 注册表分发 |
| 死亡 | 不清零（`lose_on_death: false`，可配）|
| 获取 | `source: "lottery"`，`unique` 有主即出池 |

**可扩展性（老板明确要求「别写死」）**：

- 加一件帝兵 = 往 `SPECIAL_EQUIPMENTS` 加一条记录，**逻辑层不动**；
- 加一种成长方式 = 写一个 `@register_passive("xxx")` 函数；
- 「怎么涨 / 涨到哪 / 死不死」全在数据表的 `passives` 里，
  对应老板的「具体问题具体分析」。

指令：`/特殊` 查看 · `/特殊 认主 <id>` · `/特殊 卸下` · `/特殊 列表`。
战斗结算统一收口在 `_apply_death`（历练/寻敌都走它），
成长推进挂在那一处，新增战斗入口不会漏。

### 16.4 修掉的三个静默 bug（都会吃掉玩家收益，不崩不报错）

#### ① 带品质后缀的装备，加成全部归零 🔴

装备名有**两套写法**并存：

| 来源 | 名字 |
|---|---|
| `items.EQUIPMENTS[i]["name"]` | `青锋剑`（基础名）|
| 掉落 / 坊市 / 玩家身上 | `青锋剑[普]`（带品质后缀）|

`equip_pct` 直接 `item_db.get(名字)` → 带后缀的查不到 → **静默返回 0**。

更隐蔽的是**口径分裂**：即便查到基础名，拿到的也是**未乘品质倍率**的裸值，
而展示/掉落走 `items.get_item`（会乘倍率）→
**同一件装备，面板显示一个数、实际生效另一个数**。

修法：新增 `Player._lookup_equip()`，把「解析名字 + 乘品质倍率」
统一交给 `items.get_item` 单一出口。

#### ② 玩家输入基础名 → 「明明有却被告知没有」

储物袋存 `小回气丹[普]`，玩家输入 `小回气丹`，指令直接查 `inventory.get`
查不到。`/装备` `/使用` `/出售` 全中招。

修法：`Player.find_inventory_name(name, *, prefer_quality=True)`。
**品质倾向按玩家意图分**：

| 指令 | 倾向 | 理由 |
|---|---|---|
| `/装备` `/使用` | 品质高者优先 | 「装上剑」显然想用好的 |
| `/出售` | 品质低者优先 | 卖东西意图相反 |

⚠️ 两个分支都必须是**全量扫描取极值**，不能「遇到第一个就 return」
（那等于依赖 dict 迭代顺序，行为不可复现 —— 调试时真踩到过）。

#### ③ 品阶门槛定义了但没有任何地方校验

`req_turns` 数据一直在，却没有闸门 → **1 转新手能直接穿九品毕业装**，
十品体系的成长曲线被一次性跳过。

修法：`/装备` 补校验，判定走 `items.can_equip`
（**不能手写 `turns >= req_turns`** —— 帝兵是无阶位限制的例外，
手写比较会把帝兵一起拦掉）。

#### ④ 碾压击杀不算「弑杀」（端到端才抓得到）

战斗结果五档，**三档都算赢**：

```
crush（碾压） / win（胜利） / narrow（惨胜）  →  算赢
draw（平局）  / lose（败北）                  →  不算
```

成长代码写的是 `outcome == OUTCOME_WIN` → **碾压和惨胜全被漏掉**，
而碾压恰恰是最常见的结果 → 表现为「打了一晚上帝兵一次没涨」。

修法：改用 `result.won`。

> **为什么单元测试抓不到**：直接调 `advance_passives(kills=1)` 永远是对的，
> bug 在「什么算击杀」这一层 —— **测试替被测代码假设了前提**。
> 为此专门写了走真实指令流程的 `verify_v34_special.py`，一次就顶出来了。

### 16.5 数值实验室（`_balance/lab/`）

老板的要求：**「数值计算你需要做好点，不然以后可能会崩，你可以参考别的成熟游戏的数值计算」**。
本版把它固化成可复跑的实验室，而不是「跑一遍看看」。

| 模块 | 作用 |
|---|---|
| `lab/__init__.py` | 环境自检 `env_guard()` + 统一夹具 `make_player()` |
| `lab/analyze.py` | 收益归因 / 强度对齐 / TTK-TTLK 战斗模型 / 品质口径检查 |
| `lab/report.py` | 一条命令出 6 节完整体检 |
| `lab/baseline.py` | 黄金基线 `save` / `check`（超 3pp 告警）|

**收益归因三层拆解**（相对同转数裸装的百分点）：

```
面板增益 = 纯面板 / 裸装 - 1     只看装备给的属性
机制增益 = 完整   / 纯面板 - 1   只看破防/暴击/反伤等机制
总增益   = 完整   / 裸装   - 1   玩家实际拿到的
```

**强度对齐用「合成增益 = 输出 + 生存」**：肉盾套机制全长在防御侧，
只看输出会误判它失衡 —— 那是判据错了，不是数值错了。

**安全边际** = `可扛回合 / 杀敌回合`：≥2.0 碾压 / ≥1.4 稳 / ≥1.1 险 / <1.1 必败。

#### 🔴 实验室踩的最贵的一课：夹具替线上漂白

实验室早期用**裸名**（`e["name"]`）造玩家，于是 bug ①
**在实验室里完全不可见** —— 报告还一路绿灯（「九品满装增益 +22.3%」），
而线上玩家穿同样的套装加成是 0。**实验室比线上好看，方向正好相反。**

修法两处：

1. `Slot.quality` 默认 `"common"`，造玩家时加品质后缀 —— **与游戏内一致**；
2. `env_guard()` 新增第 7 项，钉住「裸名 vs 带后缀名必须算出同一份加成」。

> **通用结论：夹具的口径必须等于线上真实口径。**
> 夹具一旦比真实情况「更干净」，实验室就会系统性地漏掉一整类 bug，
> 而且给出**假绿灯**。

#### 其他判据修正

- **总增益地板按品阶分档**（`MIN_COMBINED_BY_RANK`）：
  第一版用固定地板 0.5%，把一品装备全标红「几乎无效」——
  但那是**设计意图**（老板说「一阶装备只会少量增加」）。
  固定地板还有个更糟的后果：把正常的一品误报成问题，
  真正的异常反而淹没在噪声里。
- **品质口径一致性检查**（`analyze.quality_consistency()`）接进报告常驻第 2 节。

### 16.6 实验室量化出的真问题（待设计师定夺，未修）

| 品阶 | 物穿 | 法穿 | 肉盾 | 合成极差 |
|---|---|---|---|---|
| 四品 | +13.3% | +21.2% | +17.8% | 8.2pp ✓ |
| 七品 | +37.5% | +54.5% | +38.0% | **16.8pp ✗** |
| 九品 | +49.7% | +67.5% | +53.3% | **18.0pp ✗** |

- **法穿套超模**：`MAGIC_AMP_TO_ATTACK` 这个折扣系数当初为
  「让法穿不只是秘典专属」而加，实际把它变成了**主输出通道**。
  这个修复方向本身是错的（加了之后法穿更超模），需要重新设计。
- **肉盾套输出通道缺失**：反伤对高血怪几乎无意义
  （400 场触发 1216 次，对 11205 血怪贡献 0.035%）。
  它有 +33% 生存增益，合成口径下不算崩，但机制贡献只有 +2.2%。
- **战斗难度整体偏松**：全境界安全边际 4~9.6（碾压级），
  玩家几乎不可能输。九品满装边际 9.57 比裸装 4.74 高出一倍。

> 这三项已上报老板，老板回复「我去问问设计师」——
> **本版不动**，等设计师意见再改，避免朝令夕改。

### 16.7 真实引擎与真实存档验证

- **真实 AstrBot 4.28.1 加载验证**（`verify_real_engine.py`，37 项全绿）：
  14 个子模块导入成功、`star_handlers_registry` 里 31 个 handler、
  v3.4 三个新指令（`/菜单` `/简介` `/特殊`）都在册、实例化正常、
  `item_db` 覆盖 135 件装备、`data_dir` 解析正确。

  > **为什么 stub 测试不够**：`selftest.py` 注入的是**自己写的假 astrbot**。
  > stub 能验证游戏逻辑，但验证不了「真实导入路径是否存在」
  > 「真实 filter 的行为」「真实 Star 签名」——
  > 「stub 里有」这件事本身没有任何保证。

  > 顺带纠正一个探针错误：真实 `filter.command` **不往函数上挂属性**，
  > 而是把 `StarHandlerMetadata` 追加到全局 `star_handlers_registry`。
  > 遍历类属性找 `_cmd` 在真实环境下会探到 0 个 —— 那是探针写错，不是装饰器失效。

- **真实存档迁移验证**（`verify_live_migration.py`，6 项全绿）：
  线上 `players.json` 里 `小型生命药水` → `小回气丹[普]`，
  所有物品名都能查到定义，二次迁移幂等，**线上数据字节级未变**。

  > 规矩：迁移验证**只读线上、改动落在副本**。先备份再跑，逐项核对。

### 16.8 回归防线

- 自测断言 **605 → 697**，连跑 8 次全绿。
- 新增独立端到端脚本 `verify_v34_special.py`（15 项，走真实 `/历练` 流程）。
- 新增真实引擎脚本 `verify_real_engine.py`（37 项）。
- 新增真实存档迁移脚本 `verify_live_migration.py`（6 项）。
- 黄金基线 27 条归因 + 9 条战斗模型，`check` 无漂移。

### 16.9 仍未决

**待设计师**：① 三套失衡怎么修（推荐「机制各归其位」）② 战斗难度是否收紧。
**待老板**：③ 天道抽奖的概率与消耗 ④ 套系命名 ⑤ 十品装备获得方式 ⑥ 帝兵唯一性细节。

*V3.4 十品装备 · 帝兵 · 数值实验室 · 2026-09-19 · 小布*

---

## 十七、v3.5：天道令兑换 · 帝兵去唯一化 · VIP 赞助（2026-09-19）

### 17.1 本轮老板拍板

| # | 议题 | 决定 |
|---|---|---|
| ③ | 帝兵获取 | **废弃天道抽奖** → 「天道令兑换」：每日签到 +1、秘境首通 +1，**100 枚换一件** |
| ④ | 套系命名 | **否掉设计师的「剑修/法修/体修」** —— 那是**职业名**，留给未来的角色创建；套系另起 **破锋 / 通玄 / 镇岳** |
| ⑤ | 十品装备 | **七品以上下架坊市**，靠秘境里程碑产出 |
| ⑥ | 帝兵唯一性 | **不搞唯一了，全民可获取** → **连带必须下调数值** |
| — | 天道令产出 | 明确「副本首通也给」（老板补充） |
| — | VIP | 只能由**主人（天道）发放**；普通 10 元/月 +1 枚、至尊 30 元/月 +3 枚 |

### 17.2 ③ 天道令兑换

```
        ┌─────────────┐
        │   天道令     │   攒 100 枚
        │  （凭证道具） │ ────────────►  帝兵 ×1
        └─────────────┘
              ▲
      ┌───────┴────────┐
  每日签到         秘境首通
   +1 枚           +1 枚（按整趟，不是按层）
```

**道具定义**：`{"name": "天道令", "effect": "token", "value": 1, "price": 0}`

`effect: "token"` 是刻意的，带来两条硬约束：

| 约束 | 拦在哪 | 为什么 |
|---|---|---|
| 不能被 `/使用` 服用 | `main.use_item()` | 不拦的话玩家会以为它是丹药 |
| 不能被 `/出售` 换灵石 | `main.sell()` | 100 枚 = 一件帝兵，误卖一枚就是白攒一天 |

`price: 0` 是双保险。

**首通按「整趟」发，不是按「每层」发**：

| 口径 | 后果 |
|---|---|
| ✗ 每首通一层给 1 枚 | 从第 5 层连破 20 层 → **一次拿 20 枚，5 天凑齐帝兵** |
| ✓ 整趟有首通就给 1 枚 | 一天最多 1 枚，节奏可控 |

这个判定**天然防刷**：打不过去的人拿不到，反复重刷老层也拿不到 ——
不需要额外加计数器或冷却。

**背包满强制入包**：进度型道具若因背包满被吞，玩家会白等一天
而且**根本不知道漏了**。所以入包失败时强制写入（最多突破 1 格上限）。

**节奏测算**：

| 玩家 | 前期 | 后期 | 凑齐 100 枚 |
|---|---|---|---|
| 免费 | 2 枚/天 | 1 枚/天 | **50 ~ 100 天** |
| 普通 VIP | 3 枚/天 | 2 枚/天 | **33 ~ 50 天** |
| 至尊 VIP | 5 枚/天 | 4 枚/天 | **20 ~ 25 天** |

> **取舍要知情**：严格按「首通」口径，秘境产出会随玩家推进变慢而
> **自然衰减到 0** —— 老玩家最后只能靠签到（100 天）。
> 若希望稳定在 50 天，把口径改成「每日秘境通关」即可（一行代码）。
> 老板确认走 A（严格首通）。

### 17.3 为什么废弃抽奖（写给以后的我）

| # | 理由 |
|---|---|
| 1 | **唯一物 + 概率制 = 结构死局**。概率太低 → 几个月没人抽到，活动形同虚设；概率太高 → 第一个人抽走后池子空了，**后面所有人抽了个寂寞，而且活动从此没有内容** |
| 2 | **确定性比概率更有粘性**。「今天 73/100」能看着涨；抽奖只能靠运气安慰自己 |
| 3 | **全民化之后抽奖失去意义**。帝兵不再唯一，「抽到」不再稀缺 |

**接口替换**：

| 原函数 | 处理 | 替代 |
|---|---|---|
| `lottery_pool()` | **删除** | `exchange_pool()`（不再过滤 owner）|
| `roll_special()` | **删除** | `redeem(player, spec_id)`（确定性）|
| `import random` | 删除 | — |

**新增 7 个函数**：`exchange_pool()` / `token_count()` / `grant_token()` /
`owns_special()` / `can_redeem()` / `redeem()` / `redeem_preview()`。

**`/特殊 认主` 加校验**：必须**已兑换过**才能认主。
判据用 `player.special_owned`（**只增不减**的永久流水）而不是 `player.special` ——
后者会被 `/特殊 卸下` 清空，用它会导致「卸下后就再也装不回去」，
或者更糟：**卸下后再兑换被白扣 100 枚**。

**扣款顺序**：先校验 → 再扣令 → 最后写归属。
任何一步失败都不会出现「令扣了但装备没到手」。

### 17.4 ⑥ 帝兵去唯一化的数值连带（本节最重要）

**冲突在哪**：帝兵是**独立乘区**，叠乘在 5 件普通装备之上。

```
最终面板 = 裸装基础 × (1 + 装备%) × (1 + 帝兵%)
```

| 项 | 原值 | 现值 | 理由 |
|---|---|---|---|
| `base_pct` | 62.0 | **20.0** | 独立乘区 +20%，量级合理 |
| 成长 `pct` | 1.0 | **0.1** | 每击杀 +0.1%，约 300 只怪满 |
| 成长 `cap_pct` | 150.0 | **30.0** | 封顶 +30% |
| **满成长合计** | **+212%（×3.12）** | **+50%（×1.50）** | — |
| `unique` | `True` | **`False`** | 不再全服唯一 |
| `source` | `"lottery"` | **`"token_exchange"`** | 来源改为兑换 |
| `owner` | `""` | `""`（**停用**） | 字段保留，展示层不再读 |

**基准对齐**（用「九品满装」当尺子）：

| 套系 | 九品 5 件累加 atk_pct | 叠乘后 |
|---|---|---|
| 破锋（物穿） | +22.37% | ×1.224 |
| 通玄（法穿） | +21.76% | ×1.218 |
| 镇岳（肉盾） | +17.87% | ×1.179 |

```
帝兵满成长 ×1.50  ÷  一整套九品 ×1.22  ≈  1.23
```

→ **一件帝兵 ≈ 1.2 套九品装备的量级。** 作为「100 天签到」的长期目标
是合理的：明显强于任何单件/单套，但不至于让装备体系失去意义。

**成长速率为什么从 +1% 砍到 +0.1%**：原值在「唯一」时代是「大佬的勋章」
（一辈子也满不了）。全民化之后按挂机击杀速度**几天就满了** ——
那「成长型被动」这个卖点就变成一次性的了。改成 0.1%/30%
→ 需约 300 只怪，活跃玩家 1~2 周满，是个能看见的成长曲线。

> **核心提醒**：「帝兵不唯一」和「帝兵 +212%」不能同时成立 ——
> 去唯一化必须带数值重估，否则整套装备体系会被一件人人可得的装备压平。
> 这是设计上的**硬冲突**，不是「调不调」的选择题。

### 17.5 ⑤ 七品以上下架坊市（顺手修一个静默 bug）

`shop_stock()` 原实现：

```python
cap = t * 2 + 6
if int(eq.get("atk", 0)) <= cap:      # ← 恒为真
```

v3.4 把装备的 `atk`（绝对值）改成 `atk_pct`（百分比）之后，
**`eq.get("atk", 0)` 恒为 0**，`0 <= cap` 恒真 ——
坊市退化成「**135 件全都能买**」，连九品也不例外。

**所以「九品也能直接买」不是设计，是个没人发现的 bug。**
（老板直觉觉得不对，直觉是对的。）

**修法**：`SHOP_MAX_TIER_RANK = 6` + 两条过滤（品阶 + `req_turns`）。
条件二用 `req_turns` 而不是攻击值 —— 它直接对齐「玩家能穿得上」这个语义，
不会出现「买回来发现装不了」。（和 `explore_equipment_pool` 同一口径。）

**下架之后七/八/九品从哪来 —— 秘境里程碑**：

```python
DUNGEON_TIER_MILESTONES = {30: 7, 35: 8, 40: 9}
```

- **按品阶指定**，不是全池随机 —— 里程碑的意义就是「爬到这一层，
  确定拿到一件顶级装备」。全池随机的话玩家爬到 40 层拿到一品剑，会骂人的。
- **部位随机、套系随机** —— 让玩家有「再来一件凑套」的动力，而不是一次毕业。
- **只在首通给**，重刷拿不到，避免「反复刷同一层刷满套装」。

### 17.6 ④ 套系命名：破锋 / 通玄 / 镇岳

**老板否掉了设计师的 A 方案**，理由很准：

> 「套系我觉得不应该用这个名字，这个名字应该是职业名字，
> 让玩家创建角色时选择职业。」

所以「剑修 / 法修 / 体修」**留给未来的职业系统**（角色身份），
套系另起一组（装备流派）：

```
内部 id（存档用，永不改）        展示名（玩家看到）
   "物穿"          ──────►        破锋   （破敌之锋）
   "法穿"          ──────►        通玄   （通达玄妙）
   "肉盾"          ──────►        镇岳   （镇压山岳）
```

**⚠️ `set_id` 一个字都不能改。** 存档里装备是按 `set_id` 匹配套系的
（`Player.active_set()`），改了 = 线上所有玩家的套装当场失效。

**落地**：`items.SET_DISPLAY_NAMES` + `set_display_name()`，
两个展示出口改走映射：

| 文件 | 位置 |
|---|---|
| `battle.py` | `BattleResult.mech_summary()` |
| `main.py` | `_equip_bonus_text()` |

`battle.py` 里用**延迟导入** —— `items` 反过来要读 `battle` 的常量
（破防系数、暴击率），顶层 import 会成环。

### 17.7 VIP 赞助者系统（`vip.py`）

**两条硬约束**：

1. **VIP 只能由主人（天道）发放。** 没有自助购买、没有卡密、
   没有消费自动升级。发放权只在老板手上。
2. **VIP 不碰战斗数值。** 只加速「攒天道令 → 换帝兵」：**给时间，不给战力。**

**为什么 VIP 数据必须单独存 `vip.json`**：游戏存档按群隔离是对的
（不同群各自玩），但赞助是**人**的属性 —— 写进群存档会导致
**「A 群充了 VIP，去 B 群就没权益了」**。所以按 **QQ 号**全局存一份。

```
data/plugin_data/astrbot_plugin_textrpg/
├── players.json      ← 群隔离的游戏存档
└── vip.json          ← 全局 VIP 名单（按 QQ 号）
```

**加成跟着签到发，不是每天自动发**：签到本来就有「一天一次」的保护
（`sign_in_date` 日期字符串，跨零点重置），VIP 加成挂进来就
**自动继承**这层防刷 —— 一行防刷代码都不用新写。

对比「每天首次任意指令时发」：要在 30+ 个指令入口都判断一次
「今天发过没」，改动面大，而且容易被玩家用 `/属性` 之类无成本指令刷。

**代价**（要对玩家说清）：VIP 玩家某天不签到，当天加成拿不到。
可接受 —— 天道令本来就只有「签到 + 秘境首通」两个来源，
VIP 的定位是加速签到，不是躺着收钱。

**续费不能简单用 `now + months`**：

```python
base_ts = max(now, rec.get("expire_ts", 0))
new_expire = base_ts + months * 30 * 86400
```

否则提前续费会**吃掉剩余天数** —— 这是最容易引起投诉的低级 bug。

**过期不删数据**：`vip_tier()` 对已过期返回空串，但记录保留。
续费时能延续 `since_ts` / `total_months`，老板也能查历史收入。

**升级不折算剩余天数**：差价只有 20 元，折算规则复杂且玩家看不懂，
容易吵架。直接覆盖 tier，`expire_ts` 按上面的规则累加。

**指令**：

| 指令 | 权限 | 作用 |
|---|---|---|
| `/激活vip <QQ>` | 仅天道 | 发普通 VIP（10 元/月，默认 1 个月） |
| `/至尊vip <QQ>` | 仅天道 | 发至尊 VIP（30 元/月，默认 1 个月） |
| `/vip 开通 <QQ> <月数> [normal\|supreme] [备注]` | 仅天道 | 多月份 / 要写备注时用 |
| `/vip` `/vip 查询 <QQ>` | 所有人 | 查状态（含已过期的历史，提示续费）|
| `/vip 撤销 <QQ>` | 仅天道 | 立即失效 |
| `/vip 列表` | 仅天道 | 列有效 VIP + 剩余天数 |

**为什么主入口是「精确短指令」而不是 `/vip 开通` 一堆参数**：
发 VIP 是个**低频、单人操作**的动作（老板自己发），
每次都敲 `/vip 开通 12345678 1 supreme 微信0919` 是纯负担。
拆成 `/激活vip <QQ>` / `/至尊vip <QQ>` 后，一条指令自带全部语义，
剩下两个参数（月数、备注）只对少数场景有意义，降级到 `/vip 开通`。

**为什么三条入口必须共用 `_vip_grant_flow()`**：
`/激活vip`、`/至尊vip`、`/vip 开通` 做的是同一件事
（校验权限 → 发放 → 公告），只是参数来源不同。
抽成一条流程后**权限判断只有一处** ——
不会出现「新开了个入口却忘了校验权限」这种白送 VIP 的漏洞。
自测里有结构性断言钉住这条（三条入口都必须委托给它）。

**为什么 `/激活vip` 不会和已有的 `/激活` 撞车**：
查过 `astrbot/core/star/filter/command.py:202`，匹配条件是

```python
message_str.startswith(f"{full_cmd} ") or message_str == full_cmd
```

即**命令名后必须跟空格，或整串完全相等**。
`/激活vip 123` 既不等于 `激活`，也不以 `激活 ` 开头 —— 不会误触。
这条必须查源码，不能凭感觉：若框架用裸 `startswith(cmd)`，
`/激活vip 123` 会被 `/激活` 吃掉，参数变成 `"vip 123"`，
玩家的 QQ 号就**静默发不出去**了。

**斜杠能不能省**：能，但那是 AstrBot 的全局机制，不是插件的。
插件侧注册的名字**本来就不含斜杠** —— `wake_prefix`（默认 `["/"]`）
在匹配前被剥掉（`core/pipeline/waking_check/stage.py:130`）。
所以群里发 `/激活vip 123`，或 `@机器人 激活vip 123`；
私聊 / 被 @ 时 `激活vip 123` 也可以。
但**群里不 @ 机器人直接发「激活vip 123」不会触发** ——
AstrBot 要先确认这条消息是在跟机器人说话，平台层规则，插件改不了。

**权限为什么用方法内显式判断，而不是 `filter.permission_type(ADMIN)`**：
「查询」要对所有人开放（玩家要能看自己还剩几天），
而「开通/撤销/列表」只给天道 —— 一个方法只能挂一种框架权限。

副作用是好的：显式判断**可测**。框架层拦截在单测里是桩，
拦不住任何东西，而「非管理员不能发放 VIP」是一条**安全属性**，
必须能被断言钉住。

**发放包装成世界观动作「天道垂青」**，而不是「已开通 VIP」：

```
⚜️ 天道垂青
　道友「xxx」以诚心供奉，自今日起受天道眷顾。
　尊号：至尊赞助者
　每日签到额外得天道令 3 枚。
　有效期至 2026-10-19。
```

付费玩家买到的社交价值翻倍（全群都看见），免费玩家看到的是
「有人受天道眷顾」而不是「有人充钱了」——
前者是世界观，后者是商业化，观感差别很大。

**明确不做的权益**：加攻击 / 加防御 / 加掉率、减冷却、扩容背包、
帝兵专属或优先兑换、「永久 VIP」。
理由：一旦 VIP 能加战力，免费玩家就变成「陪玩」——
10 元/月的小社区经不起这个。

**风险说在明处**：

| 风险 | 对策 |
|---|---|
| 收了钱就有责任 | 开通时说明「赞助性质，非购买服务」；**不承诺永久** |
| 定价过低难回本 | 1核1G 一年约 100~300 元；10 元 × 10 人 = 100 元/月，够 |
| 免费玩家流失 | 本方案已限制：**只加速时间，不给战力** |
| 开通出错无法追溯 | `note` 字段 + `/vip 列表` 随时可查 |

### 17.8 新增/变更字段（存档）

| 文件 | 字段 | 说明 |
|---|---|---|
| `player.py` | `sign_in_date: str` | 上次签到的本地日期字符串（跨零点重置）|
| `player.py` | `special_owned: list[str]` | 曾兑换过的特殊装备 id（**只增不减**）|
| `vip.json` | `tier` / `expire_ts` / `since_ts` / `total_months` / `note` | 全局 VIP 名单 |

`special_owned` 的 `from_dict` 兜底**不能只做「无则置空」** ——
要兼容三种脏数据：缺失 / 存成了字符串 / 列表里混进非字符串。
用 `dict.fromkeys` 去重保序。

`vip.json` 不存在或损坏时**降级为空名单**，不影响插件启动。

### 17.9 回归防线

- 自测断言 **697 → 793**，连跑 3 次全绿。
- 新增端到端脚本 `verify_v35_token.py`（57 项），走真实指令入口：
  签到 → 攒令 → 兑换 → 认主 → 拦截 → 里程碑 → 重刷不发令。
- 线上目录同样 793 + 57 全绿，31 个文件逐字节一致。
- **踩到一个测试陷阱**：用 5 转角色跑**真实战斗**验「首通发令」，
  运气差时第 1 层就败 → `first_clears = 0` → 断言随机失败（3 次挂 1 次）。
  测「发令口径」不该依赖数值平衡，改成「必胜 + 压层数上限」的确定性构造。

### 17.10 仍未决

**待老板**：

| # | 问题 | 现状 |
|---|---|---|
| 1 | 秘境天道令判定口径 | 已按 A（严格首通）实现。若觉得后期太慢，改「每日通关」是一行代码 |
| 2 | 帝兵数值下调幅度 | 已按 A（base 20% + 成长 30% = ×1.50）实现 |
| 3 | 至尊 +3 是否太多 | 保持 +3（30 元是 10 元的 3 倍价格，权益也 3 倍，定价结构自洽）|
| 4 | 未来职业系统 | 「剑修 / 法修 / 体修」这组名字已为它预留 |

**上线动作**：需要**重启 AstrBot** 才加载新代码。

*V3.5 天道令兑换 · 帝兵去唯一化 · VIP 赞助 · 2026-09-19 · 小布*
