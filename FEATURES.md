# 功能汇总

> 修仙文字 RPG 插件 —— 一份给人看的清单

## 一、快速上手（三步）

```
1. 在 WebUI 插件配置里填「主人 QQ 列表」= 你的 QQ 号
2. 把机器人拉进你要玩的群，在群里发：/激活
3. 群里每个人发：/报名
```

第 1 步不做，后面两步都做不了 —— 因为激活和重置只有主人能操作。

---

## 二、指令总表（44 条，含别名）

指令支持多别名，同一功能用老名或新名都能触发。

### 管理类（仅主人）

| 指令 | 作用 | 备注 |
|---|---|---|
| `/激活` | 在当前群启用游戏 | 必须在群里发。加群后第一件事 |
| `/取消激活` | 关闭当前群的游戏 | **存档保留**，重新激活后进度还在 |
| `/激活列表` | 查看哪些群已激活 | 建议私聊发，不刷屏 |
| `/重置` | 清空存档 | 群里=清本群；私聊=只清自己的私聊存档 |
| `/激活vip <QQ>` | 发普通 VIP（10 元/月，默认 1 个月） | **仅天道**。精确短指令，一步到位 |
| `/至尊vip <QQ>` | 发至尊 VIP（30 元/月，默认 1 个月） | **仅天道**。精确短指令，一步到位 |
| `/vip 开通 <QQ> <月数> [normal\|supreme] [备注]` | 发放 / 续费赞助 | **仅天道**。只在「多月份」或「要写备注」时用 |
| `/vip 撤销 <QQ>` | 立即失效 | **仅天道**。退款 / 误操作用 |
| `/vip 列表` | 列有效赞助者 | **仅天道**。含剩余天数与备注 |

> `/重置` 在私聊里**不会**清空所有群的数据。早期版本会，太危险，已改掉。

### 角色与查看

| 指令 | 别名 | 作用 | 备注 |
|---|---|---|---|
| `/报名` | — | 创建角色 | 三步捏脸：起名 → 形象 → 确认 |
| `/改名 <新名>` | — | 改角色名 | — |
| `/菜单` | `/帮助` `/指令` `/help` | **指令总览** | 6 组分类；顶部按你的状态给「下一步做什么」 |
| `/简介` | `/玩法` `/介绍` | **玩法总览** | 7 个板块：怎么变强 / 修为来源 / 装备三套 / 秘典 / 秘境 / 战斗结果 |
| `/属性` | `/面板` `/属性面板` | 道途面板 | 境界、修为、乘区拆解、秘典、战绩、【特殊】栏、VIP 称号 |
| `/背包` | `/储物袋` | 持有物 | 按法宝类别与丹药分类 |
| `/排行榜` | — | 本群境界榜 | 只显示本群 |
| `/vip` | `/赞助` | 价目表 + 自己是否激活 | 所有人可用；`/vip 查询 <QQ>` 查别人 |

### 战斗

| 指令 | 别名 | 作用 | 备注 |
|---|---|---|---|
| `/历练` | `/打怪` | 随机遭遇妖兽 | 5 分钟冷却。**修为只给 15%**，主要产出物品与灵石 |
| `/寻敌 <妖兽名>` | `/挑战` | 指定妖兽 | 最多越 9 转 |
| `/秘境 [层数]` | `/闯关` | 每日爬塔副本 | 连闯直到失败，失败**不计死亡**。修为大头 |
| `/恢复 [1\|3]` | `/复活` | 灵石疗伤 | 1=少量，3=全愈 |

战斗结果文案：碾压 / 胜利 / 险胜 / 势均力敌 / **道消**

> **打怪不再是大头。** v3.1 起历练的修为压到 15%（`EXP_SCALE_FIELD`），
> 定位改为**物品与灵石来源**。修为主要靠 `/修炼` `/闭关` `/探索` 三条挂机线，
> 以及每日一次的 `/秘境`。

### 妖兽（v3.2 重做）

**72 只，覆盖 Lv1~72 全部转数**，每个境界 9 只。

| 特性 | 说明 |
|---|---|
| 属性来源 | 由「玩家在该 Lv 的面板」**反推**，不独立设计 |
| 全境界手感 | **5 回合击杀 / 19 回合可扛**（8 个境界完全一致） |
| 抽取方式 | **境界分池**：本境 70% / 低一境 20% / 高一境 10% |
| 池子规模 | 恒为 27 只左右、跨 3 境，不再出现「72 转抽到史莱姆」 |
| 三档位 | 普通 / 精英（妖王），难度靠血厚而非秒人 |
| 回合上限 | **30**（v3.3 由 10 抬升），妖王成为「高消耗磨血战」而非打不完判平 |
| 新手保护 | 9 转前怪 Lv 不超过转数 +3 |

修为占比全境界锁定 **15%**（基准 exp 4~910），
保证「一次打怪 = 0.20 次修炼」在任何境界都成立。

### 修为

| 指令 | 别名 | 作用 | 备注 |
|---|---|---|---|
| `/修炼` | — | 主动打坐加修为 | 5 分钟冷却，小收益 |
| `/闭关` | — | 挂机修行 | 最长 8 小时，结算后 1 小时冷却，有走火入魔风险 |
| `/探索` | — | 游历挂机 | 30 分钟~8 小时，产修为+物品+**秘典残页** |
| `/突破` | — | 突破境界 / 渡天劫 | 九转圆满时可用 |

### 装备与物品

| 指令 | 别名 | 作用 | 备注 |
|---|---|---|---|
| `/装备 <物品名>` | `/祭炼` | 披挂法宝 | 同部位自动换下；**可只写基础名**（`青锋剑`），自动匹配品质最好的那件 |
| `/卸下 <部位>` | — | 卸下 | 法宝 / 护身宝衣 / 灵饰 / 灵靴 / 法印 |
| `/特殊 [动作]` | — | **【特殊】栏（帝兵）** | 无参数=查看；`认主 <id>`=穿戴（**需已兑换**）；`卸下`；`列表`=兑换进度 |
| `/签到` | `/打卡` | **每日领 1 枚天道令** | 跨零点重置；VIP 另有额外加成 |
| `/兑换 <帝兵id>` | — | **100 枚天道令换帝兵** | 兑换后直接认主；不可重复兑换 |
| `/使用 <物品名>` | — | 使用丹药 | 同样支持只写基础名 |
| `/出售 <物品名>` | — | 换灵石 | 已披挂的不能卖；同名多品质时**先卖品质最低的** |
| `/坊市` | `/商店` | 查看货架 | **七品以上已下架**，只能打秘境拿 |
| `/购买 <物品名>` | — | 买入 | 买价 = 售价 × 3 |

### 大道秘典

| 指令 | 别名 | 作用 | 备注 |
|---|---|---|---|
| `/大道` | — | 查看秘典面板 | 已习得阶数、残页存量、装配位 |
| `/修行 <秘典名>` | — | 提升阶数 | 消耗残页 + 修为 |
| `/装配 <秘典名>` | — | 装配 / 卸下 | 装配位有限，大乘期最多 8 个 |

---

## 三、境界体系（八境 × 九转 = 72 转）

| 境界 | 转数 | 倍率 | 修为门槛 |
|---|---|---|---|
| 炼气期 | 1 ~ 9 | ×1.00 | 0 |
| 筑基期 | 10 ~ 18 | ×1.25 | 2,000 |
| 金丹期 | 19 ~ 27 | ×1.50 | 8,000 |
| 元婴期 | 28 ~ 36 | ×1.80 | 25,000 |
| 化神期 | 37 ~ 45 | ×2.20 | 70,000 |
| 炼虚期 | 46 ~ 54 | ×2.70 | 180,000 |
| 合体期 | 55 ~ 63 | ×3.30 | 450,000 |
| 大乘期 | 64 ~ 72 | ×4.00 | 1,100,000 |

**飞升**：大乘九转圆满 + 累计修为 250 万 → 渡天劫 → 转生。

---

## 四、属性数据流（最关键的设计）

```
面板值 = 基础属性 × 总乘区 × (1 + 装备%) × (1 + 特殊装备%)

总乘区 = 境界倍率 × 1.05^转生次数 × 1.20^突破次数
```

| 层级 | 内容 | 作用 |
|---|---|---|
| 基础属性 | 只跟转数有关 | 唯一计算源头 |
| 乘区 | 境界 / 转生 / 突破 | 境界类百分比汇此处 |
| 装备% | 5 件法宝的百分比之和 | 独立一层乘区（v3.4 改） |
| 特殊装备% | 帝兵的百分比 | 叠乘在装备之上（v3.4 新增） |

**可扩展性**：加任何新系统（新功法、新丹药、新天赋）都是
「插一个新乘区」，不改战斗代码。

> **v3.4 为什么把装备从「加法」改成「百分比」**
>
> 旧公式 `基础 × 乘区 + 装备绝对值` 有结构性缺陷：
> 乘区跨境翻倍（×2），装备却是固定值 ——
>
> | | 炼气期 | 大乘期 |
> |---|---|---|
> | 顶级 5 件装备的相对增益 | **+722%** | **+5%** |
>
> 越后期越没用，装备在后期形同虚设。改百分比后装备跟着面板涨，
> 任何境界都保持同一份相对增益。

---

## 四·五、十品装备体系（v3.4）

**一品 ~ 九品 + 极品帝兵**，共 **135 件**，按「品阶 × 三套系 × 五部位」组织。

### 品阶骨架与门槛

| 品阶 | 档位 | 倍率骨架 | 转数门槛 |
|---|---|---|---|
| 一品 / 二品 / 三品 | 下品 | 每级 ×2 | 1 / 10 / 19 |
| 四品 / 五品 / 六品 | 中品 | 每级 ×2.5 | 28 / 37 / 46 |
| 七品 / 八品 / 九品 | 上品 | 每级 ×3 | 55 / 64 / 68 |
| 帝兵 | 特殊 | 全服定制 | **无门槛** |

门槛不足时 `/装备` 会拦下并告诉你「还需几转」。

### 三套系（机制分层，不是一味乘算）

| 套系 | 定位 | 机制 |
|---|---|---|
| 物穿 | 破防输出 | 穿甲 + 概率无视防御（追加伤害）+ 暴击 |
| 法穿 | 术法输出 | 穿甲 ×1.15 + 术法增伤 + 冷却缩减 |
| 肉盾 | 生存 | 减伤 + 反伤 + 低血线额外减免（上品专属）|

**必须穿满同一套系的 5 件**才触发套装效果，3+2 混搭不触发任何机制。

机制实现走**注册表**（`ATTACK_MODIFIERS` / `DEFENSE_MODIFIERS` +
`@register_attack_modifier` / `@register_defense_modifier`）：
主循环只查表遍历，不写 if-else 链，加新机制只加一个函数。

### 品质

装备名带品质后缀（`青锋剑[稀]`），品质倍率作用在**百分比**上：

| 品质 | 标记 | 倍率 | 掉落率 |
|---|---|---|---|
| common | 普 | ×1.0 | 60% |
| fine | 精 | ×1.3 | 25% |
| rare | 稀 | ×1.7 | 10% |
| epic | 史 | ×2.2 | 4% |
| legend | 传 | ×3.0 | 1% |

**名字解析**：玩家只需写基础名（`/装备 青锋剑`），
系统自动匹配储物袋里品质最好的那件。
`/出售` 相反 —— 同名多品质时先卖品质**最低**的。

---

## 四·六、帝兵（特殊装备）

| 特性 | 说明 |
|---|---|
| 栏位 | **不占 5 个普通部位**，独立【特殊】栏；没有帝兵时不显示这一栏 |
| 门槛 | **无阶位限制** —— 1 转就能带（核心价值：越早拿越强）|
| 加成 | 基础 **+20%** 全属性，**叠乘**在 5 件普通装备之上 |
| 成长 | 每击杀一只妖兽 **+0.1% 全属性**，封顶 **+30%**（满成长合计 +50% → 面板 ×1.50）|
| 死亡 | **不清零**（卸下也保留进度，重新认主继续累积）|
| 获取 | **100 枚天道令兑换**（v3.5 起废弃天道抽奖）|
| 唯一性 | **已去唯一化** —— 人人可得，不搞全服限定 |
| 认主前提 | 必须先兑换过（`special_owned` 永久流水校验），否则被拦 |

指令：`/特殊` 查看 · `/兑换 <id>` 兑换 · `/特殊 认主 <id>` 穿戴 ·
`/特殊 卸下` · `/特殊 列表`（=兑换进度面板）

**可扩展性**：帝兵数量**不写死** —— 加一件就是往
`items.SPECIAL_EQUIPMENTS` 加一条记录，逻辑层不用改。
成长被动走 `PASSIVE_HANDLERS` 注册表，「怎么涨、涨到哪、死不死」
全部由数据表 `passives` 字段决定。

> **v3.5 为什么数值下调这么多（62%→20% / 1%→0.1% / 150%→30%）**
> 原数值是**为「全服唯一」设计的** —— 一个人独享 +212% 无所谓，
> 那正是唯一物的价值来源。
> 但帝兵是**独立乘区**（叠乘在 5 件装备之上），一旦人人可得，
> +212% 就意味着所有玩家面板 ×3.12 ——
> 135 件装备、三套流派、九个品阶的梯度**全部失去意义**。
> 「去唯一化」和「原数值」二者只能留一个，所以必须一起改。

---

## 四·七、天道令与兑换（v3.5）

**帝兵的唯一凭证**。攒够 100 枚换一件。

| 特性 | 说明 |
|---|---|
| 道具 | `effect: "token"` —— 刻意这么标，带来两条硬约束 |
| 不能被 `/使用` | 不拦的话玩家会以为它是丹药 |
| 不能被 `/出售` | 100 枚 = 一件帝兵，误卖一枚就是白攒一天（`price: 0` 是第二道保险）|
| 产出 1 | 每日 `/签到` +1 枚（存档字段 `sign_in_date`，**跨零点重置**）|
| 产出 2 | 秘境**首通** +1 枚（**按整趟发，不是按层**）|
| 背包满 | **强制入包** —— 进度型道具不能因背包满静默丢 |

**为什么按整趟发**：每首通一层给 1 枚的话，玩家从第 5 层开打、
连破 20 层 → 一次拿 20 枚，5 天凑齐帝兵，节奏全崩。
按趟发则一天最多 1 枚。这个判定**天然防刷**：打不过去的人拿不到，
反复重刷老层也拿不到。

**兑换流程**（`/兑换 <id>`）：

```
1. 查 id 是否存在              → 不存在：提示
2. 查是否已经兑换过            → 已兑换：拒绝（防重复扣）
3. 查天道令够不够 100 枚       → 不够：提示还差几枚
4. 扣 100 枚天道令
5. 记入 special_owned + 直接认主
```

**第 2 步不能省**：`/特殊 卸下` 会清空 `player.special`，
如果只检查它，玩家卸下后再兑换就会被**白扣 100 枚**。
所以用 `special_owned`（**只增不减**的永久流水）。

**扣款顺序**：先校验 → 再扣令 → 最后写归属。
任何一步失败都不会出现「令扣了但装备没到手」。

**为什么废弃抽奖**：唯一物 + 概率制是结构死局 ——
概率太低几个月没人抽到，活动形同虚设；概率太高第一个人抽走后
池子空掉，**后面所有人抽了个寂寞**。而「今天 73/100」
是**确定的进度条**，比概率更有粘性。

---

## 四·八、VIP 赞助者（v3.5）

**两条硬约束**：只能由天道发放；不碰战斗数值。

**发放用「精确短指令」** —— 一条指令自带全部语义，不用敲一长串参数：

```
/激活vip 12345678      → 普通 VIP（10 元/月），默认 1 个月
/至尊vip 12345678      → 至尊 VIP（30 元/月），默认 1 个月
```

| 指令 | 权限 | 作用 |
|---|---|---|
| `/激活vip <QQ>` | 仅天道 | 发普通 VIP，默认 1 个月 |
| `/至尊vip <QQ>` | 仅天道 | 发至尊 VIP，默认 1 个月 |
| `/vip 开通 <QQ> <月数> [normal\|supreme] [备注]` | 仅天道 | 多月份 / 要写备注时用 |
| `/vip` | 所有人 | 价目表 + 自己是否激活 |
| `/vip 查询 <QQ>` | 所有人 | 查某人状态（含已过期历史） |
| `/vip 列表` | 仅天道 | 列有效赞助者 + 剩余天数 + 备注 |
| `/vip 撤销 <QQ>` | 仅天道 | 立即失效 |

`/vip` 一眼看到价目与自己的状态（三态）：

```
⚜️ VIP（赞助者）· 价目

　赞助者　10 元/月　每日签到额外 +1 枚天道令
　至尊赞助者　30 元/月　每日签到额外 +3 枚天道令

你的状态：✅ 已激活 · 赞助者　剩余 30 天（至 2026-10-19）
```

| 等级 | 定价 | 权益 | 凑齐 100 枚 |
|---|---|---|---|
| 普通 VIP | 10 元/月 | 每日签到额外 **+1** 枚天道令 | 33 ~ 50 天 |
| 至尊 VIP | 30 元/月 | 每日签到额外 **+3** 枚天道令 | 20 ~ 25 天 |
| （免费玩家） | — | — | 50 ~ 100 天 |

| 特性 | 说明 |
|---|---|
| 发放权 | **只在主人（天道）手上** —— 无自助购买、无卡密、无消费自动升级 |
| 存储 | `vip.json`，按 **QQ 号**索引，**全局**（不是群存档）|
| 跨群 | **有效** —— A 群赞助了，去 B 群照样有权益 |
| 加成发放 | **跟着签到走** —— 自动继承签到的「一天一次」防刷 |
| 续费 | `base_ts = max(now, expire_ts)`，**不吃掉剩余天数** |
| 过期 | **不删记录** —— 续费时延续累计月数，也方便对账 |
| 到期提醒 | 前 3 天在签到回复里提醒续费 |
| 面板称号 | 「赞助者」/「至尊赞助者」|
| 发放公告 | 包装成世界观动作「**天道垂青**」，而不是「已开通 VIP」|
| 权限入口 | 三条发放路径（`/激活vip` `/至尊vip` `/vip 开通`）共用一条流程，**权限判断只有一处** |

> **`/激活vip` 会不会被已有的 `/激活` 吞掉？不会。**
> AstrBot 的指令匹配要求「命令名后必须跟空格，或整串完全相等」
> （`core/star/filter/command.py:202`），
> `/激活vip 123` 既不等于 `激活` 也不以 `激活 ` 开头，不会误触。
> 这条查过源码 —— 若框架用裸 `startswith`，玩家的 QQ 号会被当成
> `/激活` 的参数，**静默发不出去**。自测里复刻了这条规则做回归保护。

> **斜杠可以省吗？看场景。** 插件注册的名字本来就不含斜杠，
> 斜杠是 AstrBot 的 `wake_prefix` 在匹配前剥掉的。
> 群里发 `/激活vip 123` 或 `@机器人 激活vip 123`；
> 私聊 / 被 @ 时 `激活vip 123` 也行。
> 但**群里不 @ 机器人直接发「激活vip 123」不会触发** ——
> 这是平台层的唤醒规则，插件改不了。

**明确不做的权益**：加攻击/防御/掉率、减冷却、扩容背包、
帝兵专属或优先兑换、「永久 VIP」。
理由：一旦 VIP 能加战力，免费玩家就变成「陪玩」——
10 元/月的小社区经不起这个。

**代价（要对玩家说清）**：VIP 玩家某天不签到，当天加成拿不到。
天道令本来就只有「签到 + 秘境首通」两个来源，VIP 的定位是加速签到。

---

## 五、天劫机制（抗住，不是打赢）

大乘九转圆满 + 修为足够后，`/突破` 第一次只提示，**再发一次才真的应劫**（防误触）。

```
9 道雷，首道 = 最大气血 × 8%，每道递增 8%
9 道累计伤害 ≈ 最大气血 × 100%
```

| 应劫时气血 | 抗住道数 | 结果 |
|---|---|---|
| 100%（满血） | 9 | **飞升** |
| 85% | 8 | 失败，掉 2 转，休整 10 分钟 |
| 70% | 7 | 失败，掉 2 转 |
| 50% | 5 | 失败，掉 2 转 |
| 30% | 3 | 失败，掉 3 转，休整 30 分钟 |

**不满血应劫几乎必失败** —— 这是设计上的核心约束，逼玩家把资源用在正确的地方。
**渡劫丹**每颗可多扛一道雷，是主要的补救手段。

**飞升保留**：灵石、储物袋、法宝、秘典、突破次数、全部统计
**飞升重置**：转数归 1、修为清零、气血回满
**飞升获得**：永久 ×1.05 基础属性（可叠乘）

---

## 六、大道秘典（首期 5 条）

从 `/探索` 掉落**残页**，用 `/修行` 参悟。每条 3 阶。

| 大道 | 描述 | 一阶（主动） | 二阶（被动） | 三阶（被动） |
|---|---|---|---|---|
| 火之秘典 | 掌控世间之火，以灼热焚尽万物。 | **火焰** 200% 攻击力火伤，CD 1 回合 | **控火** 受火伤 -25% | **燎原** 所有伤害附加 20% 攻击力火伤 |
| 水之秘典 | 上善若水，润物无声。 | 回春 | 润泽 | 潮涌 |
| 金之秘典 | 金者坚刚，千锤不折。 | 铁壁 | 锐金 | 不坏 |
| 土之秘典 | 厚德载物，镇岳不移。 | 落石 | 厚重 | 陷阵 |
| 风之秘典 | 风无常形，来去无踪。 | 疾风 | 轻身 | 追风 |

**修行消耗**：

| 阶数 | 残页 | 修为 |
|---|---|---|
| 一阶（学会） | 1 张 | 0 |
| 二阶 | 3 张 | 2,000 |
| 三阶 | 6 张 | 8,000 |

**装配位**：炼气期 1 个 → 每突破大境界 +1 → 大乘期 8 个。
未装配的秘典**被动不生效**。

**可扩展性设计**（重要）：
秘典在 `scriptures.py` 只做**声明**，例如：

```python
1: {"name": "火焰", "type": "active", "cooldown": 1,
    "effect": {"kind": "magic_damage", "mult": 2.0, "element": "fire"}}
```

战斗引擎靠 `EFFECT_HANDLERS` 注册表**查表分派**，不写 if-else 链。
现有 8 种 kind：`magic_damage` / `heal` / `buff` / `resist` /
`add_damage` / `counter` / `dodge` / `dot`。
加新效果只需 `@register_effect("新kind")` + 在秘典表写一行。

---

## 七、秘境副本（每日一次）

`/秘境`（别名 `/闯关`）。**每日一次**，从历史最高层的倒数第 3 层开打，
自动连闯直到失败。

| 特性 | 设计 |
|---|---|
| 每日限制 | 是，按本地日期记 `dungeon_date` |
| 起始层 | `max(1, 历史最高 - 2)` —— 跳过已碾压的层 |
| 每层状态 | **满血开打** |
| 失败后果 | **不计死亡、不掉转、不进休整** |
| 收益分档 | 首通层全额；重刷层（本次开始前的纪录以内）只给 1/4，且不给物品 |

**每层满血开打**是刻意的：否则前面几层的消耗会让玩家被「累积伤害」磨死，
那是折磨不是难度。

### 难度曲线（固定锚点，与玩家转数无关）

锚点取 **1 转裸装属性**，之后按层数复利：

```
气血 = 锚点血 × 0.70 × 1.13^(n-1)
攻击 = 锚点攻 × 1.20 × 1.13^(n-1)
防御 = 锚点防 × 0.40 × 1.13^(n-1)
```

**锚点为什么取 1 转而不是 72 转**（实测迭代三次）：

| 锚点 | 结果 |
|---|---|
| 72 转 | 1 转玩家连第 1 层都打不过，副本对新人是死内容 |
| 3 转 | 1 转玩家仍然 0% —— 0.7×3转血已超出新人输出 |
| **1 转** | 1 转能过 2 层，72 转能过 46 层，曲线完全连续 ✓ |

实测可达层数（400 场采样）：

| 转数 | 1 | 3 | 5 | 10 | 15 | 20 | 30 | 40 | 60 | 72 |
|---|---|---|---|---|---|---|---|---|---|---|
| 可达层 | L2 | L5 | L10 | L20 | L25 | L25 | L30+ | L30+ | L30+ | **L46** |

天花板是复利自己形成的（每层 ×1.13），不需要额外写死上限。
另有一道**兜底保护**：怪血不超过玩家气血的 5 倍，
只在层数极高时触发，防止出现「血厚到打不死」的纯平局层。

---

## 八、战力

```
战力 = 攻击 × 2.0 + 防御 × 1.5 + 气血 × 0.5
```

在 `/属性` 面板展示。实测裸装战力：1 转 **77** → 72 转 **16356**（212 倍）。

**战力只用于展示，不参与战斗计算** —— 刻意如此，
否则它会变成第二个难度旋钮，和妖兽缩放互相打架。

---

## 九、装备部位与穿甲（v3.4 已重做）

> ⚠️ 本节原描述的是 v3.3 的「42 件装备 + 每件穿甲 1% 封顶 5%」，
> **该体系已被十品装备体系取代**（见「四·五」）。
> 这里只保留仍然成立的部分。

**5 个部位**（不变）：

| 部位 | key | 风格 |
|---|---|---|
| 法宝 | `weapon` | 攻击为主 |
| 护身宝衣 | `armor` | 防御为主 |
| 灵饰 | `accessory` | 均衡 |
| 灵靴 | `boots` | 偏机动，攻击 + 小量气血，防御薄 |
| 法印 | `talisman` | 偏厚重，防御 + 厚血，攻击低 |

**穿甲（v3.4 改为按套系给）**：

| 情况 | 穿甲值 |
|---|---|
| 穿满某套系 5 件 | 取该品阶的套装穿甲值（一品 10% ~ 九品 45%）|
| 没穿满 | 退回旧规则（每件 +1%，封顶 5%）|
| 法穿套 | 在上述基础上 **×1.15** |

> **为什么必须改**：老板定的「五件一阶物穿套 +10 穿甲」在旧上限 5% 下
> 根本装不下。改成按套给之后，穿甲成了**套装的核心卖点**，
> 也才配得上「物穿套」这个定位。

- 同部位塞两件**骗不过**套装判定（每个部位最多计 1 件）。
- 穿甲的作用方式：`有效防御 = 目标防御 × (1 - 穿甲)`，
  然后 `伤害 = 攻击 × U(0.9,1.1) - 有效防御 × 0.5`。
- **只在玩家打怪这一侧生效**，怪反击玩家仍走原公式 ——
  两个入口分开，语义清晰。

---

## 十、三种挂机方式对比

| | `/修炼` | `/闭关` | `/探索` |
|---|---|---|---|
| 冷却 | 5 分钟 | 结算后 1 小时 | 结算后 30 分钟 |
| 时长 | 立即结算 | 最长 8 小时 | 30 分钟 ~ 8 小时 |
| 修为 | 小（一次性） | **大**（约修炼 3 倍/小时） | 中（约闭关 1/3） |
| 额外产出 | 无 | 无 | 物品、灵石、**秘典残页** |
| 风险 | 无 | **走火入魔** | 无 |

**走火入魔概率**（只减收益，不扣任何资源）：

| 闭关时长 | 概率 | 后果 |
|---|---|---|
| < 2 小时 | 0% | — |
| 2 ~ 4 小时 | 5% | 收益减半 |
| 4 ~ 8 小时 | 12% | 收益减半 |
| ≥ 8 小时 | 20% | 收益减半 |

**探索产出权重**：修为 35 / 丹药 30 / 法宝 17 / **秘典残页 8** / 灵石 8 / 特殊 2

---

## 十一、数据存储

存档位置：`data/plugin_data/astrbot_plugin_textrpg/`

| 文件 | 内容 | 索引 |
|---|---|---|
| `players.json` | 游戏存档 | `群号:QQ号`（**按群隔离**）|
| `vip.json` | 赞助者名单 | **QQ 号**（**全局，跨群有效**）|

**按群隔离**：存档键是 `群号:QQ号`，同人在不同群各有独立进度。

> **为什么 VIP 要单独一个文件**：游戏进度按群隔离是对的（不同群各自玩），
> 但赞助是**人**的属性 —— 写进群存档会导致
> 「A 群充了 VIP，去 B 群就没权益了」。所以按 QQ 号全局存一份。

- 写入用「临时文件 + rename」，不会出现写一半的坏存档
- 写入前自动备份到 `players.json.bak`
- 多人同时操作由异步锁串行化
- 存档损坏时自动从备份恢复，再不行降级为空存档（不崩插件）

**旧存档自动迁移**（都在 `Player.from_dict` 里做，读档即生效）：

- 纯 QQ 号为键的老格式 → 归入私聊池
- v2 的 `level`（上限 200）→ 钳到 72 转
- 旧物品名 → `ITEM_ALIASES` 映射（`小型生命药水` → `小回气丹`）。
  新旧名同时存在时**数量相加**，不覆盖；迁移**幂等**，重复读档不变
- 旧部位名 → `SLOT_ALIASES` 映射（中文 `武器` → 英文 key `weapon`）

> 迁移必须发生在**存档层**，不能只在查表时兜底：
> 早先 `resolve_alias` 只在 `get_item` / `buy_price` 里被调用，
> 于是 `/使用 小型生命药水` 能查到定义（靠 `get_item` 兜底），
> 但 `/背包` 显示的仍是 v2 的旧名，和 `/坊市` 里的新名对不上。
> 现在 `from_dict` 会真正改写名字。

---

## 十二、自测

```bash
python selftest.py                       # 包内直接跑（827 项断言）
python -m astrbot_plugin_textrpg.selftest  # 模块方式跑（两种都支持）
python verify_v34_special.py             # 帝兵端到端（15 项，走真实指令流程）
python verify_v35_token.py               # 天道令/兑换/里程碑端到端（57 项）
```

不需要 AstrBot 环境，注入假 API 跑完整流程，**827 项断言**。

> selftest 钉**不变式**（快、粒度细）；`verify_*.py` 钉**链路**
> （走真实指令入口，覆盖跨模块协作）。两者分工互补。

覆盖：捏脸流程、战斗健康度、精进链路、法宝祭炼、境界映射、属性乘区、
天劫判定、探索产出、走火入魔概率、秘典修行与装配、旧存档迁移、
指令别名、群隔离、访问控制、防封节流、战力公式、妖兽缩放三态、
副本曲线与可达层数、每日限制、失败不计死亡、历练修为折扣、
**十品装备结构约束、三套系机制、穿甲/减伤表单调性、品阶门槛正反用例、
帝兵系统层（成长累积/封顶/不掉/无帝兵 no-op）、/菜单 与 /简介 指令、
/特殊 指令、名字解析（基础名命中带品质后缀 / 多品质优先序）、
equip_pct 品质倍率口径、三档赢法都算击杀**。

**概率性断言的处理规矩**：走火入魔 5%、探索掉落权重都是真随机，
不能写死单一路径。验收标准是**连跑 8 次全绿无抖动** ——
只跑一次的"全绿"不可信。

**为什么需要独立的端到端脚本**：`selftest.py` 里的帝兵断言是直接调
`advance_passives()` 的，**永远测不出「什么算击杀」这一层的错**。
真实 bug（碾压不算击杀）就是靠 `verify_v34_special.py`
走完整 `/历练` 指令流程才抓到的。教训：**夹具必须与真实路径一致**，
否则测试会替你漂白。

---

## 十二·五、数值实验室（`_balance/lab/`）

改动数值后**必跑**的体检工具，一条命令出完整报告：

```bash
cd _balance
python -m lab.report           # 完整体检（6 节）
python -m lab.baseline check   # 与黄金基线对比，超 3pp 告警
python -m lab.baseline save    # 确认改动无误后更新基线
```

| 节 | 内容 |
|---|---|
| [1] 环境自检 | 装备表条数、字段完整性、套装表单调性、**品质口径一致性** |
| [2] 品质口径 | 裸名 vs 带后缀名必须算出同一份加成；品质倍率单调递增 |
| [3] 收益归因 | 每个品阶每套系，拆出**面板增益 / 机制增益 / 总增益** |
| [4] 强度对齐 | 三流派收益差是否在容忍范围内（合成口径，容差 4pp）|
| [5] 战斗模型 | 各境界 TTK / 可扛回合 / **安全边际** |
| [6] 守门汇总 | 汇总所有问题 |

**收益归因三层拆解**（相对同转数裸装）：

```
面板增益 = 纯面板 / 裸装 - 1     只看装备给的属性
机制增益 = 完整   / 纯面板 - 1   只看破防/暴击/反伤等机制
总增益   = 完整   / 裸装   - 1   玩家实际拿到的
```

**为什么必须拆开**：第一版验证只测「机制有没有触发」，
结果肉盾套反伤「触发了 1216 次」看起来正常，
但实际输出贡献是 **0%**（那些伤害加起来不到怪物血量的千分之一）。
**「触发了」和「有意义」是两件事。**

**强度对齐为什么用「合成增益」**：肉盾套的机制全长在防御侧，
输出增益天然接近 0。只看输出会误判它失衡 —— 那是判据错了，不是数值错了。
所以用 `合成 = 输出 + 生存` 做判定。

---

## 十三、兼容性承诺

v2 → v3 / v3.0 → v3.1 的所有变化都保证**老玩家不丢档**：

- 旧指令全部保留为别名（`/打怪` `/挑战` `/复活` `/商店` `/背包` `/装备` `/卸下`）
- 旧物品名走别名映射，老存档里的东西照样能用
- `level` 字段复用为转数，语义变化但数据不丢
- 装备从 3 部位扩到 5 部位，`equipment` 是 dict，**缺的部位自然为空**，
  老存档无需迁移

---

## 十四、已知限制

- 没有 PVP 与玩家交易
- 排行榜只显示境界，没有灵石榜、击杀榜、秘境层数榜
- 每日内容只有 `/签到` 与 `/秘境`，没有成就系统
- VIP 只能由天道手动发放（**刻意的** —— 发放权不外包，
  没有卡密 / 自助激活 / 消费自动升级）
- 大道秘典首期 5 条 —— 世界观上是「三千大道」，
  但按老板要求「做点能玩的扩展就行」，逐步扩充而非一次做满
- 秘境 30 层以后区分度收敛（复利的天花板），
  高转数玩家之间拉不开差距
